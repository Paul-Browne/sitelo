import { a, h2, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/it.js'
import { islandsSnippets } from '../../lib/snippets/islands.js'

const s = islandsSnippets('it')

export default () =>
  docsLayout({
    title: 'Island server',
    description:
      'Mantieni le pagine statiche e renderizza sul server, al momento della richiesta, le regioni che marchi.',
    activeHref: '/it/docs/islands',
    children: [
      p(
        'A volte una sola regione di una pagina per il resto statica ha bisogno di dati freschi a ogni richiesta — i commenti sotto un articolo in cache, un badge di disponibilità su una pagina prodotto. Le island server tengono la pagina statica e renderizzano solo quella regione su un server quando la pagina viene visitata.',
      ),
      h2('1. Scrivi l’island'),
      p(
        'Un’island è un modulo-frammento sotto ',
        code('src/islands/'),
        ' — un semplice file ',
        code('.js'),
        ' o ',
        code('.ts'),
        ' (non ',
        code('.ht.js'),
        ', perché le island sono frammenti, non pagine). Stessa idea che vale ovunque in sitelo: una funzione che restituisce HTML.',
      ),
      codeBlock('src/islands/comments.js', s.islandModule, 'javascript'),
      p(
        'Riceve ',
        code('{ name, props, request }'),
        ' e deve restituire una stringa HTML. I moduli island vivono solo sul server — il codice non referenziato sotto ',
        code('src/'),
        ' non arriva mai al browser.',
      ),
      h2('2. Mettila in una pagina'),
      p(
        'Importa ',
        code('island()'),
        ' da ',
        code('sitelo/islands'),
        '. La build statica pubblica l’HTML di riserva; le props sono incorporate nel segnaposto, quindi tienile piccole e non segrete.',
      ),
      pageCodeTabs({
        file: 'src/blog/[slug].ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Aggiungi il loader client'),
      p(
        'Un minuscolo script scarica ogni frammento renderizzato e lo scambia al suo posto. Passa dalla normale pipeline delle risorse, quindi basta un semplice punto di ingresso ',
        code('src/islands.js'),
        ':',
      ),
      codeBlock('src/islands.js', s.loader, 'javascript'),
      p(
        'Con ',
        code('sitelo'),
        ' (sviluppo) e ',
        code('sitelo preview'),
        ' funziona già — entrambi servono le island su ',
        code('/_sitelo/islands/<nome>'),
        ' a partire da ',
        code('src/islands/'),
        '. La preview carica moduli ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' nativi (come un host Node); in sviluppo le island TypeScript sono supportate tramite Vite.',
      ),
      h2('Produzione'),
      p(
        'Il tuo host statico continua a servire le pagine. Monta un piccolo handler ovunque tu esegua codice server — Node, serverless o una funzione edge — e renderizzerà gli stessi moduli island. Per una guida completa con un host Node eseguibile più gli stub per Netlify e Vercel, vedi l’',
        a({ href: '/it/examples/islands' }, 'esempio sulle island server'),
        '.',
      ),
      codeBlock('islands-function.js', s.server, 'javascript'),
      p(
        'Su http di Node o su express, usa invece ',
        code('createIslandsNodeHandler(options)'),
        ' — stesse opzioni, firma ',
        code('(req, res, next)'),
        '. Collega automaticamente ogni modulo ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' sotto ',
        code('src/islands/'),
        ' con ',
        code('createIslandsFromDirectory'),
        '. Se il loader scarica da un’origine o da un percorso diverso, passa ',
        code("mountIslands({ endpoint: 'https://api.example.com/islands' })"),
        ' e fallo combaciare con l’opzione ',
        code('endpoint'),
        ' dell’handler.',
      ),
      h2('Checklist di stabilità'),
      ul(
        { class: 'docs-list' },
        li(
          code('island()'),
          ' / ',
          code('mountIslands()'),
          ' / ',
          code('createIslandsHandler'),
          ' / ',
          code('createIslandsNodeHandler'),
          ' — API pubblica, considerata stabile',
        ),
        li(
          code('sitelo'),
          ' e ',
          code('sitelo preview'),
          ' servono entrambi ',
          code('/_sitelo/islands'),
          ' a partire da ',
          code('src/islands/'),
        ),
        li(
          code('createIslandsFromDirectory'),
          ' — la stessa mappa per gli host Node e per la preview (',
          code('.js'),
          ' / ',
          code('.mjs'),
          ' / ',
          code('.cjs'),
          ' nativi)',
        ),
        li(
          'Stub di host nell’',
          a({ href: '/it/examples/islands' }, 'esempio sulle island server'),
          ': ',
          code('server.js'),
          ' per Node, function + rewrite per Netlify, serverless + rewrite per Vercel',
        ),
        li(
          'Le props restano piccole e non segrete (query string di una GET) — è voluto; recupera i segreti dentro l’island, sul server',
        ),
        li(
          'Le props arrivano dal client — validale, oppure firmale con ',
          code('SITELO_ISLANDS_SECRET'),
        ),
      ),
      h2('Strategie di caricamento'),
      p(
        'Per impostazione predefinita ogni island viene scaricata appena la pagina si carica, quindi una pagina con otto island fa otto richieste contemporanee durante il primo paint. Passa ',
        code('when'),
        ' per rimandare quelle che non sono subito visibili.',
      ),
      codeBlock('islands-when', s.strategy, 'js'),
      ul(
        { class: 'docs-list' },
        li(
          code("'load'"),
          ' (predefinita) — subito, insieme a ogni altra island',
        ),
        li(
          code("'idle'"),
          ' — su ',
          code('requestIdleCallback'),
          ' (con ripiego su un timeout)',
        ),
        li(
          code("'visible'"),
          ' — quando entra nella vista, tramite IntersectionObserver',
        ),
      ),
      p(
        code('rootMargin'),
        ' vale solo per ',
        code("'visible'"),
        ' e per impostazione predefinita è ',
        code("'200px'"),
        '. Le island vanno anche in timeout invece di girare a vuoto per sempre:',
      ),
      codeBlock('islands-mount-options', s.mountOptions, 'js'),
      p(
        code('mountIslands()'),
        ' si risolve quando le island immediate si sono assestate — quelle rimandate si caricano più tardi per conto loro e deliberatamente non vengono attese. Un’island fallita o andata in timeout si tiene il suo HTML di riserva.',
      ),
      h2('Le props sono input non fidato'),
      p(
        'Le props di un’island arrivano dal client. Sono incorporate nella pagina, rispedite con la richiesta, e chiunque può modificarle prima:',
      ),
      codeBlock('islands-forged', s.forged, 'http'),
      p(
        'Tratta le props che la tua island riceve esattamente come un parametro di query — validale, e non usarle mai per recuperare dati che chi guarda non ha già il diritto di vedere.',
      ),
      p(
        'Quando le props selezionano dati riservati, firmale. Imposta un segreto e sitelo firma ogni segnaposto in fase di build, respingendo tutto il resto con un ',
        code('403'),
        ':',
      ),
      codeBlock('islands-secret', s.secret, 'bash'),
      p(
        'La stessa variabile viene letta da ',
        code('sitelo'),
        ', ',
        code('sitelo preview'),
        ' e ',
        code('createIslandsHandler'),
        ', così sviluppo, preview e produzione vanno d’accordo. Dai lo stesso segreto al tuo host di produzione. Preferisci impostarlo nel codice?',
      ),
      codeBlock('islands-configure', s.configure, 'js'),
      p(
        'Le firme sono HMAC-SHA256 sul nome dell’island e sulle sue props, quindi una firma emessa per un’island non può essere riusata su un’altra. Firmare dimostra che le props vengono dalla tua build — non le nasconde, perciò devono comunque restare non segrete. Senza un segreto le props vengono accettate così come sono, e validarle è interamente compito del tuo modulo island.',
      ),
      h2('Buono a sapersi'),
      ul(
        { class: 'docs-list' },
        li(
          'Nessun endpoint per le island pubblicato? L’HTML di riserva semplicemente resta — le pagine degradano con grazia.',
        ),
        li(
          'Le richieste sono ',
          code('GET'),
          ' con le props nella query string, quindi le risposte si possono mettere in cache — imposta l’opzione ',
          code('cacheControl'),
          ' dell’handler se vuoi che una CDN trattenga i frammenti per un po’.',
        ),
        li(
          'Durante il caricamento il segnaposto porta ',
          code('data-sitelo-island-state="loading"'),
          ' (poi ',
          code('loaded'),
          ' oppure ',
          code('error'),
          ') — comodo per il CSS.',
        ),
      ),
    ],
  })
