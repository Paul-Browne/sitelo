import {
  a,
  div,
  h2,
  h3,
  li,
  p,
  table,
  tbody,
  td,
  th,
  thead,
  tr,
  ul,
} from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/it.js'
import { configurationSnippets } from '../../lib/snippets/configuration.js'

const s = configurationSnippets('it')

export default () =>
  docsLayout({
    title: 'Configurazione',
    description:
      'Opzioni del plugin in sitelo.config.js, impostazioni di Vite, sitemap, RSS e Pagefind.',
    activeHref: '/it/docs/configuration',
    children: [
      p(
        'Metti le opzioni del plugin e le eventuali impostazioni di Vite in ',
        code('sitelo.config.js'),
        '. Nessun ',
        code('vite.config.js'),
        ' è necessario.',
      ),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('Opzioni del plugin'),
      ul(
        { class: 'docs-list' },
        li(code('pagesDir'), ' — predefinito ', code("'src'")),
        li(code('pageExtensions'), ' — quali suffissi contano come pagine'),
        li(
          code('cleanUrls'),
          ' — predefinito ',
          code('true'),
          ' (',
          code('/about/index.html'),
          ')',
        ),
        li(code('site'), ' — URL di base; abilita ', code('sitemap.xml')),
        li(code('rss'), ' — configurazione del feed RSS'),
        li(
          code('pagefind'),
          ' — ',
          code('true'),
          ' oppure un oggetto di opzioni; indicizza il sito con ',
          a({ href: 'https://pagefind.app', rel: 'noopener' }, 'Pagefind'),
          ' dopo ',
          code('sitelo build'),
          ' (richiede ',
          code('npm install -D pagefind'),
          ')',
        ),
        li(
          code('images'),
          ' — ',
          code('true'),
          ' oppure un oggetto di opzioni; ',
          a({ href: '/it/docs/images' }, 'ottimizza le immagini'),
          ' in sviluppo e dopo ',
          code('sitelo build'),
          ' (richiede ',
          code('npm install -D sharp'),
          ')',
        ),
        li(code('missingAssets'), ' — ', code("'error'"), ' oppure ', code("'warn'")),
        li(
          code('linkCheck'),
          ' — link interni morti (vedi ',
          a({ href: '#controllo-dei-link' }, 'Controllo dei link'),
          ')',
        ),
        li(
          code('lighthouse'),
          ' — audit Lighthouse della build (vedi ',
          a({ href: '#audit-lighthouse' }, 'Audit Lighthouse'),
          ')',
        ),
        li(code('generatedTypesDir'), ' — predefinito ', code("'.sitelo/types'")),
        li(
          code('renderConcurrency'),
          ' / ',
          code('renderBatchSize'),
          ' — parallelismo della build',
        ),
        li(
          code('buildReport'),
          ' — predefinito ',
          code('true'),
          '; riepilogo post-build di pagine, dimensione dell’output, file più grandi e tempi delle fasi. ',
          code('false'),
          ' per disattivarlo, oppure ',
          code('{ top }'),
          ' per cambiare quanti file grandi vengono elencati',
        ),
        li(
          code('pruneCss'),
          ' — predefinito ',
          code('false'),
          '; ',
          code('true'),
          ' oppure ',
          code('{ keep }'),
          ' scrive il ',
          a({ href: '/it/docs/ui#stili' }, 'foglio di stile di sitelo/ui'),
          ' con le sole regole che le pagine costruite riescono ad agganciare',
        ),
        li(code('debug'), ' — log prolisso'),
        li(
          code('devToolbar'),
          ' — predefinito ',
          code('true'),
          '; metti ',
          code('false'),
          ' per nascondere la toolbar presente solo in sviluppo (file sorgente, parametri, numero di island, selettore di viewport)',
        ),
        li(
          code('devToolbarDocsUrl'),
          ' — link alla documentazione nella toolbar (predefinito ',
          code('https://sitelo.dev/docs'),
          ')',
        ),
      ),
      h2('Toolbar di sviluppo'),
      p(
        'Mentre ',
        code('sitelo'),
        ' (sviluppo) è in esecuzione, una piccola barra in fondo a ogni pagina mostra il file della pagina, i parametri e quante ',
        a({ href: '/it/docs/islands' }, 'island server'),
        ' ci sono nella pagina. Usa il pulsante del viewport per ciclare fra le anteprime ',
        code('Desktop'),
        ' / ',
        code('Tablet'),
        ' / ',
        code('Mobile'),
        ' (in un iframe, così le media query combaciano), e ',
        code('Copy'),
        ' per un blocco di informazioni di debug quando apri una segnalazione. Non compare mai nell’output di ',
        code('sitelo build'),
        '.',
      ),
      codeBlock('sitelo.config.js', s.devToolbarOff, 'javascript'),
      h2('Opzioni di Vite'),
      p(
        'Tutto ciò che sta sotto ',
        code('vite'),
        ' viene fuso nella configurazione di Vite. I flag della CLI (per esempio ',
        code('--port'),
        ') hanno la precedenza su entrambi.',
      ),
      p(
        'Le build di sitelo impostano ',
        code('build.rollupOptions.checks.pluginTimings: false'),
        '. Altrimenti Rolldown segnala che gli hook dei plugin dominano la build, cosa che su un sito sitelo è sempre vera — generare le pagine è la build — quindi nomina lo stesso plugin a ogni esecuzione. Mettilo a ',
        code('true'),
        ' sotto ',
        code('vite'),
        ' quando profili i tuoi plugin.',
      ),
      h2('Un vite.config.js esistente'),
      p(
        'È ancora supportato — o solo opzioni di Vite, oppure controllo completo del plugin:',
      ),
      codeBlock('vite.config.js', s.viteOnly, 'javascript'),
      codeBlock('vite.config.js', s.vitePlugin, 'javascript'),
      p(
        'Se il plugin è già nella tua configurazione di Vite, metti lì le opzioni del plugin — e non anche come opzioni del plugin in ',
        code('sitelo.config.js'),
        ' (sitelo darà errore).',
      ),
      h2('Sitemap e RSS'),
      p(
        'Imposta ',
        code('site'),
        ' per produrre ',
        code('dist/sitemap.xml'),
        '.',
      ),
      p('RSS:'),
      codeBlock('sitelo.config.js', s.rss, 'javascript'),
      p(
        'Produce ',
        code('dist/rss.xml'),
        ' con un elemento per ogni pagina sotto ',
        code('routePrefix'),
        '.',
      ),
      h2('Controllo dei link'),
      p(
        'Uno ',
        code('<script src>'),
        ' rotto e gli href dei fogli di stile fanno già fallire la build (vedi ',
        code('missingAssets'),
        '). ',
        code('linkCheck'),
        ' copre l’altra metà: i link interni ',
        code('<a href>'),
        ' che puntano a una pagina che non esiste.',
      ),
      codeBlock('sitelo.config.js', s.linkCheck, 'javascript'),
      p(
        'Dopo ',
        code('sitelo build'),
        ', ogni link interno nell’output viene risolto e tutto ciò che non ha una pagina dietro viene segnalato, raggruppato per la pagina in cui compare:',
      ),
      codeBlock('output', s.linkCheckOutput, 'text'),
      h3('Come si risolvono i link'),
      p(
        'Il controllo gira sul sito prodotto, non sulla tabella delle rotte — quindi tiene conto di ',
        code('cleanUrls'),
        ', dei gruppi di rotte, di ',
        code('mapOutputPath'),
        ', dei file copiati da ',
        code('public/'),
        ' e delle pagine prodotte da rotte dinamiche. Gira anche dopo l’ottimizzazione delle immagini e Pagefind, quindi vede esattamente ciò che viene pubblicato. Un link è valido quando gli risponde un file vero, provato nell’ordine in cui lo farebbe un host statico:',
      ),
      ul(
        { class: 'docs-list' },
        li(
          code('/about'),
          ' → ',
          code('about'),
          ', poi ',
          code('about/index.html'),
          ', poi ',
          code('about.html'),
        ),
        li(
          code('/blog/'),
          ' → ',
          code('blog/index.html'),
          ' (una barra finale significa sempre e solo l’indice di una cartella)',
        ),
        li(code('/'), ' → ', code('index.html')),
      ),
      p(
        'I link relativi (',
        code('../about'),
        ') si risolvono rispetto alla pagina che li contiene, e uno che esce dalla cartella di output viene segnalato. Le query string vengono ignorate nella risoluzione — ',
        code('/about?utm=x'),
        ' controlla ',
        code('/about'),
        '.',
      ),
      p(
        'I link esterni non vengono mai scaricati. ',
        code('https://'),
        ', i relativi al protocollo ',
        code('//cdn.example.com'),
        ', ',
        code('mailto:'),
        ', ',
        code('tel:'),
        ' e gli altri schemi vengono saltati del tutto.',
      ),
      h3('Opzioni'),
      div(
        { class: 'docs-table-scroll' },
        table(
          { class: 'docs-table docs-table--wrap-last' },
          thead(tr(th('Opzione'), th('Predefinito'), th('Descrizione'))),
          tbody(
            tr(
              td(code('mode')),
              td(code("'warn'")),
              td(
                code("'warn'"),
                ' registra e prosegue; ',
                code("'error'"),
                ' fa fallire la build — utile in CI',
              ),
            ),
            tr(
              td(code('exclude')),
              td(code('[]')),
              td('Glob o espressioni regolari di href da saltare'),
            ),
            tr(
              td(code('checkFragments')),
              td(code('false')),
              td(
                'Verifica anche che i target ',
                code('#frammento'),
                ' esistano nella pagina collegata',
              ),
            ),
          ),
        ),
      ),
      codeBlock('sitelo.config.js', s.linkCheckOptions, 'javascript'),
      p(
        code('checkFragments'),
        ' è disattivato per impostazione predefinita perché gli id aggiunti da JavaScript lato client non sono nell’HTML costruito, e verrebbero segnalati come mancanti. Contano come target di frammento sia gli attributi ',
        code('id'),
        ' sia i vecchi ',
        code('name'),
        ' delle ancore.',
      ),
      h3('Siti serviti da una base'),
      p(
        'Se il tuo sito è pubblicato sotto una ',
        code('base'),
        ' (per esempio un sito di progetto su GitHub Pages), un link relativo alla radice che non porta quella base viene segnalato. ',
        code('/about'),
        ' su un sito servito da ',
        code('/repo/'),
        ' manda il browser alla radice dell’host, non dentro il tuo sito — risolverlo comunque rispetto all’output nasconderebbe esattamente l’errore che vale la pena cogliere. Usa ',
        code('exclude'),
        ' quando è voluto.',
      ),
      h2('Audit Lighthouse'),
      p(
        code('sitelo lighthouse'),
        ' analizza la build finita con ',
        a(
          {
            href: 'https://github.com/GoogleChrome/lighthouse',
            rel: 'noopener',
          },
          'Lighthouse',
        ),
        '. È una peer dependency opzionale:',
      ),
      codeBlock('shell', s.installLighthouse, 'bash'),
      p(
        'sitelo serve ',
        code('dist/'),
        ' nello stesso modo di ',
        code('sitelo preview'),
        ', punta un Chrome headless a ogni pagina e stampa i punteggi:',
      ),
      codeBlock('output', s.lighthouseOutput, 'text'),
      p(
        'Ogni pagina viene analizzata all’URL con cui il sito la collega — ',
        code('/docs'),
        ', mai ',
        code('dist/docs.html'),
        ' — quindi ',
        code('cleanUrls'),
        ', i gruppi di rotte e una ',
        code('base'),
        ' sono tutti tenuti in conto. Aggiungi delle soglie e il report diventa un controllo: qualunque punteggio sotto la sua soglia fa fallire il comando.',
      ),
      codeBlock('sitelo.config.js', s.lighthouse, 'javascript'),
      p(
        'I punteggi si scrivono nel modo in cui li mostra Lighthouse (',
        code('0'),
        '–',
        code('100'),
        '); funzionano anche le sue frazioni ',
        code('0'),
        '–',
        code('1'),
        '. Usa ',
        code("mode: 'warn'"),
        ' per registrare invece di far fallire.',
      ),
      ul(
        { class: 'docs-list' },
        li(
          code('include'),
          ' / ',
          code('exclude'),
          ' — quali pagine vengono analizzate (glob o espressioni regolari)',
        ),
        li(
          code('sample'),
          ' — analizza questo numero di pagine casuali per ogni pattern ',
          code('include'),
          ' invece di tutte',
        ),
        li(
          code('categories'),
          ' — ',
          code('performance'),
          ', ',
          code('accessibility'),
          ', ',
          code('best-practices'),
          ', ',
          code('seo'),
        ),
        li(code('thresholds'), ' — il punteggio minimo per categoria'),
        li(
          code('mode'),
          ' — ',
          code("'error'"),
          ' (predefinito) oppure ',
          code("'warn'"),
        ),
        li(
          code('formFactor'),
          ' — ',
          code("'mobile'"),
          ' (predefinito), ',
          code("'desktop'"),
          ', oppure entrambi',
        ),
        li(code('runs'), ' — ripeti l’audit e riporta il punteggio mediano'),
        li(
          code('output'),
          ' / ',
          code('formats'),
          ' — salva il report Lighthouse completo di ogni pagina',
        ),
        li(
          code('flags'),
          ' / ',
          code('config'),
          ' — passati direttamente a Lighthouse, quindi funziona tutto ciò che accetta la sua CLI',
        ),
        li(
          code('onBuild'),
          ' — esegui l’audit anche alla fine di ',
          code('sitelo build'),
        ),
      ),
      codeBlock('sitelo.config.js', s.lighthouseOptions, 'javascript'),
      p(
        'Lighthouse guida un Chrome vero: installane uno dove gira l’audit, oppure punta ',
        code('CHROME_PATH'),
        ' a un eseguibile. I punteggi di prestazione si muovono fra un’esecuzione e l’altra, quindi usa ',
        code('runs: 3'),
        ' prima di fissarci sopra una soglia.',
      ),
      h2('Ricerca con Pagefind'),
      p(
        'Ricerca statica opzionale mossa da ',
        a({ href: 'https://pagefind.app', rel: 'noopener' }, 'Pagefind'),
        ', una peer dependency facoltativa. Installala quando vuoi la ricerca, poi abilita l’indicizzazione, marca i contenuti, monta l’interfaccia ed esegui ',
        code('sitelo build'),
        '.',
      ),
      codeBlock('shell', s.installPagefind, 'bash'),
      h3('1. Abilita l’indicizzazione'),
      p(
        'Imposta ',
        code('pagefind: true'),
        '. Dopo ',
        code('sitelo build'),
        ' ottieni ',
        code('dist/pagefind/'),
        ', e per impostazione predefinita una copia in ',
        code('public/pagefind/'),
        ' così il prossimo ',
        code('sitelo'),
        ' (sviluppo) o ',
        code('sitelo preview'),
        ' può servire ',
        code('/pagefind/'),
        ' senza ricostruire.',
      ),
      codeBlock('sitelo.config.js', s.pagefind, 'javascript'),
      h3('2. Marca i contenuti e aggiungi un punto d’innesto'),
      p(
        'Metti ',
        code('data-pagefind-body'),
        ' sul contenuto principale così navigazione e piè di pagina non vengono indicizzati. Lascia un elemento vuoto per l’interfaccia di ricerca:',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pagefindPageTemplate,
        ht: s.pagefindPageHt,
        jsx: s.pagefindPageJsx,
      }),
      h3('3. Monta l’interfaccia di Pagefind'),
      p(
        'Carica ',
        code('/pagefind/pagefind-ui.js'),
        ' e ',
        code('pagefind-ui.css'),
        ' dal tuo script client (solo dopo che una build ha prodotto l’indice):',
      ),
      codeBlock('src/main.js', s.pagefindMain, 'javascript'),
      h3('4. Fai la build e ignora il bundle sincronizzato'),
      codeBlock('shell', s.pagefindBuild, 'bash'),
      codeBlock('.gitignore', s.pagefindGitignore, 'bash'),
      p(
        'Le opzioni avanzate (',
        code('syncPublic'),
        ', ',
        code('glob'),
        ', lingua, selettori, …) vanno su un oggetto: ',
        code("pagefind: { syncPublic: false, glob: '**/*.html' }"),
        '. Opzioni complete dell’interfaccia: ',
        a({ href: 'https://pagefind.app', rel: 'noopener' }, 'pagefind.app'),
        '.',
      ),
      h3('404'),
      p(
        'Crea ',
        code('src/404.ht.js'),
        ' per ottenere ',
        code('dist/404.html'),
        '. Altrimenti ne viene generata una predefinita e pulita.',
      ),
    ],
  })
