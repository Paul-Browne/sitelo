import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/it.js'
import { pagesSnippets } from '../../lib/snippets/pages.js'

const s = pagesSnippets('it')

export default () =>
  docsLayout({
    title: 'Scrivere pagine',
    description:
      'Come si scrivono le pagine sitelo — template literal, ht.js (consigliato) o JSX.',
    activeHref: '/it/docs/pages',
    children: [
      p(
        'Qualunque file che termina con un’estensione di pagina è una pagina. Estensioni predefinite: ',
        code('.ht.js'),
        ', ',
        code('.html.js'),
        ', e le varianti TypeScript / JSX (',
        code('.ht.ts'),
        ', ',
        code('.ht.tsx'),
        ', …).',
      ),
      p(
        'Se l’output inizia con ',
        code('<html>'),
        ', sitelo antepone ',
        code('<!DOCTYPE html>'),
        ' automaticamente. Per il markup preferisci ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' (',
        code('javascript-to-html'),
        '): funzioni-tag che restituiscono stringhe, senza motori di template né runtime React.',
      ),
      h2('1. Una funzione che restituisce HTML'),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.fnTemplate,
        ht: s.fnHt,
        jsx: s.fnJsx,
      }),
      p(
        'I file JSX usano ',
        code('*.ht.jsx'),
        ' / ',
        code('*.ht.tsx'),
        ' e richiedono ',
        code('react'),
        ' + ',
        code('react-dom'),
        ' installati. Vedi le ',
        a({ href: '#limitazioni-di-jsx' }, 'limitazioni di JSX'),
        ' più sotto — sitelo trasforma JSX in HTML statico in fase di build; non è un’app React nel browser.',
      ),
      h2('2. Una semplice stringa'),
      codeBlock('page.ht.js', s.string, 'javascript'),
      h2('3. Un modulo strutturato'),
      p(
        'Tieni insieme ',
        code('render'),
        ', ',
        code('data'),
        ' e ',
        code('generateStaticParams'),
        ':',
      ),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.structuredTemplate,
        ht: s.structuredHt,
        jsx: s.structuredJsx,
      }),
      h2('Limitazioni di JSX'),
      p(
        'sitelo può renderizzare pagine ',
        code('.ht.jsx'),
        ' / ',
        code('.ht.tsx'),
        ' con ',
        code('react-dom/server'),
        ', così puoi scrivere con la sintassi JSX che conosci. È una comodità di scrittura — non un’applicazione React.',
      ),
      p(
        'React è pensato per il browser: un albero di componenti che si monta, si idrata e risponde agli eventi. sitelo non spedisce mai quel runtime insieme alle tue pagine. In fase di build renderizza il tuo JSX in una stringa HTML, scrive il file e si ferma. Non c’è alcun bundle React lato client, nessuna idratazione e nessun virtual DOM in attesa nel browser.',
      ),
      p('Per questo:'),
      ul(
        { class: 'docs-list' },
        li(
          'I gestori di eventi (',
          code('onClick'),
          ', ',
          code('onChange'),
          ', …) non fanno nulla nel sito finito — non c’è niente in ascolto. Il server di sviluppo avvisa se li trova.',
        ),
        li(
          'Gli hook (',
          code('useState'),
          ', ',
          code('useEffect'),
          ', …) non fanno parte della pagina pubblicata. Possono girare una volta durante il render di build; non mantengono stato nel browser.',
        ),
        li(
          'Le API disponibili solo nel browser (',
          code('window'),
          ', ',
          code('document'),
          ') non esistono mentre la pagina viene renderizzata in fase di build.',
        ),
        li(
          code('dangerouslySetInnerHTML'),
          ' funziona per iniettare stringhe HTML; l’interattività richiede comunque uno ',
          code('<script>'),
          ' client separato.',
        ),
      ),
      p(
        'Per clic, form e altri comportamenti lato client, collega un normale script tramite la ',
        a({ href: '/it/docs/assets' }, 'pipeline delle risorse'),
        ', oppure usa le ',
        a({ href: '/it/docs/islands' }, 'island server'),
        ' per HTML generato al momento della richiesta. Quando non ti serve la sintassi JSX, preferisci ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' o i template literal — nessuna installazione di React, e il modello combacia con “HTML in fase di build”.',
      ),
      h2('Contesto di render'),
      p('Ogni funzione di pagina riceve un solo argomento:'),
      h3('params'),
      p(
        'I parametri di rotta di questa pagina (',
        code('Record<string, string | string[]>'),
        ').',
      ),
      h3('data'),
      p('Qualunque cosa abbia restituito la tua funzione ', code('data()'), '.'),
      h3('page'),
      p(
        'Metadati della rotta (',
        code('routePath'),
        ', ',
        code('relativePath'),
        ', …).',
      ),
      h3('dev'),
      p(
        code('true'),
        ' nel server di sviluppo, ',
        code('false'),
        ' in fase di build.',
      ),
    ],
  })
