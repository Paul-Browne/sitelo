import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, examplesLayout, pageCodeTabs } from '../../lib/it.js'
import { islandsExampleSnippets } from '../../lib/snippets/examples-islands.js'

const s = islandsExampleSnippets('it')

export default () =>
  examplesLayout({
    title: 'Island server',
    description:
      'Pubblica un sito sitelo statico e monta un host Node che renderizza le island al momento della richiesta.',
    activeHref: '/it/examples/islands',
    children: [
      p(
        'sitelo costruisce HTML statico. Le island server riempiono le parti che devono essere fresche — un orologio, i commenti, la disponibilità, tutto ciò che ha bisogno della richiesta. Questa ricetta costruisce una pagina con un’island dell’ora, poi avvia un piccolo server Node che serve ',
        code('dist/'),
        ' e ',
        code('/_sitelo/islands'),
        '.',
      ),
      p(
        'Una copia di questo progetto si trova nel repository di sitelo sotto ',
        code('examples/islands/'),
        '.',
      ),
      h2('Cosa ottieni'),
      ul(
        { class: 'docs-list' },
        li(
          'Una home page statica con un segnaposto per l’island e l’HTML di riserva',
        ),
        li(
          'Un modulo island destinato solo al server sotto ',
          code('src/islands/'),
          ', che non arriva mai al browser',
        ),
        li(
          'Un loader client che sostituisce il frammento preso da ',
          code('/_sitelo/islands/<nome>'),
        ),
        li(
          'Un ',
          code('server.js'),
          ' con la forma della produzione — file statici + ',
          code('createIslandsNodeHandler'),
        ),
      ),
      h2('Struttura del progetto'),
      codeBlock('project', s.structure, 'bash'),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('1. Modulo island'),
      p(
        'Un semplice ',
        code('.js'),
        ' (non ',
        code('.ht.js'),
        '). Riceve ',
        code('{ name, props, request }'),
        ' e restituisce una stringa HTML. Questo usa l’ora della richiesta e lo user-agent, così vedi che viene renderizzato a ogni richiesta.',
      ),
      codeBlock('src/islands/time.js', s.island, 'javascript'),
      h2('2. Metti l’island in una pagina'),
      p(
        code('island()'),
        ' incorpora le props nel segnaposto. La build pubblica il contenuto di riserva; il loader lo sostituisce quando l’endpoint risponde.',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Loader client'),
      codeBlock('src/js/islands.js', s.loader, 'javascript'),
      codeBlock('src/css/styles.css', s.styles, 'css'),
      h2('4. Host Node'),
      p(
        'Dopo ',
        code('sitelo build'),
        ', questo processo serve ',
        code('dist/'),
        ' e renderizza le island con ',
        code('createIslandsNodeHandler'),
        ' da ',
        code('sitelo/islands/server'),
        '. I moduli island restano fuori da ',
        code('dist/'),
        ' — l’host li importa da ',
        code('src/'),
        '.',
      ),
      codeBlock('server.js', s.server, 'javascript'),
      h2('5. Build ed esecuzione'),
      codeBlock('shell', s.buildRun, 'bash'),
      p(
        'Apri ',
        code('http://localhost:3000'),
        '. Dovresti vedere per un attimo il contenuto di riserva, poi l’ora del server. Ricarica — l’orario cambia. Con ',
        code('sitelo'),
        ' (sviluppo) e ',
        code('sitelo preview'),
        ' non ti serve ',
        code('server.js'),
        ': la CLI serve già ',
        code('/_sitelo/islands'),
        '.',
      ),
      h2('Deploy'),
      p(
        'Questo esempio porta con sé degli stub di host accanto al server Node:',
      ),
      ul(
        { class: 'docs-list' },
        li(
          code('Node'),
          ' — ',
          code('npm run build && npm start'),
          ' (imposta ',
          code('PORT'),
          ' sulla piattaforma). Usa ',
          code('createIslandsFromDirectory'),
          '.',
        ),
        li(
          code('Netlify'),
          ' — ',
          code('netlify.toml'),
          ' riscrive ',
          code('/_sitelo/islands/*'),
          ' verso ',
          code('netlify/functions/islands.mjs'),
          '.',
        ),
        li(
          code('Vercel'),
          ' — ',
          code('vercel.json'),
          ' riscrive verso ',
          code('api/islands/[...path].js'),
          '.',
        ),
        li(
          'Host solo statici (GitHub Pages, S3 puro) — i segnaposto si tengono il contenuto di riserva finché non aggiungi una funzione.',
        ),
      ),
      p(
        'Per serverless o edge altrove, usa ',
        code('createIslandsHandler'),
        ' (',
        code('Request'),
        ' web → ',
        code('Response'),
        ') — vedi la ',
        a({ href: '/it/docs/islands' }, 'documentazione sulle island server'),
        '. Punta ',
        code('mountIslands({ endpoint })'),
        ' all’URL di quella funzione se non è della stessa origine.',
      ),
      h2('Note'),
      h3('Host solo statici'),
      p(
        'GitHub Pages, S3 puro e host simili non hanno un processo server. Senza un endpoint per le island l’HTML di riserva semplicemente resta — le pagine funzionano lo stesso, solo senza il frammento dal vivo.',
      ),
      h3('Tieni le props piccole'),
      p(
        'Le props viaggiano nell’attributo HTML e nella query string della richiesta. Non metterci segreti o carichi grossi — recuperali dentro il modulo island, sul server.',
      ),
      p(
        a({ href: '/it/docs/islands' }, 'Documentazione sulle island server'),
        ' · ',
        a({ href: '/it/examples/basic' }, 'Sito di base / deploy'),
        ' · ',
        a({ href: '/it/examples' }, 'Tutti gli esempi'),
      ),
    ],
  })
