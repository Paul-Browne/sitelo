import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/zh.js'
import { pagesSnippets } from '../../lib/snippets/pages.js'

const s = pagesSnippets('zh')

export default () =>
  docsLayout({
    title: '编写页面',
    description: '如何编写 sitelo 页面 —— 模板字符串、ht.js（推荐）或 JSX。',
    activeHref: '/zh/docs/pages',
    children: [
      p(
        '任何以页面扩展名结尾的文件都是一个页面。默认扩展名：',
        code('.ht.js'),
        '、',
        code('.html.js'),
        '，以及 TypeScript / JSX 变体（',
        code('.ht.ts'),
        '、',
        code('.ht.tsx'),
        '，…）。',
      ),
      p(
        '如果输出以 ',
        code('<html>'),
        ' 开头，sitelo 会自动补上 ',
        code('<!DOCTYPE html>'),
        '。标记推荐使用 ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        '（',
        code('javascript-to-html'),
        '）：返回字符串的标签函数，不需要模板引擎，也不需要 React 运行时。',
      ),
      h2('1. 一个返回 HTML 的函数'),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.fnTemplate,
        ht: s.fnHt,
        jsx: s.fnJsx,
      }),
      p(
        'JSX 文件使用 ',
        code('*.ht.jsx'),
        ' / ',
        code('*.ht.tsx'),
        '，并需要安装 ',
        code('react'),
        ' 和 ',
        code('react-dom'),
        '。请看下方的',
        a({ href: '#jsx-的限制' }, 'JSX 的限制'),
        ' —— sitelo 在构建时把 JSX 变成静态 HTML；它不是浏览器里的 React 应用。',
      ),
      h2('2. 一个普通字符串'),
      codeBlock('page.ht.js', s.string, 'javascript'),
      h2('3. 一个结构化模块'),
      p(
        '把 ',
        code('render'),
        '、',
        code('data'),
        ' 和 ',
        code('generateStaticParams'),
        ' 放在一起：',
      ),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.structuredTemplate,
        ht: s.structuredHt,
        jsx: s.structuredJsx,
      }),
      h2('JSX 的限制'),
      p(
        'sitelo 可以用 ',
        code('react-dom/server'),
        ' 渲染 ',
        code('.ht.jsx'),
        ' / ',
        code('.ht.tsx'),
        ' 页面，让你写熟悉的 JSX 语法。这只是书写上的便利 —— 而不是一个 React 应用。',
      ),
      p(
        'React 是为浏览器而生的：一棵会挂载、会水合、会响应事件的组件树。sitelo 从不把这套运行时随页面一起发布。构建时它把你的 JSX 渲染成 HTML 字符串，写入文件，到此为止。没有客户端 React 包，没有水合，浏览器里也没有等待中的虚拟 DOM。',
      ),
      p('因此：'),
      ul(
        { class: 'docs-list' },
        li(
          '事件处理函数（',
          code('onClick'),
          '、',
          code('onChange'),
          '，…）在成品站点里不起作用 —— 没有任何东西在监听。开发服务器若发现它们会给出警告。',
        ),
        li(
          'Hooks（',
          code('useState'),
          '、',
          code('useEffect'),
          '，…）不会成为已发布页面的一部分。它们可能在构建渲染时执行一次，但不会在浏览器中保存状态。',
        ),
        li(
          '仅浏览器可用的 API（',
          code('window'),
          '、',
          code('document'),
          '）在构建时渲染页面的过程中不可用。',
        ),
        li(
          code('dangerouslySetInnerHTML'),
          ' 可以用来注入 HTML 字符串；交互能力仍然需要单独的客户端 ',
          code('<script>'),
          '。',
        ),
      ),
      p(
        '点击、表单和其他客户端行为，请通过',
        a({ href: '/zh/docs/assets' }, '资源流水线'),
        '引入一个普通脚本，或使用',
        a({ href: '/zh/docs/islands' }, '服务端群岛'),
        '来获得请求时生成的 HTML。当你不需要 JSX 语法时，更适合用 ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' 或模板字符串 —— 无需安装 React，而且这个模型正好对应「构建时产出 HTML」。',
      ),
      h2('渲染上下文'),
      p('每个页面函数只接收一个参数：'),
      h3('params'),
      p(
        '本页面的路由参数（',
        code('Record<string, string | string[]>'),
        '）。',
      ),
      h3('data'),
      p('你的 ', code('data()'), ' 函数返回的内容。'),
      h3('page'),
      p(
        '路由元信息（',
        code('routePath'),
        '、',
        code('relativePath'),
        '，…）。',
      ),
      h3('dev'),
      p(
        '在开发服务器中为 ',
        code('true'),
        '，构建时为 ',
        code('false'),
        '。',
      ),
    ],
  })
