import { a, h2, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/zh.js'
import { islandsSnippets } from '../../lib/snippets/islands.js'

const s = islandsSnippets('zh')

export default () =>
  docsLayout({
    title: '服务端群岛',
    description: '保持页面静态，并在请求时于服务端渲染被标记的区域。',
    activeHref: '/zh/docs/islands',
    children: [
      p(
        '有时候，一个原本静态的页面上会有某个区域需要每次请求都拿到最新数据 —— 缓存博客文章下方的评论、商品页上的库存标签。服务端群岛让页面保持静态，只在有人访问时把那一块放到服务端渲染。',
      ),
      h2('1. 写一个群岛'),
      p(
        '群岛是放在 ',
        code('src/islands/'),
        ' 下的片段模块 —— 一个普通的 ',
        code('.js'),
        ' 或 ',
        code('.ts'),
        ' 文件（不是 ',
        code('.ht.js'),
        '，因为群岛是片段而非页面）。思路和 sitelo 其他地方一样：一个返回 HTML 的函数。',
      ),
      codeBlock('src/islands/comments.js', s.islandModule, 'javascript'),
      p(
        '它接收 ',
        code('{ name, props, request }'),
        '，并且必须返回一个 HTML 字符串。群岛模块只在服务端运行 —— ',
        code('src/'),
        ' 下未被引用的代码永远不会进入浏览器。',
      ),
      h2('2. 放进页面'),
      p(
        '从 ',
        code('sitelo/islands'),
        ' 引入 ',
        code('island()'),
        '。静态构建会发布回退 HTML；props 会被嵌进占位元素，所以请让它们保持精简且不含机密。',
      ),
      pageCodeTabs({
        file: 'src/blog/[slug].ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. 加上客户端加载器'),
      p(
        '一个很小的脚本会取回每个渲染好的片段并替换进去。它走的是常规资源流水线，所以只需要一个普通的 ',
        code('src/islands.js'),
        ' 入口：',
      ),
      codeBlock('src/islands.js', s.loader, 'javascript'),
      p(
        '在 ',
        code('sitelo'),
        '（dev）和 ',
        code('sitelo preview'),
        ' 中这已经能用 —— 两者都会从 ',
        code('src/islands/'),
        ' 在 ',
        code('/_sitelo/islands/<name>'),
        ' 提供群岛。preview 加载原生 ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' 模块（与 Node 宿主一致）；TypeScript 群岛在开发时由 Vite 支持。',
      ),
      h2('生产环境'),
      p(
        '你的静态托管继续提供页面。在任何能运行服务端代码的地方挂一个小处理器 —— Node、serverless 或边缘函数 —— 它就会渲染同样的群岛模块。想要包含可运行 Node 宿主以及 Netlify、Vercel 模板的完整示例，请看',
        a({ href: '/zh/examples/islands' }, '服务端群岛示例'),
        '。',
      ),
      codeBlock('islands-function.js', s.server, 'javascript'),
      p(
        '在原生 Node http 或 express 上，请改用 ',
        code('createIslandsNodeHandler(options)'),
        ' —— 选项相同，签名为 ',
        code('(req, res, next)'),
        '。可以用 ',
        code('createIslandsFromDirectory'),
        ' 自动接入 ',
        code('src/islands/'),
        ' 下的每个 ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' 模块。如果加载器要访问其他源或路径，请传入 ',
        code("mountIslands({ endpoint: 'https://api.example.com/islands' })"),
        '，并与处理器的 ',
        code('endpoint'),
        ' 选项保持一致。',
      ),
      h2('稳定性清单'),
      ul(
        { class: 'docs-list' },
        li(
          code('island()'),
          ' / ',
          code('mountIslands()'),
          ' / ',
          code('createIslandsHandler'),
          ' / ',
          code('createIslandsNodeHandler'),
          ' —— 公开 API，视为稳定',
        ),
        li(
          code('sitelo'),
          ' 和 ',
          code('sitelo preview'),
          ' 都会从 ',
          code('src/islands/'),
          ' 提供 ',
          code('/_sitelo/islands'),
        ),
        li(
          code('createIslandsFromDirectory'),
          ' —— Node 宿主与 preview 共用同一份映射（原生 ',
          code('.js'),
          ' / ',
          code('.mjs'),
          ' / ',
          code('.cjs'),
          '）',
        ),
        li(
          a({ href: '/zh/examples/islands' }, 'examples/islands'),
          ' 中的宿主模板：Node 的 ',
          code('server.js'),
          '、Netlify 函数 + rewrite、Vercel serverless + rewrite',
        ),
        li(
          'props 保持精简且不含机密（GET 查询串）—— 这是有意为之；机密请在群岛内部、在服务端获取',
        ),
        li(
          'props 来自客户端 —— 请校验它们，或用 ',
          code('SITELO_ISLANDS_SECRET'),
          ' 对其签名',
        ),
      ),
      h2('加载策略'),
      p(
        '默认情况下每个群岛都会在页面加载时立刻取数据，所以有八个群岛的页面会在首屏绘制期间发出八个并发请求。用 ',
        code('when'),
        ' 把那些不会立刻看到的推迟掉。',
      ),
      codeBlock('islands-when', s.strategy, 'js'),
      ul(
        { class: 'docs-list' },
        li(code("'load'"), '（默认）—— 立即执行，与其他所有群岛一起'),
        li(
          code("'idle'"),
          ' —— 在 ',
          code('requestIdleCallback'),
          ' 时执行（不支持时回退到定时器）',
        ),
        li(code("'visible'"), ' —— 滚动进入视口时，通过 IntersectionObserver 触发'),
      ),
      p(
        code('rootMargin'),
        ' 只对 ',
        code("'visible'"),
        ' 生效，默认为 ',
        code("'200px'"),
        '。群岛也会超时结束，而不会一直转圈：',
      ),
      codeBlock('islands-mount-options', s.mountOptions, 'js'),
      p(
        code('mountIslands()'),
        ' 会在「立即加载」的群岛稳定后就完成 —— 被推迟的群岛稍后自行加载，并且刻意不被等待。失败或超时的群岛会保留它的回退 HTML。',
      ),
      h2('props 是不可信输入'),
      p(
        '群岛的 props 来自客户端。它们被嵌进页面、随请求回传，任何人都可以先改动它们：',
      ),
      codeBlock('islands-forged', s.forged, 'http'),
      p(
        '请把群岛收到的 props 完全当作查询参数看待 —— 校验它们，并且绝不要用它们去取访问者本就无权查看的数据。',
      ),
      p(
        '当 props 会决定取用特权数据时，请对其签名。设置一个 secret，sitelo 就会在构建时为每个占位元素签名，并以 ',
        code('403'),
        ' 拒绝其他一切：',
      ),
      codeBlock('islands-secret', s.secret, 'bash'),
      p(
        code('sitelo'),
        '、',
        code('sitelo preview'),
        ' 和 ',
        code('createIslandsHandler'),
        ' 读取的是同一个变量，因此开发、预览与生产保持一致。请给生产宿主配置同样的 secret。更想在代码里设置？',
      ),
      codeBlock('islands-configure', s.configure, 'js'),
      p(
        '签名是对群岛名称及其 props 计算的 HMAC-SHA256，因此为某个群岛签发的签名无法重放到另一个群岛上。签名只能证明 props 来自你的构建 —— 它并不隐藏内容，所以 props 仍然不能包含机密。没有 secret 时，props 会被原样接受，校验完全由你的群岛模块负责。',
      ),
      h2('值得知道的细节'),
      ul(
        { class: 'docs-list' },
        li('没有部署群岛端点？那就只会保留回退 HTML —— 页面会优雅降级。'),
        li(
          '请求是 ',
          code('GET'),
          '，props 放在查询串里，因此响应可被缓存 —— 如果希望 CDN 短暂保留这些片段，请设置处理器的 ',
          code('cacheControl'),
          ' 选项。',
        ),
        li(
          '加载期间占位元素带有 ',
          code('data-sitelo-island-state="loading"'),
          '（随后是 ',
          code('loaded'),
          ' 或 ',
          code('error'),
          '）—— 写 CSS 时很好用。',
        ),
      ),
    ],
  })
