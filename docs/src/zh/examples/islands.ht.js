import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, examplesLayout, pageCodeTabs } from '../../lib/zh.js'
import { islandsExampleSnippets } from '../../lib/snippets/examples-islands.js'

const s = islandsExampleSnippets('zh')

export default () =>
  examplesLayout({
    title: '服务端区块',
    description:
      '发布一个静态的 sitelo 站点，并挂上一个在请求时渲染区块的 Node 宿主。',
    activeHref: '/zh/examples/islands',
    children: [
      p(
        'sitelo 构建的是静态 HTML。服务端区块负责填补那些必须保持新鲜的部分 —— 时钟、评论、库存，任何依赖请求的东西。这个范例先构建一个带时间区块的页面，再运行一个小型 Node 服务器，同时提供 ',
        code('dist/'),
        ' 和 ',
        code('/_sitelo/islands'),
        '。',
      ),
      p(
        '这个项目的副本在 sitelo 仓库的 ',
        code('examples/islands/'),
        ' 下。',
      ),
      h2('你会得到什么'),
      ul(
        { class: 'docs-list' },
        li('一个带区块占位符和回退 HTML 的静态首页'),
        li(
          code('src/islands/'),
          ' 下一个仅服务端的区块模块，绝不会进入浏览器',
        ),
        li(
          '一个客户端加载器，把来自 ',
          code('/_sitelo/islands/<name>'),
          ' 的片段换进页面',
        ),
        li(
          '一份贴近生产形态的 ',
          code('server.js'),
          ' —— 静态文件 + ',
          code('createIslandsNodeHandler'),
        ),
      ),
      h2('项目结构'),
      codeBlock('project', s.structure, 'bash'),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('1. 区块模块'),
      p(
        '一个普通的 ',
        code('.js'),
        ' 文件（不是 ',
        code('.ht.js'),
        '）。它接收 ',
        code('{ name, props, request }'),
        '，并返回一个 HTML 字符串。这个模块用到了请求时间和 user-agent，好让你看出它确实是按请求渲染的。',
      ),
      codeBlock('src/islands/time.js', s.island, 'javascript'),
      h2('2. 把区块放进页面'),
      p(
        code('island()'),
        ' 会把 props 嵌进占位元素。构建产物发布的是回退内容；端点响应之后，加载器会替换它。',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. 客户端加载器'),
      codeBlock('src/js/islands.js', s.loader, 'javascript'),
      codeBlock('src/css/styles.css', s.styles, 'css'),
      h2('4. Node 宿主'),
      p(
        '执行 ',
        code('sitelo build'),
        ' 之后，这个进程会提供 ',
        code('dist/'),
        '，并用 ',
        code('sitelo/islands/server'),
        ' 的 ',
        code('createIslandsNodeHandler'),
        ' 渲染区块。区块模块留在 ',
        code('dist/'),
        ' 之外 —— 宿主从 ',
        code('src/'),
        ' 导入它们。',
      ),
      codeBlock('server.js', s.server, 'javascript'),
      h2('5. 构建并运行'),
      codeBlock('shell', s.buildRun, 'bash'),
      p(
        '打开 ',
        code('http://localhost:3000'),
        '。你应该会先短暂看到回退内容，然后是服务器时间。刷新一下 —— 时间戳会变。在 ',
        code('sitelo'),
        '（dev）和 ',
        code('sitelo preview'),
        ' 里你不需要 ',
        code('server.js'),
        '：CLI 已经在提供 ',
        code('/_sitelo/islands'),
        '。',
      ),
      h2('部署'),
      p('本示例在 Node 服务器旁边还附带了几份宿主模板：'),
      ul(
        { class: 'docs-list' },
        li(
          code('Node'),
          ' —— ',
          code('npm run build && npm start'),
          '（在平台上设置 ',
          code('PORT'),
          '）。使用 ',
          code('createIslandsFromDirectory'),
          '。',
        ),
        li(
          code('Netlify'),
          ' —— ',
          code('netlify.toml'),
          ' 把 ',
          code('/_sitelo/islands/*'),
          ' 重写到 ',
          code('netlify/functions/islands.mjs'),
          '。',
        ),
        li(
          code('Vercel'),
          ' —— ',
          code('vercel.json'),
          ' 重写到 ',
          code('api/islands/[...path].js'),
          '。',
        ),
        li(
          '纯静态托管（GitHub Pages、朴素的 S3）—— 在你加上函数之前，占位符会一直保留回退内容。',
        ),
      ),
      p(
        '要在别处使用 serverless 或边缘环境，请用 ',
        code('createIslandsHandler'),
        '（Web ',
        code('Request'),
        ' → ',
        code('Response'),
        '）—— 参见',
        a({ href: '/zh/docs/islands' }, '服务端区块文档'),
        '。如果该函数不同源，请把 ',
        code('mountIslands({ endpoint })'),
        ' 指向它的 URL。',
      ),
      h2('说明'),
      h3('只有静态托管时'),
      p(
        'GitHub Pages、朴素的 S3 等托管没有服务器进程。没有区块端点时，回退 HTML 就会一直留着 —— 页面照样能用，只是少了那个动态片段。',
      ),
      h3('让 props 保持精简'),
      p(
        'props 会随 HTML 属性和请求查询串一起传输。不要把机密或大块数据放进去 —— 那些应当在服务端、在区块模块内部去获取。',
      ),
      p(
        a({ href: '/zh/docs/islands' }, '服务端区块文档'),
        ' · ',
        a({ href: '/zh/examples/basic' }, '基础站点 / 部署'),
        ' · ',
        a({ href: '/zh/examples' }, '全部示例'),
      ),
    ],
  })
