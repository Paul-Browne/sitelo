import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/ru.js'
import { pagesSnippets } from '../../lib/snippets/pages.js'

const s = pagesSnippets('ru')

export default () =>
  docsLayout({
    title: 'Написание страниц',
    description:
      'Как писать страницы sitelo — шаблонные строки, ht.js (рекомендуется) или JSX.',
    activeHref: '/ru/docs/pages',
    children: [
      p(
        'Любой файл с расширением страницы является страницей. Расширения по умолчанию: ',
        code('.ht.js'),
        ', ',
        code('.html.js'),
        ', а также варианты TypeScript и JSX (',
        code('.ht.ts'),
        ', ',
        code('.ht.tsx'),
        ', …).',
      ),
      p(
        'Если вывод начинается с ',
        code('<html>'),
        ', sitelo автоматически добавляет ',
        code('<!DOCTYPE html>'),
        '. Для разметки рекомендуем ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' (',
        code('javascript-to-html'),
        '): функции-теги, возвращающие строки, без шаблонизатора и без рантайма React.',
      ),
      h2('1. Функция, возвращающая HTML'),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.fnTemplate,
        ht: s.fnHt,
        jsx: s.fnJsx,
      }),
      p(
        'JSX-файлы используют ',
        code('*.ht.jsx'),
        ' / ',
        code('*.ht.tsx'),
        ' и требуют установленных ',
        code('react'),
        ' и ',
        code('react-dom'),
        '. См. ',
        a({ href: '#ограничения-jsx' }, 'ограничения JSX'),
        ' ниже — sitelo превращает JSX в статический HTML во время сборки; это не React-приложение в браузере.',
      ),
      h2('2. Обычная строка'),
      codeBlock('page.ht.js', s.string, 'javascript'),
      h2('3. Структурированный модуль'),
      p(
        'Держите ',
        code('render'),
        ', ',
        code('data'),
        ' и ',
        code('generateStaticParams'),
        ' рядом:',
      ),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.structuredTemplate,
        ht: s.structuredHt,
        jsx: s.structuredJsx,
      }),
      h2('Ограничения JSX'),
      p(
        'sitelo умеет рендерить страницы ',
        code('.ht.jsx'),
        ' / ',
        code('.ht.tsx'),
        ' через ',
        code('react-dom/server'),
        ', так что вы можете писать привычным синтаксисом JSX. Это удобство записи — а не React-приложение.',
      ),
      p(
        'React создан для браузера: дерево компонентов, которое монтируется, гидратируется и реагирует на события. sitelo никогда не отправляет этот рантайм вместе с вашими страницами. Во время сборки он рендерит ваш JSX в строку HTML, записывает файл и на этом останавливается. Ни клиентского бандла React, ни гидратации, ни виртуального DOM, ждущего в браузере.',
      ),
      p('Из-за этого:'),
      ul(
        { class: 'docs-list' },
        li(
          'Обработчики событий (',
          code('onClick'),
          ', ',
          code('onChange'),
          ', …) на готовом сайте ничего не делают — их некому слушать. Сервер разработки предупредит, если найдёт их.',
        ),
        li(
          'Хуки (',
          code('useState'),
          ', ',
          code('useEffect'),
          ', …) не попадают на опубликованную страницу. Они могут выполниться один раз при рендеринге во время сборки, но состояние в браузере не хранят.',
        ),
        li(
          'Браузерные API (',
          code('window'),
          ', ',
          code('document'),
          ') недоступны, пока страница рендерится во время сборки.',
        ),
        li(
          code('dangerouslySetInnerHTML'),
          ' работает для вставки строк HTML; интерактивности всё равно нужен отдельный клиентский ',
          code('<script>'),
          '.',
        ),
      ),
      p(
        'Для кликов, форм и прочего клиентского поведения подключите обычный скрипт через ',
        a({ href: '/ru/docs/assets' }, 'конвейер ресурсов'),
        ', либо используйте ',
        a({ href: '/ru/docs/islands' }, 'серверные острова'),
        ' для HTML, создаваемого в момент запроса. Когда синтаксис JSX не нужен, лучше взять ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' или шаблонные строки — без установки React, и модель совпадает с «HTML во время сборки».',
      ),
      h2('Контекст рендеринга'),
      p('Каждая функция страницы получает один аргумент:'),
      h3('params'),
      p(
        'Параметры маршрута этой страницы (',
        code('Record<string, string | string[]>'),
        ').',
      ),
      h3('data'),
      p('То, что вернула ваша функция ', code('data()'), '.'),
      h3('page'),
      p(
        'Метаданные маршрута (',
        code('routePath'),
        ', ',
        code('relativePath'),
        ', …).',
      ),
      h3('dev'),
      p(
        code('true'),
        ' в сервере разработки, ',
        code('false'),
        ' во время сборки.',
      ),
    ],
  })
