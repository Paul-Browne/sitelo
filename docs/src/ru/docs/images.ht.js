import { a, h2, h3, li, p, strong, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/ru.js'
import { imagesSnippets } from '../../lib/snippets/images.js'

const s = imagesSnippets('ru')

export default () =>
  docsLayout({
    title: 'Оптимизация изображений',
    description:
      'Автоматическое масштабирование, современные форматы и srcset для каждого <img> — и в разработке, и в сборке.',
    activeHref: '/ru/docs/images',
    children: [
      p(
        'Положите полноразмерное изображение в ',
        code('src/'),
        ' или ',
        code('public/'),
        ', направьте на него ',
        code('<img>'),
        ' — и sitelo уменьшит его, переведёт в современный формат и перепишет тег с ',
        code('srcset'),
        '. Ничего импортировать не нужно, никакого компонента изучать не придётся.',
      ),
      h2('Включение'),
      codeBlock('sitelo.config.js', s.enable, 'javascript'),
      p(
        'Кодированием занимается ',
        a({ href: 'https://sharp.pixelplumbing.com', rel: 'noopener' }, 'sharp'),
        ' — необязательная peer-зависимость; установите её вместе с sitelo, когда включаете изображения:',
      ),
      codeBlock('shell', s.installSharp, 'bash'),
      p(
        'Сайтам без оптимизации изображений устанавливать её не нужно вовсе.',
      ),
      h2('Пишите обычный &lt;img&gt;'),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      p('Исходник 3000×2000 выходит вот таким:'),
      codeBlock('dist/index.html', s.output, 'html'),
      h2('Что вы получаете'),
      ul(
        { class: 'docs-list' },
        li(
          strong('Масштабирование, которое никогда не увеличивает'),
          ' — исходник в 600px с ',
          code('widths: [400, 800, 1200]'),
          ' даст 400 и 600, и на этом остановится.',
        ),
        li(
          strong('Современные форматы'),
          ' — один формат даёт обычный ',
          code('<img srcset>'),
          '; два и больше оборачиваются в ',
          code('<picture>'),
          ' с запасным вариантом в исходном формате.',
        ),
        li(
          strong('Без скачков вёрстки'),
          ' — ',
          code('width'),
          ' и ',
          code('height'),
          ' заполняются из настоящего изображения, плюс ',
          code('loading="lazy"'),
          ' и ',
          code('decoding="async"'),
          '.',
        ),
        li(
          strong('Та же разметка в разработке'),
          ' — ',
          code('sitelo'),
          ' (dev) переписывает страницы и отдаёт варианты по запросу, так что вы видите именно то, что опубликуете.',
        ),
        li(
          strong('Общий кэш'),
          ' — варианты хранятся по хэшу содержимого в ',
          code('node_modules/.sitelo/images'),
          ', поэтому ни разработка, ни повторные сборки не кодируют одно и то же изображение дважды.',
        ),
      ),
      p(
        'Переписывание идёт по собранному HTML, поэтому охватывает изображения и из ',
        code('src/'),
        ', и из ',
        code('public/'),
        '.',
      ),
      h2('Параметры'),
      codeBlock('sitelo.config.js', s.options, 'javascript'),
      ul(
        { class: 'docs-list' },
        li(
          code('widths'),
          ' — по умолчанию ',
          code('[400, 800, 1200]'),
          '; наибольшее значение служит и верхней границей',
        ),
        li(
          code('formats'),
          ' — по умолчанию ',
          code("['webp']"),
          '; ',
          code('avif'),
          ', ',
          code('webp'),
          ', ',
          code('jpeg'),
          ', ',
          code('png'),
        ),
        li(
          code('quality'),
          ' — по умолчанию ',
          code('{ avif: 55, webp: 78, jpeg: 82 }'),
          '; качество кодировщика по форматам (png использует сжатие, а не качество)',
        ),
        li(
          code('sizes'),
          ' — атрибут ',
          code('sizes'),
          '; ',
          code('sizes'),
          ', указанный в теге, всегда важнее',
        ),
        li(
          code('dimensions'),
          ' — по умолчанию ',
          code('true'),
          '; добавляет ',
          code('width'),
          ' / ',
          code('height'),
        ),
        li(
          code('lazy'),
          ' — по умолчанию ',
          code('true'),
          '; добавляет ',
          code('loading'),
          ' и ',
          code('decoding'),
        ),
        li(
          code('exclude'),
          ' — glob-шаблоны или регулярные выражения URL изображений, которые не трогать',
        ),
        li(code('assetsDir'), ' — по умолчанию ', code("'assets/img'")),
        li(
          code('cacheDir'),
          ' — по умолчанию ',
          code("'node_modules/.sitelo/images'"),
        ),
        li(
          code('remote'),
          ' — по умолчанию ',
          code('false'),
          '; оптимизирует изображения ',
          code('https://'),
          ' во время сборки',
        ),
        li(
          code('prune'),
          ' — по умолчанию ',
          code('false'),
          '; удаляет оригиналы, на которые больше ничего не ссылается',
        ),
        li(
          code('dev'),
          ' — по умолчанию ',
          code('true'),
          '; поставьте ',
          code('false'),
          ', чтобы в разработке отдавать нетронутые оригиналы',
        ),
        li(
          code('concurrency'),
          ' — параллельные кодирования, по умолчанию CPU − 1 (максимум 8); 1, если включён remote',
        ),
      ),
      h3('Два формата дают <picture>'),
      p(
        'AVIF меньше, но моложе WebP, поэтому указание обоих позволяет браузеру выбрать и сохраняет запасной вариант для старых браузеров:',
      ),
      codeBlock('dist/index.html', s.picture, 'html'),
      h2('Как исключить изображение'),
      p(
        'Теги, у которых уже есть ',
        code('srcset'),
        ', которые находятся внутри ',
        code('<picture>'),
        ' или указывают на SVG, анимированный GIF либо внешний URL, остаются нетронутыми. Для всего остального скажите об этом явно:',
      ),
      codeBlock('src/index.ht.js', s.optOut, 'html'),
      p(
        'Изображения для соцсетей и фавиконки живут в ',
        code('<meta>'),
        ' и ',
        code('<link>'),
        ', которые здесь никогда не переписываются — их URL остаётся неизменным.',
      ),
      h2('Внешние изображения'),
      p(
        'Контент, импортированный из CMS, часто указывает на чужой сервер. Включите ',
        code('remote'),
        ' — и такие изображения будут скачаны, оптимизированы и отданы с вашего домена:',
      ),
      codeBlock('sitelo.config.js', s.remote, 'javascript'),
      p(
        'Неудачная загрузка оставляет тег ровно таким, каким он был, с предупреждением — нестабильный источник никогда не обрушит вашу сборку. Затем ',
        code('prune'),
        ' убирает локальные оригиналы, на которые больше ничего не ссылается.',
      ),
    ],
  })
