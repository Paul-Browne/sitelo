import { a, h2, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/ru.js'
import { islandsSnippets } from '../../lib/snippets/islands.js'

const s = islandsSnippets('ru')

export default () =>
  docsLayout({
    title: 'Серверные острова',
    description:
      'Держите страницы статичными и рендерите отмеченные области на сервере в момент запроса.',
    activeHref: '/ru/docs/islands',
    children: [
      p(
        'Иногда одной области в остальном статичной страницы нужны свежие данные на каждый запрос — комментарии под закэшированной записью блога, наличие товара на карточке. Серверные острова оставляют страницу статичной и рендерят только эту область на сервере, когда страницу открывают.',
      ),
      h2('1. Напишите остров'),
      p(
        'Остров — это модуль-фрагмент внутри ',
        code('src/islands/'),
        ': обычный файл ',
        code('.js'),
        ' или ',
        code('.ts'),
        ' (не ',
        code('.ht.js'),
        ', потому что острова — фрагменты, а не страницы). Идея та же, что и везде в sitelo: функция, возвращающая HTML.',
      ),
      codeBlock('src/islands/comments.js', s.islandModule, 'javascript'),
      p(
        'Он получает ',
        code('{ name, props, request }'),
        ' и должен вернуть строку HTML. Модули островов работают только на сервере — код без ссылок внутри ',
        code('src/'),
        ' никогда не попадает в браузер.',
      ),
      h2('2. Разместите его на странице'),
      p(
        'Импортируйте ',
        code('island()'),
        ' из ',
        code('sitelo/islands'),
        '. Статическая сборка публикует запасной HTML; пропсы встраиваются в плейсхолдер, поэтому держите их небольшими и без секретов.',
      ),
      pageCodeTabs({
        file: 'src/blog/[slug].ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Добавьте клиентский загрузчик'),
      p(
        'Крошечный скрипт запрашивает каждый отрендеренный фрагмент и подставляет его. Он проходит через обычный конвейер ресурсов, так что достаточно простой точки входа ',
        code('src/islands.js'),
        ':',
      ),
      codeBlock('src/islands.js', s.loader, 'javascript'),
      p(
        'В ',
        code('sitelo'),
        ' (dev) и ',
        code('sitelo preview'),
        ' это уже работает — оба отдают острова по адресу ',
        code('/_sitelo/islands/<name>'),
        ' из ',
        code('src/islands/'),
        '. Preview загружает нативные модули ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' (как и хост на Node); острова на TypeScript поддерживаются в разработке через Vite.',
      ),
      h2('Продакшн'),
      p(
        'Ваш статический хостинг продолжает отдавать страницы. Подключите небольшой обработчик там, где у вас выполняется серверный код — Node, serverless или edge-функция — и он отрендерит те же модули островов. Полный разбор с готовым Node-хостом и заготовками для Netlify и Vercel — в ',
        a({ href: '/examples/islands' }, 'примере серверных островов'),
        ' (на английском).',
      ),
      codeBlock('islands-function.js', s.server, 'javascript'),
      p(
        'С обычным http из Node или с express используйте ',
        code('createIslandsNodeHandler(options)'),
        ' — те же параметры, сигнатура ',
        code('(req, res, next)'),
        '. Все модули ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' внутри ',
        code('src/islands/'),
        ' можно подключить автоматически через ',
        code('createIslandsFromDirectory'),
        '. Если загрузчик обращается к другому источнику или пути, передайте ',
        code("mountIslands({ endpoint: 'https://api.example.com/islands' })"),
        ' и согласуйте это с параметром ',
        code('endpoint'),
        ' обработчика.',
      ),
      h2('Что считается стабильным'),
      ul(
        { class: 'docs-list' },
        li(
          code('island()'),
          ' / ',
          code('mountIslands()'),
          ' / ',
          code('createIslandsHandler'),
          ' / ',
          code('createIslandsNodeHandler'),
          ' — публичный API, считается стабильным',
        ),
        li(
          code('sitelo'),
          ' и ',
          code('sitelo preview'),
          ' оба отдают ',
          code('/_sitelo/islands'),
          ' из ',
          code('src/islands/'),
        ),
        li(
          code('createIslandsFromDirectory'),
          ' — одна и та же карта для Node-хостов и preview (нативные ',
          code('.js'),
          ' / ',
          code('.mjs'),
          ' / ',
          code('.cjs'),
          ')',
        ),
        li(
          'Заготовки хостов в ',
          a({ href: '/examples/islands' }, 'examples/islands'),
          ': ',
          code('server.js'),
          ' для Node, функция Netlify + rewrite, serverless Vercel + rewrite',
        ),
        li(
          'Пропсы остаются небольшими и без секретов (строка запроса GET) — так задумано; получайте секреты внутри острова, на сервере',
        ),
        li(
          'Пропсы приходят от клиента — проверяйте их или подписывайте через ',
          code('SITELO_ISLANDS_SECRET'),
        ),
      ),
      h2('Стратегии загрузки'),
      p(
        'По умолчанию каждый остров загружается сразу вместе со страницей, так что страница с восемью островами делает восемь одновременных запросов во время первой отрисовки. Передайте ',
        code('when'),
        ', чтобы отложить те, что не видны сразу.',
      ),
      codeBlock('islands-when', s.strategy, 'js'),
      ul(
        { class: 'docs-list' },
        li(
          code("'load'"),
          ' (по умолчанию) — немедленно, вместе со всеми остальными островами',
        ),
        li(
          code("'idle'"),
          ' — по ',
          code('requestIdleCallback'),
          ' (с откатом на таймаут)',
        ),
        li(
          code("'visible'"),
          ' — когда остров попадает в область просмотра, через IntersectionObserver',
        ),
      ),
      p(
        code('rootMargin'),
        ' применяется только к ',
        code("'visible'"),
        ' и по умолчанию равен ',
        code("'200px'"),
        '. Кроме того, острова завершаются по таймауту, а не крутятся вечно:',
      ),
      codeBlock('islands-mount-options', s.mountOptions, 'js'),
      p(
        code('mountIslands()'),
        ' завершается, как только «немедленные» острова улеглись — отложенные загружаются позже сами по себе и намеренно не ожидаются. Остров, который упал или не уложился в таймаут, сохраняет свой запасной HTML.',
      ),
      h2('Пропсы — недоверенный ввод'),
      p(
        'Пропсы острова приходят от клиента. Они встроены в страницу, возвращаются с запросом, и кто угодно может изменить их по пути:',
      ),
      codeBlock('islands-forged', s.forged, 'http'),
      p(
        'Относитесь к пропсам, которые получает ваш остров, точно как к параметру запроса: проверяйте их и никогда не используйте, чтобы достать данные, которые посетителю и так не положены.',
      ),
      p(
        'Когда пропсы выбирают привилегированные данные, подписывайте их. Задайте секрет — и sitelo подпишет каждый плейсхолдер во время сборки, отклоняя всё остальное с кодом ',
        code('403'),
        ':',
      ),
      codeBlock('islands-secret', s.secret, 'bash'),
      p(
        'Ту же переменную читают ',
        code('sitelo'),
        ', ',
        code('sitelo preview'),
        ' и ',
        code('createIslandsHandler'),
        ', так что разработка, preview и продакшн согласованы. Передайте тот же секрет своему продакшн-хосту. Предпочитаете задать его в коде?',
      ),
      codeBlock('islands-configure', s.configure, 'js'),
      p(
        'Подписи — это HMAC-SHA256 от имени острова и его пропсов, поэтому подпись, выданная одному острову, не сработает для другого. Подпись доказывает, что пропсы пришли из вашей сборки — но не скрывает их, так что секретов в них по-прежнему быть не должно. Без секрета пропсы принимаются как есть, и их проверка целиком на вашем модуле острова.',
      ),
      h2('Полезно знать'),
      ul(
        { class: 'docs-list' },
        li(
          'Не развернули эндпоинт островов? Тогда просто останется запасной HTML — страницы деградируют мягко.',
        ),
        li(
          'Запросы идут методом ',
          code('GET'),
          ' с пропсами в строке запроса, поэтому ответы кэшируемы — задайте параметр ',
          code('cacheControl'),
          ' обработчика, если хотите, чтобы CDN ненадолго придерживал фрагменты.',
        ),
        li(
          'Во время загрузки плейсхолдер несёт ',
          code('data-sitelo-island-state="loading"'),
          ' (затем ',
          code('loaded'),
          ' или ',
          code('error'),
          ') — удобно для CSS.',
        ),
      ),
    ],
  })
