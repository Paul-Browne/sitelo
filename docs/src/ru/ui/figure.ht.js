import { h2, p } from 'javascript-to-html'
import { code, demo, propsTable, uiLayout } from '../../lib/ru.js'

export default () =>
  uiLayout({
    title: 'Иллюстрация',
    description:
      'Картинка и подпись к ней как одна иллюстрация — с местом, занятым заранее, до появления картинки.',
    activeHref: '/ru/ui/figure',
    children: [
      p(
        '<figure> связывает подпись с тем, что она описывает, чего абзац под картинкой не делает. Передайте ',
        code('src'),
        ' для обычного случая или потомков для всего остального, что достойно подписи.',
      ),

      h2('Простая иллюстрация'),
      demo(`figure({
  src: '/logo.svg',
  alt: 'Логотип sitelo',
  caption: 'Логотип в том виде, в каком он появляется в верхней панели.',
  style: '--su-figure-bg: var(--su-surface-2)',
})`, { align: 'stretch' }),

      h2('С удержанным соотношением'),
      p(
        code('ratio'),
        ' оборачивает картинку в ',
        code('aspectRatio()'),
        ', поэтому подпись никогда не прыгает вниз, когда картинка загрузится.',
      ),
      demo(`grid({ min: '13rem' },
  figure({ src: '/logo.svg', alt: '', ratio: '16 / 9', caption: 'ratio: 16 / 9' }),
  figure({ src: '/logo.svg', alt: '', ratio: '1 / 1', caption: 'ratio: 1 / 1' }),
)`, { align: 'stretch' }),

      h2('Подпись к чему-то другому'),
      p('Без ', code('src'), ' содержимым иллюстрации становятся потомки.'),
      demo(`figure({ caption: 'Таблица 1 — вывод сборки по умолчанию.' },
  table({
    dense: true,
    columns: [{ key: 'file', header: 'Файл' }, { key: 'size', header: 'Размер', align: 'end' }],
    rows: [
      { file: 'index.html', size: '4,1 кБ' },
      { file: '404.html', size: '860 Б' },
      { file: 'sitemap.xml', size: '155 Б' },
    ],
  }),
)`, { align: 'stretch' }),

      h2('Код с подписью'),
      p(
        'Обратите внимание на проп ',
        code('text'),
        ' у ',
        code('code()'),
        ': во всей этой библиотеке потомки рендерятся как HTML, поэтому пример с тегами нужно экранировать, иначе браузер соберёт его вместо того, чтобы показать.',
      ),
      demo(`figure({ caption: 'Целая страница sitelo.' },
  code({ text: 'export default () => "<h1>Привет</h1>"' }),
)`, { align: 'stretch' }),

      h2('Альтернативный текст'),
      p(
        'Атрибут ',
        code('alt'),
        ' пишется всегда — пустым, если вы ничего не дали: картинку вовсе без ',
        code('alt'),
        ' объявляют по имени файла, а это хуже тишины. Подпись его не заменяет: подпись читают все, а alt описывает картинку тому, кто её не видит.',
      ),
      p(
        'Когда подпись уже говорит всё, что говорит картинка, правильный ответ — ',
        code("alt: ''"),
        '.',
      ),

      h2('В тексте'),
      p(
        'Иллюстрации, приходящие из рендерера Markdown, уже оформляет ',
        code('prose()'),
        '. Этот компонент — для иллюстраций, которые вы собираете сами.',
      ),

      h2('Пропсы'),
      propsTable([
        ['src', 'string', '', 'Источник картинки. Опустите и используйте потомков.'],
        ['alt', 'string', "''", 'Альтернативный текст. Пишется всегда, даже пустым.'],
        ['caption', 'Child', '', 'Тот самый figcaption.'],
        ['ratio', 'string', '', 'Держит место до загрузки картинки.'],
      ]),
    ],
  })
