import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, examplesLayout, pageCodeTabs } from '../../lib/ru.js'
import { islandsExampleSnippets } from '../../lib/snippets/examples-islands.js'

const s = islandsExampleSnippets('ru')

export default () =>
  examplesLayout({
    title: 'Серверные острова',
    description:
      'Опубликуйте статический сайт на sitelo и поднимите хост на Node, который рендерит острова в момент запроса.',
    activeHref: '/ru/examples/islands',
    children: [
      p(
        'sitelo собирает статический HTML. Серверные острова заполняют то, что должно быть свежим, — часы, комментарии, наличие товара, всё, чему нужен запрос. Этот рецепт собирает страницу с островом времени, а затем запускает небольшой сервер на Node, который отдаёт ',
        code('dist/'),
        ' и ',
        code('/_sitelo/islands'),
        '.',
      ),
      p(
        'Копия этого проекта лежит в репозитории sitelo, в ',
        code('examples/islands/'),
        '.',
      ),
      h2('Что вы получаете'),
      ul(
        { class: 'docs-list' },
        li('Статическую главную страницу с плейсхолдером острова и запасным HTML'),
        li(
          'Серверный модуль острова в ',
          code('src/islands/'),
          ', который никогда не попадает в браузер',
        ),
        li(
          'Клиентский загрузчик, подставляющий фрагмент из ',
          code('/_sitelo/islands/<name>'),
        ),
        li(
          'Готовый к продакшну ',
          code('server.js'),
          ' — статические файлы + ',
          code('createIslandsNodeHandler'),
        ),
      ),
      h2('Структура проекта'),
      codeBlock('project', s.structure, 'bash'),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('1. Модуль острова'),
      p(
        'Обычный файл ',
        code('.js'),
        ' (не ',
        code('.ht.js'),
        '). Получает ',
        code('{ name, props, request }'),
        ' и возвращает строку HTML. Этот использует время запроса и user-agent, чтобы было видно: он рендерится на каждый запрос.',
      ),
      codeBlock('src/islands/time.js', s.island, 'javascript'),
      h2('2. Разместите остров на странице'),
      p(
        code('island()'),
        ' встраивает props в плейсхолдер. Сборка публикует запасной вариант; загрузчик заменяет его, когда эндпоинт ответит.',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Клиентский загрузчик'),
      codeBlock('src/js/islands.js', s.loader, 'javascript'),
      codeBlock('src/css/styles.css', s.styles, 'css'),
      h2('4. Хост на Node'),
      p(
        'После ',
        code('sitelo build'),
        ' этот процесс отдаёт ',
        code('dist/'),
        ' и рендерит острова через ',
        code('createIslandsNodeHandler'),
        ' из ',
        code('sitelo/islands/server'),
        '. Модули островов остаются вне ',
        code('dist/'),
        ' — хост импортирует их из ',
        code('src/'),
        '.',
      ),
      codeBlock('server.js', s.server, 'javascript'),
      h2('5. Сборка и запуск'),
      codeBlock('shell', s.buildRun, 'bash'),
      p(
        'Откройте ',
        code('http://localhost:3000'),
        '. Вы должны на мгновение увидеть запасной вариант, а затем время сервера. Обновите страницу — метка времени изменится. В ',
        code('sitelo'),
        ' (dev) и ',
        code('sitelo preview'),
        ' ',
        code('server.js'),
        ' не нужен: CLI уже отдаёт ',
        code('/_sitelo/islands'),
        '.',
      ),
      h2('Развёртывание'),
      p('Этот пример поставляется с заготовками хостов рядом с сервером на Node:'),
      ul(
        { class: 'docs-list' },
        li(
          code('Node'),
          ' — ',
          code('npm run build && npm start'),
          ' (задайте ',
          code('PORT'),
          ' на платформе). Использует ',
          code('createIslandsFromDirectory'),
          '.',
        ),
        li(
          code('Netlify'),
          ' — ',
          code('netlify.toml'),
          ' переписывает ',
          code('/_sitelo/islands/*'),
          ' в ',
          code('netlify/functions/islands.mjs'),
          '.',
        ),
        li(
          code('Vercel'),
          ' — ',
          code('vercel.json'),
          ' переписывает в ',
          code('api/islands/[...path].js'),
          '.',
        ),
        li(
          'Только статические хосты (GitHub Pages, простой S3) — плейсхолдеры сохраняют запасной вариант, пока вы не добавите функцию.',
        ),
      ),
      p(
        'Для serverless или edge в другом месте используйте ',
        code('createIslandsHandler'),
        ' (веб-',
        code('Request'),
        ' → ',
        code('Response'),
        ') — см. ',
        a({ href: '/ru/docs/islands' }, 'документацию по серверным островам'),
        '. Направьте ',
        code('mountIslands({ endpoint })'),
        ' на URL этой функции, если она не на том же источнике.',
      ),
      h2('Заметки'),
      h3('Только статические хосты'),
      p(
        'У GitHub Pages, простого S3 и подобных хостов нет серверного процесса. Без эндпоинта островов запасной HTML просто остаётся на месте — страницы всё равно работают, только без живого фрагмента.',
      ),
      h3('Держите props небольшими'),
      p(
        'Props путешествуют в HTML-атрибуте и в строке запроса. Не кладите туда секреты или крупные данные — получайте их внутри модуля острова, на сервере.',
      ),
      p(
        a({ href: '/ru/docs/islands' }, 'Документация по серверным островам'),
        ' · ',
        a({ href: '/ru/examples/basic' }, 'Базовый сайт / развёртывание'),
        ' · ',
        a({ href: '/ru/examples' }, 'Все примеры'),
      ),
    ],
  })
