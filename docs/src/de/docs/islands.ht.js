import { a, h2, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/de.js'
import { islandsSnippets } from '../../lib/snippets/islands.js'

const s = islandsSnippets('de')

export default () =>
  docsLayout({
    title: 'Server-Islands',
    description:
      'Seiten statisch halten und markierte Bereiche zur Anfragezeit auf dem Server rendern.',
    activeHref: '/de/docs/islands',
    children: [
      p(
        'Manchmal braucht ein Bereich einer ansonsten statischen Seite frische Daten pro Anfrage — Kommentare unter einem gecachten Blogbeitrag, ein Lagerbestand auf einer Produktseite. Server-Islands halten die Seite statisch und rendern genau diesen Bereich auf einem Server, wenn die Seite aufgerufen wird.',
      ),
      h2('1. Die Island schreiben'),
      p(
        'Eine Island ist ein Fragment-Modul unter ',
        code('src/islands/'),
        ' — eine schlichte ',
        code('.js'),
        '- oder ',
        code('.ts'),
        '-Datei (nicht ',
        code('.ht.js'),
        ', denn Islands sind Fragmente, keine Seiten). Dieselbe Idee wie überall in sitelo: eine Funktion, die HTML zurückgibt.',
      ),
      codeBlock('src/islands/comments.js', s.islandModule, 'javascript'),
      p(
        'Sie erhält ',
        code('{ name, props, request }'),
        ' und muss einen HTML-String zurückgeben. Island-Module laufen ausschließlich auf dem Server — nicht referenzierter Code unter ',
        code('src/'),
        ' gelangt nie in den Browser.',
      ),
      h2('2. In einer Seite platzieren'),
      p(
        'Importiere ',
        code('island()'),
        ' aus ',
        code('sitelo/islands'),
        '. Der statische Build liefert das Fallback-HTML aus; die Props stecken im Platzhalter, halte sie also klein und frei von Geheimnissen.',
      ),
      pageCodeTabs({
        file: 'src/blog/[slug].ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Den Client-Loader ergänzen'),
      p(
        'Ein winziges Skript holt jedes gerenderte Fragment und tauscht es ein. Es läuft durch die normale Asset-Pipeline, ein schlichter ',
        code('src/islands.js'),
        '-Einstiegspunkt genügt also:',
      ),
      codeBlock('src/islands.js', s.loader, 'javascript'),
      p(
        'In ',
        code('sitelo'),
        ' (dev) und ',
        code('sitelo preview'),
        ' funktioniert das bereits — beide liefern Islands unter ',
        code('/_sitelo/islands/<name>'),
        ' aus ',
        code('src/islands/'),
        ' aus. Preview lädt native ',
        code('.js'),
        '- / ',
        code('.mjs'),
        '-Module (wie ein Node-Host); TypeScript-Islands werden in der Entwicklung über Vite unterstützt.',
      ),
      h2('Produktion'),
      p(
        'Dein statischer Hoster liefert die Seiten weiterhin aus. Hänge einen kleinen Handler dort ein, wo du Server-Code ausführst — Node, Serverless oder eine Edge-Funktion — und er rendert dieselben Island-Module. Für einen vollständigen Durchgang mit lauffähigem Node-Host sowie Netlify- und Vercel-Vorlagen siehe das ',
        a({ href: '/de/examples/islands' }, 'Server-Islands-Beispiel'),
        '.',
      ),
      codeBlock('islands-function.js', s.server, 'javascript'),
      p(
        'Mit schlichtem Node-http oder express nimmst du stattdessen ',
        code('createIslandsNodeHandler(options)'),
        ' — dieselben Optionen, Signatur ',
        code('(req, res, next)'),
        '. Jedes ',
        code('.js'),
        '- / ',
        code('.mjs'),
        '-Modul unter ',
        code('src/islands/'),
        ' lässt sich mit ',
        code('createIslandsFromDirectory'),
        ' automatisch verdrahten. Holt der Loader von einem anderen Ursprung oder Pfad, übergib ',
        code("mountIslands({ endpoint: 'https://api.example.com/islands' })"),
        ' und stimme es mit der ',
        code('endpoint'),
        '-Option des Handlers ab.',
      ),
      h2('Stabilitäts-Checkliste'),
      ul(
        { class: 'docs-list' },
        li(
          code('island()'),
          ' / ',
          code('mountIslands()'),
          ' / ',
          code('createIslandsHandler'),
          ' / ',
          code('createIslandsNodeHandler'),
          ' — öffentliche API, gilt als stabil',
        ),
        li(
          code('sitelo'),
          ' und ',
          code('sitelo preview'),
          ' liefern beide ',
          code('/_sitelo/islands'),
          ' aus ',
          code('src/islands/'),
          ' aus',
        ),
        li(
          code('createIslandsFromDirectory'),
          ' — dieselbe Zuordnung für Node-Hosts und Preview (nativ ',
          code('.js'),
          ' / ',
          code('.mjs'),
          ' / ',
          code('.cjs'),
          ')',
        ),
        li(
          'Host-Vorlagen in ',
          a({ href: '/de/examples/islands' }, 'examples/islands'),
          ': Node-',
          code('server.js'),
          ', Netlify-Funktion + Rewrite, Vercel-Serverless + Rewrite',
        ),
        li(
          'Props bleiben klein und frei von Geheimnissen (GET-Query-String) — das ist Absicht; hole Geheimnisse innerhalb der Island auf dem Server',
        ),
        li(
          'Props kommen vom Client — validiere sie, oder signiere sie mit ',
          code('SITELO_ISLANDS_SECRET'),
        ),
      ),
      h2('Ladestrategien'),
      p(
        'Standardmäßig lädt jede Island, sobald die Seite lädt; eine Seite mit acht Islands stellt also acht gleichzeitige Anfragen während des ersten Aufbaus. Übergib ',
        code('when'),
        ', um die nicht sofort sichtbaren zu verzögern.',
      ),
      codeBlock('islands-when', s.strategy, 'js'),
      ul(
        { class: 'docs-list' },
        li(
          code("'load'"),
          ' (Standard) — sofort, zusammen mit allen anderen Islands',
        ),
        li(
          code("'idle'"),
          ' — per ',
          code('requestIdleCallback'),
          ' (mit Timeout als Rückfall)',
        ),
        li(
          code("'visible'"),
          ' — sobald sie ins Bild scrollt, per IntersectionObserver',
        ),
      ),
      p(
        code('rootMargin'),
        ' gilt nur für ',
        code("'visible'"),
        ' und steht standardmäßig auf ',
        code("'200px'"),
        '. Außerdem laufen Islands in einen Timeout, statt ewig zu drehen:',
      ),
      codeBlock('islands-mount-options', s.mountOptions, 'js'),
      p(
        code('mountIslands()'),
        ' löst auf, sobald die sofortigen Islands fertig sind — verzögerte laden später von selbst und werden bewusst nicht abgewartet. Eine fehlgeschlagene oder abgelaufene Island behält ihr Fallback-HTML.',
      ),
      h2('Props sind nicht vertrauenswürdig'),
      p(
        'Island-Props kommen vom Client. Sie stecken in der Seite, werden mit der Anfrage zurückgeschickt, und jede Person kann sie vorher bearbeiten:',
      ),
      codeBlock('islands-forged', s.forged, 'http'),
      p(
        'Behandle die Props, die deine Island empfängt, genau wie einen Query-Parameter — validiere sie, und nutze sie nie, um Daten nachzuschlagen, die die betrachtende Person nicht ohnehin sehen darf.',
      ),
      p(
        'Wählen Props privilegierte Daten aus, dann signiere sie. Setze ein Secret, und sitelo signiert jeden Platzhalter zur Build-Zeit und weist alles andere mit einem ',
        code('403'),
        ' ab:',
      ),
      codeBlock('islands-secret', s.secret, 'bash'),
      p(
        'Dieselbe Variable lesen ',
        code('sitelo'),
        ', ',
        code('sitelo preview'),
        ' und ',
        code('createIslandsHandler'),
        ', sodass Entwicklung, Preview und Produktion übereinstimmen. Gib deinem Produktions-Host dasselbe Secret. Lieber im Code setzen?',
      ),
      codeBlock('islands-configure', s.configure, 'js'),
      p(
        'Signaturen sind HMAC-SHA256 über den Island-Namen und ihre Props, eine für eine Island ausgestellte Signatur lässt sich also nicht gegen eine andere wiederverwenden. Das Signieren belegt, dass die Props aus deinem Build stammen — es verbirgt sie nicht, sie müssen also weiterhin frei von Geheimnissen sein. Ohne Secret werden Props unverändert akzeptiert, und ihre Validierung liegt ganz bei deinem Island-Modul.',
      ),
      h2('Gut zu wissen'),
      ul(
        { class: 'docs-list' },
        li(
          'Kein Island-Endpunkt ausgerollt? Dann bleibt schlicht das Fallback-HTML stehen — die Seiten verhalten sich gutmütig.',
        ),
        li(
          'Anfragen sind ',
          code('GET'),
          ' mit den Props im Query-String, die Antworten sind also cachebar — setze die ',
          code('cacheControl'),
          '-Option des Handlers, wenn ein CDN Fragmente kurz halten soll.',
        ),
        li(
          'Während des Ladens trägt der Platzhalter ',
          code('data-sitelo-island-state="loading"'),
          ' (danach ',
          code('loaded'),
          ' oder ',
          code('error'),
          ') — praktisch fürs CSS.',
        ),
      ),
    ],
  })
