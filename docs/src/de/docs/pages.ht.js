import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/de.js'
import { pagesSnippets } from '../../lib/snippets/pages.js'

const s = pagesSnippets('de')

export default () =>
  docsLayout({
    title: 'Seiten schreiben',
    description:
      'Wie man sitelo-Seiten schreibt — Template-Literale, ht.js (empfohlen) oder JSX.',
    activeHref: '/de/docs/pages',
    children: [
      p(
        'Jede Datei mit einer Seitenendung ist eine Seite. Standardendungen: ',
        code('.ht.js'),
        ', ',
        code('.html.js'),
        ' sowie die TypeScript- und JSX-Varianten (',
        code('.ht.ts'),
        ', ',
        code('.ht.tsx'),
        ', …).',
      ),
      p(
        'Beginnt die Ausgabe mit ',
        code('<html>'),
        ', stellt sitelo automatisch ',
        code('<!DOCTYPE html>'),
        ' voran. Fürs Markup empfehlen wir ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' (',
        code('javascript-to-html'),
        '): Tag-Funktionen, die Strings zurückgeben — ohne Template-Engine und ohne React-Runtime.',
      ),
      h2('1. Eine Funktion, die HTML zurückgibt'),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.fnTemplate,
        ht: s.fnHt,
        jsx: s.fnJsx,
      }),
      p(
        'JSX-Dateien nutzen ',
        code('*.ht.jsx'),
        ' / ',
        code('*.ht.tsx'),
        ' und benötigen installiertes ',
        code('react'),
        ' und ',
        code('react-dom'),
        '. Siehe die ',
        a({ href: '#jsx-einschraenkungen' }, 'JSX-Einschränkungen'),
        ' weiter unten — sitelo verwandelt JSX zur Build-Zeit in statisches HTML; es ist keine React-Anwendung im Browser.',
      ),
      h2('2. Ein einfacher String'),
      codeBlock('page.ht.js', s.string, 'javascript'),
      h2('3. Ein strukturiertes Modul'),
      p(
        'Halte ',
        code('render'),
        ', ',
        code('data'),
        ' und ',
        code('generateStaticParams'),
        ' zusammen:',
      ),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.structuredTemplate,
        ht: s.structuredHt,
        jsx: s.structuredJsx,
      }),
      h2('JSX-Einschränkungen'),
      p(
        'sitelo kann ',
        code('.ht.jsx'),
        '- / ',
        code('.ht.tsx'),
        '-Seiten mit ',
        code('react-dom/server'),
        ' rendern, damit du die vertraute JSX-Syntax schreiben kannst. Das ist eine Schreiberleichterung — keine React-Anwendung.',
      ),
      p(
        'React ist für den Browser gebaut: ein Komponentenbaum, der mountet, hydriert und auf Ereignisse reagiert. sitelo liefert diese Runtime nie mit deinen Seiten aus. Zur Build-Zeit rendert es dein JSX zu einem HTML-String, schreibt die Datei und hört dort auf. Kein React-Bundle im Client, keine Hydration, kein virtuelles DOM, das im Browser wartet.',
      ),
      p('Daraus folgt:'),
      ul(
        { class: 'docs-list' },
        li(
          'Event-Handler (',
          code('onClick'),
          ', ',
          code('onChange'),
          ', …) bewirken auf der fertigen Website nichts — es hört niemand zu. Der Entwicklungsserver warnt, wenn er welche findet.',
        ),
        li(
          'Hooks (',
          code('useState'),
          ', ',
          code('useEffect'),
          ', …) sind nicht Teil der ausgelieferten Seite. Sie laufen unter Umständen einmal beim Build-Rendering; einen Zustand im Browser halten sie nicht.',
        ),
        li(
          'Nur im Browser verfügbare APIs (',
          code('window'),
          ', ',
          code('document'),
          ') stehen beim Rendern zur Build-Zeit nicht zur Verfügung.',
        ),
        li(
          code('dangerouslySetInnerHTML'),
          ' funktioniert zum Einfügen von HTML-Strings; Interaktivität braucht weiterhin ein eigenes Client-',
          code('<script>'),
          '.',
        ),
      ),
      p(
        'Für Klicks, Formulare und sonstiges Client-Verhalten bindest du ein normales Skript über die ',
        a({ href: '/de/docs/assets' }, 'Asset-Pipeline'),
        ' ein oder nutzt ',
        a({ href: '/de/docs/islands' }, 'Server-Islands'),
        ' für HTML zur Anfragezeit. Wenn du die JSX-Syntax nicht brauchst, nimm lieber ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' oder Template-Literale — keine React-Installation, und das Modell passt zu „HTML zur Build-Zeit“.',
      ),
      h2('Render-Kontext'),
      p('Jede Seitenfunktion bekommt genau ein Argument:'),
      h3('params'),
      p(
        'Die Routenparameter dieser Seite (',
        code('Record<string, string | string[]>'),
        ').',
      ),
      h3('data'),
      p('Was deine ', code('data()'), '-Funktion zurückgegeben hat.'),
      h3('page'),
      p(
        'Metadaten zur Route (',
        code('routePath'),
        ', ',
        code('relativePath'),
        ', …).',
      ),
      h3('dev'),
      p(
        code('true'),
        ' im Entwicklungsserver, ',
        code('false'),
        ' zur Build-Zeit.',
      ),
    ],
  })
