import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, examplesLayout, pageCodeTabs } from '../../lib/es.js'
import { islandsExampleSnippets } from '../../lib/snippets/examples-islands.js'

const s = islandsExampleSnippets('es')

export default () =>
  examplesLayout({
    title: 'Islas de servidor',
    description:
      'Publica un sitio sitelo estático y monta un host Node que renderiza islas en el momento de la petición.',
    activeHref: '/es/examples/islands',
    children: [
      p(
        'sitelo genera HTML estático. Las islas de servidor rellenan las partes que deben estar frescas: un reloj, comentarios, el stock, cualquier cosa que dependa de la petición. Esta receta construye una página con una isla de hora y luego levanta un pequeño servidor Node que sirve ',
        code('dist/'),
        ' y ',
        code('/_sitelo/islands'),
        '.',
      ),
      p(
        'Hay una copia de este proyecto en el repositorio de sitelo, en ',
        code('examples/islands/'),
        '.',
      ),
      h2('Qué obtienes'),
      ul(
        { class: 'docs-list' },
        li('Una portada estática con un marcador de isla y HTML de reserva'),
        li(
          'Un módulo de isla exclusivo de servidor dentro de ',
          code('src/islands/'),
          ' que nunca llega al navegador',
        ),
        li(
          'Un cargador de cliente que sustituye el fragmento traído de ',
          code('/_sitelo/islands/<name>'),
        ),
        li(
          'Un ',
          code('server.js'),
          ' con forma de producción — archivos estáticos + ',
          code('createIslandsNodeHandler'),
        ),
      ),
      h2('Estructura del proyecto'),
      codeBlock('project', s.structure, 'bash'),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('1. Módulo de isla'),
      p(
        'Un archivo ',
        code('.js'),
        ' normal (no ',
        code('.ht.js'),
        '). Recibe ',
        code('{ name, props, request }'),
        ' y devuelve una cadena HTML. Este usa la hora de la petición y el user-agent para que veas que se renderiza en cada petición.',
      ),
      codeBlock('src/islands/time.js', s.island, 'javascript'),
      h2('2. Coloca la isla en una página'),
      p(
        code('island()'),
        ' incrusta las props en el marcador de posición. La compilación publica el contenido de reserva; el cargador lo sustituye cuando el endpoint responde.',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Cargador de cliente'),
      codeBlock('src/js/islands.js', s.loader, 'javascript'),
      codeBlock('src/css/styles.css', s.styles, 'css'),
      h2('4. Host Node'),
      p(
        'Después de ',
        code('sitelo build'),
        ', este proceso sirve ',
        code('dist/'),
        ' y renderiza las islas con ',
        code('createIslandsNodeHandler'),
        ' de ',
        code('sitelo/islands/server'),
        '. Los módulos de isla se quedan fuera de ',
        code('dist/'),
        ' — el host los importa desde ',
        code('src/'),
        '.',
      ),
      codeBlock('server.js', s.server, 'javascript'),
      h2('5. Compilar y ejecutar'),
      codeBlock('shell', s.buildRun, 'bash'),
      p(
        'Abre ',
        code('http://localhost:3000'),
        '. Deberías ver brevemente el contenido de reserva y luego la hora del servidor. Recarga — la marca de tiempo cambia. En ',
        code('sitelo'),
        ' (dev) y ',
        code('sitelo preview'),
        ' no necesitas ',
        code('server.js'),
        ': la CLI ya sirve ',
        code('/_sitelo/islands'),
        '.',
      ),
      h2('Despliegue'),
      p('Este ejemplo incluye plantillas de host junto al servidor Node:'),
      ul(
        { class: 'docs-list' },
        li(
          code('Node'),
          ' — ',
          code('npm run build && npm start'),
          ' (define ',
          code('PORT'),
          ' en la plataforma). Usa ',
          code('createIslandsFromDirectory'),
          '.',
        ),
        li(
          code('Netlify'),
          ' — ',
          code('netlify.toml'),
          ' reescribe ',
          code('/_sitelo/islands/*'),
          ' hacia ',
          code('netlify/functions/islands.mjs'),
          '.',
        ),
        li(
          code('Vercel'),
          ' — ',
          code('vercel.json'),
          ' reescribe hacia ',
          code('api/islands/[...path].js'),
          '.',
        ),
        li(
          'Hosts solo estáticos (GitHub Pages, S3 a secas) — los marcadores conservan su contenido de reserva hasta que añadas una función.',
        ),
      ),
      p(
        'Para serverless o edge en otro sitio, usa ',
        code('createIslandsHandler'),
        ' (',
        code('Request'),
        ' web → ',
        code('Response'),
        ') — consulta la ',
        a({ href: '/es/docs/islands' }, 'documentación de islas de servidor'),
        '. Apunta ',
        code('mountIslands({ endpoint })'),
        ' a la URL de esa función si no está en el mismo origen.',
      ),
      h2('Notas'),
      h3('Hosts solo estáticos'),
      p(
        'GitHub Pages, S3 a secas y hosts similares no tienen proceso de servidor. Sin un endpoint de islas, el HTML de reserva simplemente se queda — las páginas siguen funcionando, solo que sin el fragmento en vivo.',
      ),
      h3('Mantén las props pequeñas'),
      p(
        'Las props viajan en el atributo HTML y en la cadena de consulta de la petición. No pongas ahí secretos ni cargas grandes — obtenlos dentro del módulo de isla, en el servidor.',
      ),
      p(
        a({ href: '/es/docs/islands' }, 'Documentación de islas de servidor'),
        ' · ',
        a({ href: '/es/examples/basic' }, 'Sitio básico / despliegue'),
        ' · ',
        a({ href: '/es/examples' }, 'Todos los ejemplos'),
      ),
    ],
  })
