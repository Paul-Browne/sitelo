import {
  a,
  div,
  h2,
  h3,
  li,
  p,
  strong,
  table,
  tbody,
  td,
  th,
  thead,
  tr,
  ul,
} from 'javascript-to-html'
import { code, codeBlock, docsLayout } from '../../lib/es.js'
import { uiSnippets } from '../../lib/snippets/ui.js'

const s = uiSnippets('es')

/** The component reference table, one row per group. */
const GROUPS = [
  ['Maquetación', ['container', 'stack', 'grid', 'divider', 'card', 'cardHeader', 'cardTitle', 'cardSubtitle', 'cardMedia', 'cardBody', 'cardFooter', 'aspectRatio']],
  ['Tipografía', ['text', 'heading', 'link', 'code', 'inlineCode', 'kbd', 'visuallyHidden', 'prose']],
  ['Entradas', ['button', 'iconButton', 'buttonGroup', 'field', 'input', 'textarea', 'select', 'textField', 'textareaField', 'selectField', 'checkbox', 'radio', 'toggle', 'choiceGroup', 'slider', 'sliderField', 'toggleButton', 'toggleGroup']],
  ['Visualización de datos', ['avatar', 'avatarGroup', 'badge', 'chip', 'tooltip', 'table', 'list', 'listItem', 'figure']],
  ['Feedback', ['alert', 'progress', 'spinner', 'skeleton', 'toasts', 'empty']],
  ['Navegación', ['breadcrumbs', 'pagination', 'tabs', 'appBar', 'appBarNav', 'appBarSpacer', 'appBarActions', 'navLink', 'themeToggle']],
  ['Superposiciones', ['modal', 'drawer', 'closeButton', 'menu', 'menuItem', 'menuSeparator', 'accordion', 'accordionItem', 'collapsible']],
  ['Secciones', ['hero', 'footer', 'siteFooter', 'footerColumn', 'footerBottom', 'stat', 'statGroup', 'steps', 'timeline', 'timelineItem', 'mockup']],
  ['Estilos', ['styles', 'stylesheet', 'theme', 'themeScript']],
]

export default () =>
  docsLayout({
    title: 'Componentes',
    description:
      'sitelo-ui: botones, tarjetas, formularios, tablas, modales y el resto, como funciones que devuelven HTML.',
    activeHref: '/es/docs/ui',
    children: [
      p(
        'sitelo-ui es una biblioteca de componentes para sitelo. Cada componente es una función que devuelve una cadena de HTML, así que encaja directamente en un árbol de ',
        a({ href: 'https://www.npmjs.com/package/javascript-to-html' }, 'javascript-to-html'),
        ' sin nada en medio: sin compilador, sin runtime, sin hidratación. Lo que construyes es lo que acaba en ',
        code('dist/'),
        '.',
      ),
      p(
        'Viene con sitelo, en el punto de entrada ',
        code('sitelo/ui'),
        '.',
      ),

      h2('Primeros pasos'),
      codeBlock('Terminal', s.install, 'bash'),
      p(
        'Pon ',
        code('styles()'),
        ' en el head y llama a los componentes en el body. Esa es toda la configuración:',
      ),
      codeBlock('src/index.ht.js', s.page, 'javascript'),
      p(
        'Los nombres de los componentes coinciden a propósito con lo que renderizan, lo que hace que unos cuantos — ',
        code('button'),
        ', ',
        code('input'),
        ', ',
        code('table'),
        ', ',
        code('link'),
        ', ',
        code('code'),
        ', ',
        code('select'),
        ', ',
        code('progress'),
        ' — choquen con las funciones de elemento de javascript-to-html. Importa la biblioteca como espacio de nombres cuando necesites ambas:',
      ),
      codeBlock('src/index.ht.js', s.namespace, 'javascript'),

      h2('La convención de llamada'),
      p(
        'Cada componente recibe un objeto de props opcional seguido de sus hijos, exactamente como un elemento de javascript-to-html. Las props que el componente entiende se consumen por nombre; ',
        strong('todo lo demás pasa al elemento renderizado como atributo'),
        ', así que ',
        code('id'),
        ', ',
        code('data-*'),
        ', ',
        code('aria-*'),
        ' y los atributos de evento funcionan sin que la biblioteca tenga que enumerarlos:',
      ),
      codeBlock('Props que pasan de largo', s.passthrough, 'javascript'),
      p(
        'Una prop cuyo valor el componente no reconoce — ',
        code('variant: \'nonsense\''),
        ' — vuelve al valor por defecto en lugar de lanzar un error. Una errata cosmética no debería romper una compilación.',
      ),

      h2('Estilos'),
      p(
        code('styles()'),
        ' devuelve un elemento ',
        code('<style>'),
        ' con la hoja completa, minificada. Son unos 7 kB por la red y no puede faltar en ',
        code('dist/'),
        ', por eso es la opción por defecto. Si prefieres enlazarla una vez y que el navegador la cachee entre páginas, importa el CSS desde un archivo de entrada empaquetado y Vite lo emitirá:',
      ),
      codeBlock('src/main.js', s.linked, 'javascript'),
      p('Usa una u otra, no las dos.'),

      h3('Temas'),
      p(
        'Todo se controla con propiedades personalizadas de CSS en ',
        code(':root'),
        ': cinco paletas, una escala de espaciado, radios, tipografía y sombras. ',
        code('theme()'),
        ' escribe sus sobrescrituras y acepta nombres en camelCase (',
        code('radiusMd'),
        ' → ',
        code('--su-radius-md'),
        '), objetos de paleta o propiedades personalizadas literales:',
      ),
      codeBlock('src/index.ht.js', s.theming, 'javascript'),
      p(
        'El modo oscuro se resuelve solo a partir de ',
        code('prefers-color-scheme'),
        '. Poner ',
        code('data-theme'),
        ' o ',
        code('data-su-theme'),
        ' en ',
        code('light'),
        ' o ',
        code('dark'),
        ' en cualquier ancestro lo anula, que es justo lo que hace ',
        code('themeToggle()'),
        ':',
      ),
      codeBlock('src/index.ht.js', s.themeToggle, 'javascript'),

      h2('JavaScript, y lo poco que hay'),
      p(
        'La mayoría de los componentes no necesita ninguno. El modal y el panel lateral son elementos ',
        code('popover'),
        ', así que el navegador se encarga de abrirlos, del fondo, del clic fuera y de Escape. El acordeón es ',
        code('<details name>'),
        '. Los menús son ',
        code('<details>'),
        '. Los tooltips son CSS.',
      ),
      p('Cuatro cosas sí quieren un script:'),
      ul(
        li('las pestañas cuyos paneles cambian en el sitio'),
        li('el botón de cerrar de una alerta descartable'),
        li(
          code('toast()'),
          ' y la región ',
          code('toasts()'),
        ),
        li('el conmutador de tema'),
      ),
      p('Impórtalo desde cualquier archivo de entrada empaquetado:'),
      codeBlock('src/main.js', s.client, 'javascript'),
      p('Pesa unos 3 kB, escucha en el documento en vez de elemento por elemento, y todos los componentes que toca se renderizan bien sin él: las pestañas con panel muestran el que el servidor marcó como activo, los menús siguen abriéndose y el botón de descartar simplemente no hace nada.'),

      h2('Ejemplos'),
      p('Los formularios conectan solos sus etiquetas, ids, textos de ayuda y mensajes de error:'),
      codeBlock('Un formulario', s.form, 'javascript'),
      p('Un modal es un popover y su disparador es cualquier botón que apunte a su id:'),
      codeBlock('Un modal', s.modal, 'javascript'),
      p('Las pestañas vienen en dos formas: enlaces o paneles.'),
      codeBlock('Pestañas', s.tabs, 'javascript'),
      p(
        'Las tablas reciben ',
        code('columns'),
        ' y ',
        code('rows'),
        ', con una función ',
        code('render'),
        ' allí donde una celda necesita más que un valor:',
      ),
      codeBlock('Una tabla', s.table, 'javascript'),
      p(
        'El directorio ',
        code('examples/ui'),
        ' del repositorio renderiza todos los componentes en una sola página: es la forma más rápida de ver el conjunto completo.',
      ),

      h2('Referencia de componentes'),
      p(
        'Todas las exportaciones, por grupo. Las props están tipadas: ',
        code('sitelo/ui'),
        ' incluye archivos ',
        code('.d.ts'),
        ', así que el editor completa ',
        code('variant'),
        ', ',
        code('color'),
        ' y ',
        code('size'),
        ' tanto en JavaScript como en TypeScript.',
      ),
      div(
        { class: 'docs-table-scroll' },
        table(
          { class: 'docs-table docs-table--wrap-last' },
          thead(tr(th('Grupo'), th('Componentes'))),
          tbody(
            ...GROUPS.map(([group, names]) =>
              tr(td(group), td(names.map((name) => code(name)).join(', '))),
            ),
          ),
        ),
      ),
      p(
        'Dos nombres se apartan de lo esperable: el interruptor es ',
        code('toggle'),
        ', porque ',
        code('switch'),
        ' es una palabra reservada y no puede ser un enlace de importación; y el enlace con estilo se exporta como ',
        code('link'),
        ' y como ',
        code('textLink'),
        ', para poder convivir con el ',
        code('link'),
        ' de javascript-to-html. ',
        code('table'),
        ', ',
        code('input'),
        ', ',
        code('select'),
        ' y ',
        code('progress'),
        ' tienen la misma salida de emergencia: ',
        code('dataTable'),
        ', ',
        code('textInput'),
        ', ',
        code('selectField'),
        ', ',
        code('progressBar'),
        '.',
      ),
    ],
  })
