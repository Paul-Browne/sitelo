import { a, h2, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/es.js'
import { islandsSnippets } from '../../lib/snippets/islands.js'

const s = islandsSnippets('es')

export default () =>
  docsLayout({
    title: 'Islas de servidor',
    description:
      'Mantén las páginas estáticas y renderiza regiones concretas en el servidor en el momento de la petición.',
    activeHref: '/es/docs/islands',
    children: [
      p(
        'A veces una región de una página por lo demás estática necesita datos frescos para cada petición: los comentarios bajo una entrada de blog cacheada, una etiqueta de stock en una página de producto. Las islas de servidor mantienen la página estática y renderizan solo esa región en un servidor cuando alguien la visita.',
      ),
      h2('1. Escribe la isla'),
      p(
        'Una isla es un módulo de fragmento dentro de ',
        code('src/islands/'),
        ' — un archivo ',
        code('.js'),
        ' o ',
        code('.ts'),
        ' normal (no ',
        code('.ht.js'),
        ', porque las islas son fragmentos, no páginas). La misma idea que en todo sitelo: una función que devuelve HTML.',
      ),
      codeBlock('src/islands/comments.js', s.islandModule, 'javascript'),
      p(
        'Recibe ',
        code('{ name, props, request }'),
        ' y debe devolver una cadena HTML. Los módulos de isla son exclusivos del servidor: el código sin referenciar dentro de ',
        code('src/'),
        ' nunca llega al navegador.',
      ),
      h2('2. Colócala en una página'),
      p(
        'Importa ',
        code('island()'),
        ' desde ',
        code('sitelo/islands'),
        '. La compilación estática publica el HTML de reserva; las props se incrustan en el marcador de posición, así que mantenlas pequeñas y sin secretos.',
      ),
      pageCodeTabs({
        file: 'src/blog/[slug].ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Añade el cargador de cliente'),
      p(
        'Un script mínimo descarga cada fragmento renderizado y lo inserta. Pasa por el pipeline de recursos habitual, así que basta con una entrada ',
        code('src/islands.js'),
        ' normal:',
      ),
      codeBlock('src/islands.js', s.loader, 'javascript'),
      p(
        'En ',
        code('sitelo'),
        ' (dev) y en ',
        code('sitelo preview'),
        ' esto ya funciona: ambos sirven las islas en ',
        code('/_sitelo/islands/<name>'),
        ' desde ',
        code('src/islands/'),
        '. Preview carga módulos ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' nativos (igual que un host de Node); las islas en TypeScript funcionan en desarrollo gracias a Vite.',
      ),
      h2('Producción'),
      p(
        'Tu hosting estático sigue sirviendo las páginas. Monta un pequeño manejador allí donde ejecutes código de servidor —Node, serverless o una función edge— y renderizará los mismos módulos de isla. Para un recorrido completo con un host de Node ejecutable y plantillas para Netlify y Vercel, consulta el ',
        a({ href: '/es/examples/islands' }, 'ejemplo de islas de servidor'),
        '.',
      ),
      codeBlock('islands-function.js', s.server, 'javascript'),
      p(
        'Con http de Node a secas o con express, usa ',
        code('createIslandsNodeHandler(options)'),
        ' en su lugar — mismas opciones, firma ',
        code('(req, res, next)'),
        '. Puedes conectar automáticamente todos los módulos ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' de ',
        code('src/islands/'),
        ' con ',
        code('createIslandsFromDirectory'),
        '. Si el cargador descarga desde otro origen o ruta, pasa ',
        code("mountIslands({ endpoint: 'https://api.example.com/islands' })"),
        ' y haz que coincida con la opción ',
        code('endpoint'),
        ' del manejador.',
      ),
      h2('Lista de estabilidad'),
      ul(
        { class: 'docs-list' },
        li(
          code('island()'),
          ' / ',
          code('mountIslands()'),
          ' / ',
          code('createIslandsHandler'),
          ' / ',
          code('createIslandsNodeHandler'),
          ' — API pública, considerada estable',
        ),
        li(
          code('sitelo'),
          ' y ',
          code('sitelo preview'),
          ' sirven ambos ',
          code('/_sitelo/islands'),
          ' desde ',
          code('src/islands/'),
        ),
        li(
          code('createIslandsFromDirectory'),
          ' — el mismo mapa para hosts de Node y para preview (nativo ',
          code('.js'),
          ' / ',
          code('.mjs'),
          ' / ',
          code('.cjs'),
          ')',
        ),
        li(
          'Plantillas de host en ',
          a({ href: '/es/examples/islands' }, 'examples/islands'),
          ': ',
          code('server.js'),
          ' de Node, función de Netlify + rewrite, serverless de Vercel + rewrite',
        ),
        li(
          'Las props se mantienen pequeñas y sin secretos (cadena de consulta GET) — es intencionado; obtén los secretos dentro de la isla, en el servidor',
        ),
        li(
          'Las props las suministra el cliente: valídalas, o fírmalas con ',
          code('SITELO_ISLANDS_SECRET'),
        ),
      ),
      h2('Estrategias de carga'),
      p(
        'Por defecto, cada isla se descarga en cuanto carga la página, así que una página con ocho islas lanza ocho peticiones simultáneas durante el primer pintado. Pasa ',
        code('when'),
        ' para aplazar las que no se ven de inmediato.',
      ),
      codeBlock('islands-when', s.strategy, 'js'),
      ul(
        { class: 'docs-list' },
        li(
          code("'load'"),
          ' (por defecto) — de inmediato, junto con todas las demás islas',
        ),
        li(
          code("'idle'"),
          ' — en un ',
          code('requestIdleCallback'),
          ' (con un temporizador como alternativa)',
        ),
        li(
          code("'visible'"),
          ' — cuando entra en pantalla, mediante IntersectionObserver',
        ),
      ),
      p(
        code('rootMargin'),
        ' solo se aplica a ',
        code("'visible'"),
        ' y su valor por defecto es ',
        code("'200px'"),
        '. Además, las islas expiran en lugar de quedarse girando para siempre:',
      ),
      codeBlock('islands-mount-options', s.mountOptions, 'js'),
      p(
        code('mountIslands()'),
        ' se resuelve en cuanto las islas inmediatas se han asentado; las aplazadas cargan después por su cuenta y deliberadamente no se esperan. Una isla que falla o expira conserva su HTML de reserva.',
      ),
      h2('Las props son entrada no confiable'),
      p(
        'Las props de una isla las suministra el cliente. Se incrustan en la página, se devuelven en la petición, y cualquiera puede editarlas antes:',
      ),
      codeBlock('islands-forged', s.forged, 'http'),
      p(
        'Trata las props que recibe tu isla exactamente como un parámetro de consulta: valídalas, y nunca las uses para consultar datos que quien mira no tenga ya derecho a ver.',
      ),
      p(
        'Cuando las props seleccionan datos privilegiados, fírmalas. Define un secreto y sitelo firmará cada marcador de posición en tiempo de compilación, rechazando cualquier otro con un ',
        code('403'),
        ':',
      ),
      codeBlock('islands-secret', s.secret, 'bash'),
      p(
        'La misma variable la leen ',
        code('sitelo'),
        ', ',
        code('sitelo preview'),
        ' y ',
        code('createIslandsHandler'),
        ', así que desarrollo, preview y producción coinciden. Dale a tu host de producción el mismo secreto. ¿Prefieres definirlo en código?',
      ),
      codeBlock('islands-configure', s.configure, 'js'),
      p(
        'Las firmas son HMAC-SHA256 sobre el nombre de la isla y sus props, así que una firma emitida para una isla no puede reutilizarse en otra. Firmar demuestra que las props vienen de tu compilación — no las oculta, así que siguen sin poder ser secretas. Sin un secreto, las props se aceptan tal cual y validarlas es responsabilidad exclusiva de tu módulo de isla.',
      ),
      h2('Cosas que conviene saber'),
      ul(
        { class: 'docs-list' },
        li(
          '¿No has desplegado ningún endpoint de islas? Simplemente se queda el HTML de reserva: las páginas se degradan con elegancia.',
        ),
        li(
          'Las peticiones son ',
          code('GET'),
          ' con las props en la cadena de consulta, así que las respuestas son cacheables — define la opción ',
          code('cacheControl'),
          ' del manejador si quieres que una CDN retenga los fragmentos un rato.',
        ),
        li(
          'Mientras carga, el marcador de posición lleva ',
          code('data-sitelo-island-state="loading"'),
          ' (y luego ',
          code('loaded'),
          ' o ',
          code('error'),
          ') — práctico para el CSS.',
        ),
      ),
    ],
  })
