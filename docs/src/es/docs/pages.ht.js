import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/es.js'
import { pagesSnippets } from '../../lib/snippets/pages.js'

const s = pagesSnippets('es')

export default () =>
  docsLayout({
    title: 'Escribir páginas',
    description:
      'Cómo escribir páginas de sitelo — plantillas de cadena, ht.js (recomendado) o JSX.',
    activeHref: '/es/docs/pages',
    children: [
      p(
        'Cualquier archivo que termine en una extensión de página es una página. Extensiones por defecto: ',
        code('.ht.js'),
        ', ',
        code('.html.js'),
        ', y las variantes de TypeScript y JSX (',
        code('.ht.ts'),
        ', ',
        code('.ht.tsx'),
        ', …).',
      ),
      p(
        'Si la salida empieza por ',
        code('<html>'),
        ', sitelo añade ',
        code('<!DOCTYPE html>'),
        ' automáticamente. Para el marcado recomendamos ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' (',
        code('javascript-to-html'),
        '): funciones de etiqueta que devuelven cadenas, sin motor de plantillas ni runtime de React.',
      ),
      h2('1. Una función que devuelve HTML'),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.fnTemplate,
        ht: s.fnHt,
        jsx: s.fnJsx,
      }),
      p(
        'Los archivos JSX usan ',
        code('*.ht.jsx'),
        ' / ',
        code('*.ht.tsx'),
        ' y necesitan ',
        code('react'),
        ' y ',
        code('react-dom'),
        ' instalados. Consulta las ',
        a({ href: '#limitaciones-de-jsx' }, 'limitaciones de JSX'),
        ' más abajo: sitelo convierte JSX en HTML estático en tiempo de compilación; no es una aplicación de React en el navegador.',
      ),
      h2('2. Una simple cadena'),
      codeBlock('page.ht.js', s.string, 'javascript'),
      h2('3. Un módulo estructurado'),
      p(
        'Mantén ',
        code('render'),
        ', ',
        code('data'),
        ' y ',
        code('generateStaticParams'),
        ' juntos:',
      ),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.structuredTemplate,
        ht: s.structuredHt,
        jsx: s.structuredJsx,
      }),
      h2('Limitaciones de JSX'),
      p(
        'sitelo puede renderizar páginas ',
        code('.ht.jsx'),
        ' / ',
        code('.ht.tsx'),
        ' con ',
        code('react-dom/server'),
        ', así que puedes escribir con la sintaxis JSX de siempre. Eso es una comodidad para escribir, no una aplicación de React.',
      ),
      p(
        'React está pensado para el navegador: un árbol de componentes que se monta, se hidrata y responde a eventos. sitelo nunca envía ese runtime con tus páginas. En tiempo de compilación renderiza tu JSX a una cadena HTML, escribe el archivo y ahí termina. No hay bundle de React en el cliente, ni hidratación, ni DOM virtual esperando en el navegador.',
      ),
      p('Por eso:'),
      ul(
        { class: 'docs-list' },
        li(
          'Los manejadores de eventos (',
          code('onClick'),
          ', ',
          code('onChange'),
          ', …) no hacen nada en el sitio final — no hay nadie escuchando. El servidor de desarrollo avisa si los encuentra.',
        ),
        li(
          'Los hooks (',
          code('useState'),
          ', ',
          code('useEffect'),
          ', …) no forman parte de la página publicada. Puede que se ejecuten una vez durante el renderizado de la compilación; no mantienen estado en el navegador.',
        ),
        li(
          'Las APIs exclusivas del navegador (',
          code('window'),
          ', ',
          code('document'),
          ') no están disponibles mientras la página se renderiza en tiempo de compilación.',
        ),
        li(
          code('dangerouslySetInnerHTML'),
          ' sirve para inyectar cadenas de HTML; la interactividad sigue necesitando un ',
          code('<script>'),
          ' de cliente aparte.',
        ),
      ),
      p(
        'Para clics, formularios y demás comportamiento de cliente, enlaza un script normal a través del ',
        a({ href: '/es/docs/assets' }, 'pipeline de recursos'),
        ', o usa ',
        a({ href: '/es/docs/islands' }, 'islas de servidor'),
        ' para HTML generado en el momento de la petición. Cuando no necesites la sintaxis JSX, mejor ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' o plantillas de cadena: sin instalar React, y con un modelo que encaja con «HTML en tiempo de compilación».',
      ),
      h2('Contexto de renderizado'),
      p('Cada función de página recibe un único argumento:'),
      h3('params'),
      p(
        'Los parámetros de ruta de esta página (',
        code('Record<string, string | string[]>'),
        ').',
      ),
      h3('data'),
      p('Lo que haya devuelto tu función ', code('data()'), '.'),
      h3('page'),
      p(
        'Metadatos de la ruta (',
        code('routePath'),
        ', ',
        code('relativePath'),
        ', …).',
      ),
      h3('dev'),
      p(
        code('true'),
        ' en el servidor de desarrollo, ',
        code('false'),
        ' en tiempo de compilación.',
      ),
    ],
  })
