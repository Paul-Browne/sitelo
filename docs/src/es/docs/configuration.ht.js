import {
  a,
  div,
  h2,
  h3,
  li,
  p,
  table,
  tbody,
  td,
  th,
  thead,
  tr,
  ul,
} from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/es.js'
import { configurationSnippets } from '../../lib/snippets/configuration.js'

const s = configurationSnippets('es')

export default () =>
  docsLayout({
    title: 'Configuración',
    description:
      'Opciones del plugin en sitelo.config.js, ajustes de Vite, sitemap, RSS y Pagefind.',
    activeHref: '/es/docs/configuration',
    children: [
      p(
        'Pon las opciones del plugin y los ajustes opcionales de Vite en ',
        code('sitelo.config.js'),
        '. No hace falta ningún ',
        code('vite.config.js'),
        '.',
      ),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('Opciones del plugin'),
      ul(
        { class: 'docs-list' },
        li(code('pagesDir'), ' — por defecto ', code("'src'")),
        li(code('pageExtensions'), ' — qué sufijos cuentan como páginas'),
        li(
          code('cleanUrls'),
          ' — por defecto ',
          code('true'),
          ' (',
          code('/about/index.html'),
          ')',
        ),
        li(code('site'), ' — URL base; habilita el ', code('sitemap.xml')),
        li(code('rss'), ' — configuración del feed RSS'),
        li(
          code('pagefind'),
          ' — ',
          code('true'),
          ' u objeto de opciones; indexa el sitio con ',
          a({ href: 'https://pagefind.app', rel: 'noopener' }, 'Pagefind'),
          ' después de ',
          code('sitelo build'),
          ' (requiere ',
          code('npm install -D pagefind'),
          ')',
        ),
        li(
          code('images'),
          ' — ',
          code('true'),
          ' u objeto de opciones; ',
          a({ href: '/es/docs/images' }, 'optimiza las imágenes'),
          ' en desarrollo y después de ',
          code('sitelo build'),
          ' (requiere ',
          code('npm install -D sharp'),
          ')',
        ),
        li(
          code('missingAssets'),
          ' — ',
          code("'error'"),
          ' o ',
          code("'warn'"),
        ),
        li(
          code('linkCheck'),
          ' — enlaces internos rotos (ver ',
          a({ href: '#comprobacion-de-enlaces' }, 'Comprobación de enlaces'),
          ')',
        ),
        li(
          code('generatedTypesDir'),
          ' — por defecto ',
          code("'.sitelo/types'"),
        ),
        li(
          code('renderConcurrency'),
          ' / ',
          code('renderBatchSize'),
          ' — paralelismo de la compilación',
        ),
        li(
          code('buildReport'),
          ' — por defecto ',
          code('true'),
          '; resumen posterior a la compilación con páginas, tamaño de salida, archivos más grandes y tiempos por fase. ',
          code('false'),
          ' para desactivarlo, o ',
          code('{ top }'),
          ' para cambiar cuántos archivos grandes se listan',
        ),
        li(code('debug'), ' — registro detallado'),
        li(
          code('devToolbar'),
          ' — por defecto ',
          code('true'),
          '; ponlo a ',
          code('false'),
          ' para ocultar la barra de herramientas de desarrollo (archivo fuente, parámetros, número de islas, selector de viewport)',
        ),
        li(
          code('devToolbarDocsUrl'),
          ' — enlace de documentación en la barra (por defecto ',
          code('https://sitelo.js.org/docs'),
          ')',
        ),
      ),
      h2('Barra de herramientas de desarrollo'),
      p(
        'Mientras ',
        code('sitelo'),
        ' (dev) está en marcha, una pequeña barra al pie de cada página muestra el archivo de la página, sus parámetros y cuántas ',
        a({ href: '/es/docs/islands' }, 'islas de servidor'),
        ' hay en ella. Usa el botón de viewport para alternar entre ',
        code('Desktop'),
        ' / ',
        code('Tablet'),
        ' / ',
        code('Mobile'),
        ' (en un iframe, para que las media queries coincidan), y ',
        code('Copy'),
        ' para obtener un volcado de depuración al abrir incidencias. Nunca aparece en la salida de ',
        code('sitelo build'),
        '.',
      ),
      codeBlock('sitelo.config.js', s.devToolbarOff, 'javascript'),
      h2('Opciones de Vite'),
      p(
        'Todo lo que haya bajo ',
        code('vite'),
        ' se fusiona con la configuración de Vite. Las opciones de la CLI (por ejemplo ',
        code('--port'),
        ') tienen prioridad sobre ambas.',
      ),
      h2('Si ya tienes un vite.config.js'),
      p(
        'Sigue siendo compatible — o solo opciones de Vite, o control total del plugin:',
      ),
      codeBlock('vite.config.js', s.viteOnly, 'javascript'),
      codeBlock('vite.config.js', s.vitePlugin, 'javascript'),
      p(
        'Si el plugin ya está en tu configuración de Vite, pon ahí las opciones del plugin — y no también como opciones de plugin en ',
        code('sitelo.config.js'),
        ' (sitelo dará error).',
      ),
      h2('Sitemap y RSS'),
      p(
        'Define ',
        code('site'),
        ' para emitir ',
        code('dist/sitemap.xml'),
        '.',
      ),
      p('RSS:'),
      codeBlock('sitelo.config.js', s.rss, 'javascript'),
      p(
        'Produce ',
        code('dist/rss.xml'),
        ' con un elemento por cada página bajo ',
        code('routePrefix'),
        '.',
      ),
      h2('Comprobación de enlaces'),
      p(
        'Los ',
        code('<script src>'),
        ' y los href de hojas de estilo rotos ya hacen fallar la compilación (ver ',
        code('missingAssets'),
        '). ',
        code('linkCheck'),
        ' cubre la otra mitad: los enlaces internos ',
        code('<a href>'),
        ' que apuntan a una página que no existe.',
      ),
      codeBlock('sitelo.config.js', s.linkCheck, 'javascript'),
      p(
        'Después de ',
        code('sitelo build'),
        ', se resuelve cada enlace interno de la salida y se informa de todo lo que no tenga una página detrás, agrupado por la página en la que aparece:',
      ),
      codeBlock('output', s.linkCheckOutput, 'text'),
      h3('Cómo se resuelven los enlaces'),
      p(
        'La comprobación se ejecuta sobre el sitio emitido, no sobre la tabla de rutas, así que tiene en cuenta ',
        code('cleanUrls'),
        ', los grupos de rutas, ',
        code('mapOutputPath'),
        ', los archivos copiados desde ',
        code('public/'),
        ' y las páginas producidas por rutas dinámicas. También se ejecuta después de la optimización de imágenes y de Pagefind, así que ve exactamente lo que se publica. Un enlace es válido cuando un archivo real responde a él, probando en el mismo orden que un hosting estático:',
      ),
      ul(
        { class: 'docs-list' },
        li(
          code('/about'),
          ' → ',
          code('about'),
          ', luego ',
          code('about/index.html'),
          ', luego ',
          code('about.html'),
        ),
        li(
          code('/blog/'),
          ' → ',
          code('blog/index.html'),
          ' (una barra final siempre significa índice de directorio)',
        ),
        li(code('/'), ' → ', code('index.html')),
      ),
      p(
        'Los enlaces relativos (',
        code('../about'),
        ') se resuelven respecto a la página que los contiene, y se informa de los que se salen del directorio de salida. Las cadenas de consulta se ignoran al resolver: ',
        code('/about?utm=x'),
        ' comprueba ',
        code('/about'),
        '.',
      ),
      p(
        'Los enlaces externos nunca se descargan. ',
        code('https://'),
        ', los relativos al protocolo ',
        code('//cdn.example.com'),
        ', ',
        code('mailto:'),
        ', ',
        code('tel:'),
        ' y otros esquemas se omiten por completo.',
      ),
      h3('Opciones'),
      div(
        { class: 'docs-table-scroll' },
        table(
          { class: 'docs-table docs-table--wrap-last' },
          thead(tr(th('Opción'), th('Por defecto'), th('Descripción'))),
          tbody(
            tr(
              td(code('mode')),
              td(code("'warn'")),
              td(
                code("'warn'"),
                ' registra y continúa; ',
                code("'error'"),
                ' hace fallar la compilación — útil en CI',
              ),
            ),
            tr(
              td(code('exclude')),
              td(code('[]')),
              td('Globs o expresiones regulares de hrefs que hay que omitir'),
            ),
            tr(
              td(code('checkFragments')),
              td(code('false')),
              td(
                'Verifica además que los destinos ',
                code('#fragmento'),
                ' existan en la página enlazada',
              ),
            ),
          ),
        ),
      ),
      codeBlock('sitelo.config.js', s.linkCheckOptions, 'javascript'),
      p(
        code('checkFragments'),
        ' viene desactivado porque los ids que añade el JavaScript de cliente no están en el HTML compilado, y se reportarían como inexistentes. Cuentan como destinos de fragmento tanto ',
        code('id'),
        ' como el atributo heredado ',
        code('name'),
        ' de las anclas.',
      ),
      h3('Sitios servidos desde una base'),
      p(
        'Si tu sitio se despliega bajo una ',
        code('base'),
        ' (un sitio de proyecto de GitHub Pages, por ejemplo), se informa de cualquier enlace relativo a la raíz que no lleve esa base. Un ',
        code('/about'),
        ' en un sitio servido desde ',
        code('/repo/'),
        ' manda el navegador a la raíz del host, no a tu sitio; resolverlo igualmente contra la salida ocultaría justo el error que vale la pena detectar. Usa ',
        code('exclude'),
        ' cuando sea intencionado.',
      ),
      h2('Búsqueda con Pagefind'),
      p(
        'Búsqueda estática opcional con ',
        a({ href: 'https://pagefind.app', rel: 'noopener' }, 'Pagefind'),
        ', una dependencia peer opcional. Instálala cuando quieras búsqueda, luego activa la indexación, marca el contenido, monta la interfaz y ejecuta ',
        code('sitelo build'),
        '.',
      ),
      codeBlock('shell', s.installPagefind, 'bash'),
      h3('1. Activa la indexación'),
      p(
        'Pon ',
        code('pagefind: true'),
        '. Después de ',
        code('sitelo build'),
        ' obtienes ',
        code('dist/pagefind/'),
        ' y, por defecto, una copia en ',
        code('public/pagefind/'),
        ' para que el siguiente ',
        code('sitelo'),
        ' (dev) o ',
        code('sitelo preview'),
        ' pueda servir ',
        code('/pagefind/'),
        ' sin recompilar.',
      ),
      codeBlock('sitelo.config.js', s.pagefind, 'javascript'),
      h3('2. Marca el contenido y añade un punto de montaje'),
      p(
        'Pon ',
        code('data-pagefind-body'),
        ' en el contenido principal para que no se indexen la navegación ni el pie. Deja un elemento vacío para la interfaz de búsqueda:',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pagefindPageTemplate,
        ht: s.pagefindPageHt,
        jsx: s.pagefindPageJsx,
      }),
      h3('3. Monta la interfaz de Pagefind'),
      p(
        'Carga ',
        code('/pagefind/pagefind-ui.js'),
        ' y ',
        code('pagefind-ui.css'),
        ' desde tu script de cliente (solo después de que una compilación haya producido el índice):',
      ),
      codeBlock('src/main.js', s.pagefindMain, 'javascript'),
      h3('4. Compila e ignora el paquete sincronizado'),
      codeBlock('shell', s.pagefindBuild, 'bash'),
      codeBlock('.gitignore', s.pagefindGitignore, 'bash'),
      p(
        'Las opciones avanzadas (',
        code('syncPublic'),
        ', ',
        code('glob'),
        ', idioma, selectores, …) van en un objeto: ',
        code("pagefind: { syncPublic: false, glob: '**/*.html' }"),
        '. Todas las opciones de la interfaz: ',
        a({ href: 'https://pagefind.app', rel: 'noopener' }, 'pagefind.app'),
        '.',
      ),
      h3('404'),
      p(
        'Crea ',
        code('src/404.ht.js'),
        ' para obtener ',
        code('dist/404.html'),
        '. Si no, se genera uno por defecto.',
      ),
    ],
  })
