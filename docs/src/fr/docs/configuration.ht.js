import {
  a,
  div,
  h2,
  h3,
  li,
  p,
  table,
  tbody,
  td,
  th,
  thead,
  tr,
  ul,
} from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/fr.js'
import { configurationSnippets } from '../../lib/snippets/configuration.js'

const s = configurationSnippets('fr')

export default () =>
  docsLayout({
    title: 'Configuration',
    description:
      'Options du plugin dans sitelo.config.js, réglages Vite, sitemap, RSS et Pagefind.',
    activeHref: '/fr/docs/configuration',
    children: [
      p(
        'Placez les options du plugin et les réglages Vite optionnels dans ',
        code('sitelo.config.js'),
        '. Aucun ',
        code('vite.config.js'),
        ' n’est requis.',
      ),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('Options du plugin'),
      ul(
        { class: 'docs-list' },
        li(code('pagesDir'), ' — par défaut ', code("'src'")),
        li(code('pageExtensions'), ' — quels suffixes comptent comme des pages'),
        li(
          code('cleanUrls'),
          ' — par défaut ',
          code('true'),
          ' (',
          code('/about/index.html'),
          ')',
        ),
        li(code('site'), ' — URL de base ; active le ', code('sitemap.xml')),
        li(code('rss'), ' — configuration du flux RSS'),
        li(
          code('pagefind'),
          ' — ',
          code('true'),
          ' ou un objet d’options ; indexe le site avec ',
          a({ href: 'https://pagefind.app', rel: 'noopener' }, 'Pagefind'),
          ' après ',
          code('sitelo build'),
          ' (nécessite ',
          code('npm install -D pagefind'),
          ')',
        ),
        li(
          code('images'),
          ' — ',
          code('true'),
          ' ou un objet d’options ; ',
          a({ href: '/fr/docs/images' }, 'optimise les images'),
          ' en développement et après ',
          code('sitelo build'),
          ' (nécessite ',
          code('npm install -D sharp'),
          ')',
        ),
        li(code('missingAssets'), ' — ', code("'error'"), ' ou ', code("'warn'")),
        li(
          code('linkCheck'),
          ' — liens internes morts (voir ',
          a({ href: '#verification-des-liens' }, 'Vérification des liens'),
          ')',
        ),
        li(
          code('lighthouse'),
          ' — audits Lighthouse du build (voir ',
          a({ href: '#audits-lighthouse' }, 'Audits Lighthouse'),
          ')',
        ),
        li(code('generatedTypesDir'), ' — par défaut ', code("'.sitelo/types'")),
        li(
          code('renderConcurrency'),
          ' / ',
          code('renderBatchSize'),
          ' — parallélisme du build',
        ),
        li(
          code('buildReport'),
          ' — par défaut ',
          code('true'),
          ' ; récapitulatif après build des pages, de la taille de sortie, des plus gros fichiers et des temps par phase. ',
          code('false'),
          ' pour le désactiver, ou ',
          code('{ top }'),
          ' pour changer le nombre de gros fichiers listés',
        ),
        li(code('debug'), ' — journalisation détaillée'),
        li(
          code('devToolbar'),
          ' — par défaut ',
          code('true'),
          ' ; mettez ',
          code('false'),
          ' pour masquer la barre d’outils de développement (fichier source, paramètres, nombre d’îlots, sélecteur de viewport)',
        ),
        li(
          code('devToolbarDocsUrl'),
          ' — lien Docs dans la barre d’outils (par défaut ',
          code('https://sitelo.dev/docs'),
          ')',
        ),
      ),
      h2('Barre d’outils de développement'),
      p(
        'Pendant que ',
        code('sitelo'),
        ' (dev) tourne, une petite barre en bas de chaque page affiche le fichier de la page, ses paramètres et le nombre d’',
        a({ href: '/fr/docs/islands' }, 'îlots serveur'),
        ' présents. Le bouton viewport fait alterner les aperçus ',
        code('Desktop'),
        ' / ',
        code('Tablet'),
        ' / ',
        code('Mobile'),
        ' (dans une iframe, pour que les media queries correspondent), et ',
        code('Copy'),
        ' produit un bloc de débogage à joindre aux tickets. Elle n’apparaît jamais dans la sortie de ',
        code('sitelo build'),
        '.',
      ),
      codeBlock('sitelo.config.js', s.devToolbarOff, 'javascript'),
      h2('Options Vite'),
      p(
        'Tout ce qui se trouve sous ',
        code('vite'),
        ' est fusionné dans la configuration de Vite. Les options de la CLI (par exemple ',
        code('--port'),
        ') priment sur les deux.',
      ),
      h2('Si vous avez déjà un vite.config.js'),
      p(
        'Toujours pris en charge — soit des options Vite uniquement, soit un contrôle complet du plugin :',
      ),
      codeBlock('vite.config.js', s.viteOnly, 'javascript'),
      codeBlock('vite.config.js', s.vitePlugin, 'javascript'),
      p(
        'Si le plugin est déjà dans votre configuration Vite, mettez-y les options du plugin — et pas aussi comme options de plugin dans ',
        code('sitelo.config.js'),
        ' (sitelo renverra une erreur).',
      ),
      h2('Sitemap et RSS'),
      p(
        'Définissez ',
        code('site'),
        ' pour émettre ',
        code('dist/sitemap.xml'),
        '.',
      ),
      p('RSS :'),
      codeBlock('sitelo.config.js', s.rss, 'javascript'),
      p(
        'Produit ',
        code('dist/rss.xml'),
        ' avec une entrée pour chaque page sous ',
        code('routePrefix'),
        '.',
      ),
      h2('Vérification des liens'),
      p(
        'Les ',
        code('<script src>'),
        ' et href de feuilles de style cassés font déjà échouer le build (voir ',
        code('missingAssets'),
        '). ',
        code('linkCheck'),
        ' couvre l’autre moitié : les liens internes ',
        code('<a href>'),
        ' qui pointent vers une page inexistante.',
      ),
      codeBlock('sitelo.config.js', s.linkCheck, 'javascript'),
      p(
        'Après ',
        code('sitelo build'),
        ', chaque lien interne de la sortie est résolu et tout ce qui n’a pas de page derrière lui est signalé, regroupé par page où il apparaît :',
      ),
      codeBlock('output', s.linkCheckOutput, 'text'),
      h3('Comment les liens sont résolus'),
      p(
        'La vérification s’exécute sur le site émis, pas sur la table des routes : elle tient donc compte de ',
        code('cleanUrls'),
        ', des groupes de routes, de ',
        code('mapOutputPath'),
        ', des fichiers copiés depuis ',
        code('public/'),
        ' et des pages produites par des routes dynamiques. Elle s’exécute aussi après l’optimisation des images et Pagefind, si bien qu’elle voit exactement ce qui est livré. Un lien est valide quand un vrai fichier y répond, essayé dans l’ordre qu’emploierait un hébergeur statique :',
      ),
      ul(
        { class: 'docs-list' },
        li(
          code('/about'),
          ' → ',
          code('about'),
          ', puis ',
          code('about/index.html'),
          ', puis ',
          code('about.html'),
        ),
        li(
          code('/blog/'),
          ' → ',
          code('blog/index.html'),
          ' (une barre oblique finale ne désigne jamais qu’un index de répertoire)',
        ),
        li(code('/'), ' → ', code('index.html')),
      ),
      p(
        'Les liens relatifs (',
        code('../about'),
        ') se résolvent par rapport à la page qui les contient, et ceux qui sortent du répertoire de sortie sont signalés. Les chaînes de requête sont ignorées lors de la résolution — ',
        code('/about?utm=x'),
        ' vérifie ',
        code('/about'),
        '.',
      ),
      p(
        'Les liens externes ne sont jamais récupérés. ',
        code('https://'),
        ', les liens relatifs au protocole ',
        code('//cdn.example.com'),
        ', ',
        code('mailto:'),
        ', ',
        code('tel:'),
        ' et les autres schémas sont entièrement ignorés.',
      ),
      h3('Options'),
      div(
        { class: 'docs-table-scroll' },
        table(
          { class: 'docs-table docs-table--wrap-last' },
          thead(tr(th('Option'), th('Par défaut'), th('Description'))),
          tbody(
            tr(
              td(code('mode')),
              td(code("'warn'")),
              td(
                code("'warn'"),
                ' journalise et continue ; ',
                code("'error'"),
                ' fait échouer le build — utile en CI',
              ),
            ),
            tr(
              td(code('exclude')),
              td(code('[]')),
              td('Globs ou expressions régulières de href à ignorer'),
            ),
            tr(
              td(code('checkFragments')),
              td(code('false')),
              td(
                'Vérifie aussi que les cibles ',
                code('#fragment'),
                ' existent dans la page liée',
              ),
            ),
          ),
        ),
      ),
      codeBlock('sitelo.config.js', s.linkCheckOptions, 'javascript'),
      p(
        code('checkFragments'),
        ' est désactivé par défaut car les identifiants ajoutés par du JavaScript client ne figurent pas dans le HTML construit et seraient signalés comme manquants. Les attributs ',
        code('id'),
        ' comme les anciens ',
        code('name'),
        ' d’ancre comptent comme cibles de fragment.',
      ),
      h3('Sites servis depuis une base'),
      p(
        'Si votre site est déployé sous une ',
        code('base'),
        ' (un site de projet GitHub Pages, par exemple), tout lien relatif à la racine qui ne porte pas cette base est signalé. Un ',
        code('/about'),
        ' sur un site servi depuis ',
        code('/repo/'),
        ' envoie le navigateur à la racine de l’hôte, pas dans votre site — le résoudre malgré tout contre la sortie masquerait précisément l’erreur qu’il vaut la peine d’attraper. Utilisez ',
        code('exclude'),
        ' quand c’est délibéré.',
      ),
      h2('Audits Lighthouse'),
      p(
        code('sitelo lighthouse'),
        ' audite le build final avec ',
        a({ href: 'https://github.com/GoogleChrome/lighthouse', rel: 'noopener' }, 'Lighthouse'),
        '. C’est une dépendance pair optionnelle :',
      ),
      codeBlock('shell', s.installLighthouse, 'bash'),
      p(
        'sitelo sert ',
        code('dist/'),
        ' comme le fait ',
        code('sitelo preview'),
        ', lance un Chrome headless sur chaque page et affiche les scores :',
      ),
      codeBlock('output', s.lighthouseOutput, 'text'),
      p(
        'Chaque page est auditée à l’URL que le site utilise réellement — ',
        code('/docs'),
        ', jamais ',
        code('dist/docs.html'),
        ' —, donc ',
        code('cleanUrls'),
        ', les groupes de routes et un ',
        code('base'),
        ' sont pris en compte. Avec des seuils, le rapport devient un contrôle : tout score en dessous fait échouer la commande.',
      ),
      codeBlock('sitelo.config.js', s.lighthouse, 'javascript'),
      p(
        'Les scores s’écrivent comme Lighthouse les affiche (',
        code('0'),
        '–',
        code('100'),
        ') ; ses fractions de ',
        code('0'),
        ' à ',
        code('1'),
        ' fonctionnent aussi. Utilisez ',
        code("mode: 'warn'"),
        ' pour journaliser sans échouer.',
      ),
      ul(
        { class: 'docs-list' },
        li(code('include'), ' / ', code('exclude'), ' — quelles pages sont auditées (globs ou expressions régulières)'),
        li(code('categories'), ' — ', code('performance'), ', ', code('accessibility'), ', ', code('best-practices'), ', ', code('seo')),
        li(code('thresholds'), ' — score minimal par catégorie'),
        li(code('mode'), ' — ', code("'error'"), ' (par défaut) ou ', code("'warn'")),
        li(code('formFactor'), ' — ', code("'mobile'"), ' (par défaut) ou ', code("'desktop'")),
        li(code('runs'), ' — répéter l’audit et retenir le score médian'),
        li(code('output'), ' / ', code('formats'), ' — enregistre le rapport Lighthouse complet de chaque page'),
        li(code('flags'), ' / ', code('config'), ' — transmis tels quels à Lighthouse, donc tout ce que sa CLI accepte'),
        li(code('onBuild'), ' — auditer aussi à la fin de ', code('sitelo build'), ''),
      ),
      codeBlock('sitelo.config.js', s.lighthouseOptions, 'javascript'),
      p(
        'Lighthouse pilote un vrai Chrome : installez-en un là où l’audit tourne, ou faites pointer ',
        code('CHROME_PATH'),
        ' vers un binaire. Les scores de performance varient d’un passage à l’autre : utilisez ',
        code('runs: 3'),
        ' avant d’y attacher un seuil.',
      ),
      h2('Recherche Pagefind'),
      p(
        'Recherche statique optionnelle propulsée par ',
        a({ href: 'https://pagefind.app', rel: 'noopener' }, 'Pagefind'),
        ', une dépendance peer optionnelle. Installez-la quand vous voulez la recherche, puis activez l’indexation, marquez le contenu, montez l’interface et lancez ',
        code('sitelo build'),
        '.',
      ),
      codeBlock('shell', s.installPagefind, 'bash'),
      h3('1. Activer l’indexation'),
      p(
        'Mettez ',
        code('pagefind: true'),
        '. Après ',
        code('sitelo build'),
        ' vous obtenez ',
        code('dist/pagefind/'),
        ', et par défaut une copie dans ',
        code('public/pagefind/'),
        ' pour que le prochain ',
        code('sitelo'),
        ' (dev) ou ',
        code('sitelo preview'),
        ' puisse servir ',
        code('/pagefind/'),
        ' sans reconstruire.',
      ),
      codeBlock('sitelo.config.js', s.pagefind, 'javascript'),
      h3('2. Marquer le contenu et ajouter un point de montage'),
      p(
        'Placez ',
        code('data-pagefind-body'),
        ' sur le contenu principal pour que la navigation et le pied de page ne soient pas indexés. Laissez un élément vide pour l’interface de recherche :',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pagefindPageTemplate,
        ht: s.pagefindPageHt,
        jsx: s.pagefindPageJsx,
      }),
      h3('3. Monter l’interface Pagefind'),
      p(
        'Chargez ',
        code('/pagefind/pagefind-ui.js'),
        ' et ',
        code('pagefind-ui.css'),
        ' depuis votre script client (seulement après qu’un build a produit l’index) :',
      ),
      codeBlock('src/main.js', s.pagefindMain, 'javascript'),
      h3('4. Construire et ignorer le bundle synchronisé'),
      codeBlock('shell', s.pagefindBuild, 'bash'),
      codeBlock('.gitignore', s.pagefindGitignore, 'bash'),
      p(
        'Les options avancées (',
        code('syncPublic'),
        ', ',
        code('glob'),
        ', langue, sélecteurs, …) se placent dans un objet : ',
        code("pagefind: { syncPublic: false, glob: '**/*.html' }"),
        '. Toutes les options de l’interface : ',
        a({ href: 'https://pagefind.app', rel: 'noopener' }, 'pagefind.app'),
        '.',
      ),
      h3('404'),
      p(
        'Créez ',
        code('src/404.ht.js'),
        ' pour obtenir ',
        code('dist/404.html'),
        '. Sinon, une page par défaut est générée.',
      ),
    ],
  })
