import { a, h2, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/fr.js'
import { islandsSnippets } from '../../lib/snippets/islands.js'

const s = islandsSnippets('fr')

export default () =>
  docsLayout({
    title: 'Îlots serveur',
    description:
      'Gardez les pages statiques et faites rendre certaines zones sur le serveur au moment de la requête.',
    activeHref: '/fr/docs/islands',
    children: [
      p(
        'Parfois, une zone d’une page par ailleurs statique a besoin de données fraîches à chaque requête — les commentaires sous un article de blog mis en cache, un badge de stock sur une fiche produit. Les îlots serveur gardent la page statique et ne font rendre que cette zone sur un serveur, au moment de la consultation.',
      ),
      h2('1. Écrivez l’îlot'),
      p(
        'Un îlot est un module de fragment placé dans ',
        code('src/islands/'),
        ' — un simple fichier ',
        code('.js'),
        ' ou ',
        code('.ts'),
        ' (pas ',
        code('.ht.js'),
        ', car les îlots sont des fragments, pas des pages). Même idée que partout ailleurs dans sitelo : une fonction qui renvoie du HTML.',
      ),
      codeBlock('src/islands/comments.js', s.islandModule, 'javascript'),
      p(
        'Il reçoit ',
        code('{ name, props, request }'),
        ' et doit renvoyer une chaîne HTML. Les modules d’îlot sont exclusivement côté serveur — le code non référencé sous ',
        code('src/'),
        ' n’atteint jamais le navigateur.',
      ),
      h2('2. Placez-le dans une page'),
      p(
        'Importez ',
        code('island()'),
        ' depuis ',
        code('sitelo/islands'),
        '. Le build statique livre le HTML de repli ; les props sont intégrées dans l’emplacement réservé, alors gardez-les petites et non secrètes.',
      ),
      pageCodeTabs({
        file: 'src/blog/[slug].ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Ajoutez le chargeur client'),
      p(
        'Un petit script récupère chaque fragment rendu et l’insère. Il passe par le pipeline de ressources habituel : une simple entrée ',
        code('src/islands.js'),
        ' suffit.',
      ),
      codeBlock('src/islands.js', s.loader, 'javascript'),
      p(
        'Dans ',
        code('sitelo'),
        ' (dev) et ',
        code('sitelo preview'),
        ', cela fonctionne déjà — les deux servent les îlots sur ',
        code('/_sitelo/islands/<name>'),
        ' depuis ',
        code('src/islands/'),
        '. Preview charge des modules ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' natifs (comme un hôte Node) ; les îlots TypeScript sont pris en charge en développement grâce à Vite.',
      ),
      h2('Production'),
      p(
        'Votre hébergeur statique continue de servir les pages. Montez un petit gestionnaire là où vous exécutez du code serveur — Node, serverless ou une fonction edge — et il rendra les mêmes modules d’îlot. Pour un parcours complet avec un hôte Node exécutable et des exemples Netlify et Vercel, voir l’',
        a({ href: '/fr/examples/islands' }, 'exemple d’îlots serveur'),
        '.',
      ),
      codeBlock('islands-function.js', s.server, 'javascript'),
      p(
        'Avec le http de Node ou express, utilisez plutôt ',
        code('createIslandsNodeHandler(options)'),
        ' — mêmes options, signature ',
        code('(req, res, next)'),
        '. Vous pouvez câbler automatiquement chaque module ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' de ',
        code('src/islands/'),
        ' avec ',
        code('createIslandsFromDirectory'),
        '. Si le chargeur interroge une autre origine ou un autre chemin, passez ',
        code("mountIslands({ endpoint: 'https://api.example.com/islands' })"),
        ' et faites-le correspondre à l’option ',
        code('endpoint'),
        ' du gestionnaire.',
      ),
      h2('Points de stabilité'),
      ul(
        { class: 'docs-list' },
        li(
          code('island()'),
          ' / ',
          code('mountIslands()'),
          ' / ',
          code('createIslandsHandler'),
          ' / ',
          code('createIslandsNodeHandler'),
          ' — API publique, considérée comme stable',
        ),
        li(
          code('sitelo'),
          ' et ',
          code('sitelo preview'),
          ' servent tous deux ',
          code('/_sitelo/islands'),
          ' depuis ',
          code('src/islands/'),
        ),
        li(
          code('createIslandsFromDirectory'),
          ' — la même table pour les hôtes Node et pour preview (natif ',
          code('.js'),
          ' / ',
          code('.mjs'),
          ' / ',
          code('.cjs'),
          ')',
        ),
        li(
          'Exemples d’hôtes dans ',
          a({ href: '/fr/examples/islands' }, 'examples/islands'),
          ' : ',
          code('server.js'),
          ' pour Node, fonction Netlify + rewrite, serverless Vercel + rewrite',
        ),
        li(
          'Les props restent petites et non secrètes (chaîne de requête GET) — c’est intentionnel ; récupérez les secrets à l’intérieur de l’îlot, sur le serveur',
        ),
        li(
          'Les props viennent du client — validez-les, ou signez-les avec ',
          code('SITELO_ISLANDS_SECRET'),
        ),
      ),
      h2('Stratégies de chargement'),
      p(
        'Par défaut, chaque îlot se charge dès que la page charge : une page avec huit îlots déclenche donc huit requêtes simultanées pendant le premier rendu. Passez ',
        code('when'),
        ' pour différer ceux qui ne sont pas immédiatement visibles.',
      ),
      codeBlock('islands-when', s.strategy, 'js'),
      ul(
        { class: 'docs-list' },
        li(
          code("'load'"),
          ' (par défaut) — immédiatement, en même temps que tous les autres îlots',
        ),
        li(
          code("'idle'"),
          ' — sur ',
          code('requestIdleCallback'),
          ' (avec repli sur un délai)',
        ),
        li(
          code("'visible'"),
          ' — quand l’élément entre dans le viewport, via IntersectionObserver',
        ),
      ),
      p(
        code('rootMargin'),
        ' ne s’applique qu’à ',
        code("'visible'"),
        ' et vaut ',
        code("'200px'"),
        ' par défaut. Les îlots expirent aussi plutôt que de tourner indéfiniment :',
      ),
      codeBlock('islands-mount-options', s.mountOptions, 'js'),
      p(
        code('mountIslands()'),
        ' se résout une fois les îlots immédiats stabilisés — les îlots différés se chargent ensuite d’eux-mêmes et ne sont volontairement pas attendus. Un îlot en échec ou expiré conserve son HTML de repli.',
      ),
      h2('Les props sont des entrées non fiables'),
      p(
        'Les props d’un îlot viennent du client. Elles sont intégrées dans la page, renvoyées dans la requête, et n’importe qui peut les modifier au passage :',
      ),
      codeBlock('islands-forged', s.forged, 'http'),
      p(
        'Traitez les props que reçoit votre îlot exactement comme un paramètre de requête — validez-les, et ne vous en servez jamais pour aller chercher des données auxquelles le visiteur n’a pas déjà droit.',
      ),
      p(
        'Quand les props sélectionnent des données privilégiées, signez-les. Définissez un secret et sitelo signe chaque emplacement réservé au build, rejetant tout le reste avec un ',
        code('403'),
        ' :',
      ),
      codeBlock('islands-secret', s.secret, 'bash'),
      p(
        'La même variable est lue par ',
        code('sitelo'),
        ', ',
        code('sitelo preview'),
        ' et ',
        code('createIslandsHandler'),
        ', si bien que développement, preview et production s’accordent. Donnez le même secret à votre hôte de production. Vous préférez le définir dans le code ?',
      ),
      codeBlock('islands-configure', s.configure, 'js'),
      p(
        'Les signatures sont des HMAC-SHA256 sur le nom de l’îlot et ses props : une signature émise pour un îlot ne peut donc pas être rejouée sur un autre. Signer prouve que les props viennent de votre build — cela ne les cache pas, elles doivent donc rester non secrètes. Sans secret, les props sont acceptées telles quelles et leur validation incombe entièrement à votre module d’îlot.',
      ),
      h2('Bon à savoir'),
      ul(
        { class: 'docs-list' },
        li(
          'Aucun point de terminaison d’îlots déployé ? Le HTML de repli reste simplement en place — les pages se dégradent élégamment.',
        ),
        li(
          'Les requêtes sont des ',
          code('GET'),
          ' avec les props dans la chaîne de requête, donc les réponses sont cacheables — réglez l’option ',
          code('cacheControl'),
          ' du gestionnaire si vous voulez qu’un CDN retienne brièvement les fragments.',
        ),
        li(
          'Pendant le chargement, l’emplacement réservé porte ',
          code('data-sitelo-island-state="loading"'),
          ' (puis ',
          code('loaded'),
          ' ou ',
          code('error'),
          ') — pratique pour le CSS.',
        ),
      ),
    ],
  })
