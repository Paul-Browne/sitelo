import {
  a,
  div,
  h2,
  h3,
  li,
  p,
  strong,
  table,
  tbody,
  td,
  th,
  thead,
  tr,
  ul,
} from 'javascript-to-html'
import { code, codeBlock, docsLayout } from '../../lib/fr.js'
import { uiSnippets } from '../../lib/snippets/ui.js'

const s = uiSnippets('fr')

/** The component reference table, one row per group. */
const GROUPS = [
  ['Mise en page', ['container', 'stack', 'grid', 'divider', 'card', 'cardHeader', 'cardTitle', 'cardSubtitle', 'cardMedia', 'cardBody', 'cardFooter', 'aspectRatio']],
  ['Typographie', ['text', 'heading', 'link', 'code', 'inlineCode', 'kbd', 'visuallyHidden', 'prose']],
  ['Champs', ['button', 'iconButton', 'buttonGroup', 'field', 'input', 'textarea', 'select', 'textField', 'textareaField', 'selectField', 'checkbox', 'radio', 'toggle', 'choiceGroup', 'slider', 'sliderField', 'toggleButton', 'toggleGroup']],
  ['Affichage de données', ['avatar', 'avatarGroup', 'badge', 'chip', 'tooltip', 'table', 'list', 'listItem', 'figure', 'carousel']],
  ['Retour d’information', ['alert', 'progress', 'skeleton', 'toasts', 'empty']],
  ['Navigation', ['breadcrumbs', 'pagination', 'tabs', 'appBar', 'appBarNav', 'appBarSpacer', 'appBarActions', 'navLink', 'themeToggle']],
  ['Superpositions', ['modal', 'drawer', 'closeButton', 'menu', 'menuItem', 'menuSeparator', 'accordion', 'accordionItem', 'collapsible']],
  ['Sections', ['hero', 'footer', 'siteFooter', 'footerColumn', 'footerBottom', 'stat', 'statGroup', 'steps', 'timeline', 'timelineItem', 'mockup']],
  ['Styles', ['styles', 'stylesheet', 'stylesUrl', 'theme', 'themeScript']],
]

export default () =>
  docsLayout({
    title: 'Composants',
    description:
      'sitelo-ui : boutons, cartes, formulaires, tableaux, modales et le reste, sous forme de fonctions qui renvoient du HTML.',
    activeHref: '/fr/docs/ui',
    children: [
      p(
        'sitelo-ui est une bibliothèque de composants pour sitelo. Chaque composant est une fonction qui renvoie une chaîne de HTML : il s’imbrique donc directement dans un arbre ',
        a({ href: 'https://www.npmjs.com/package/javascript-to-html' }, 'javascript-to-html'),
        ', sans rien entre les deux — pas de compilateur, pas de runtime, pas d’hydratation. Ce que vous écrivez est exactement ce qui atterrit dans ',
        code('dist/'),
        '.',
      ),
      p(
        'Elle est livrée avec sitelo, sous le point d’entrée ',
        code('sitelo/ui'),
        '.',
      ),

      h2('Démarrage'),
      codeBlock('Terminal', s.install, 'bash'),
      p(
        'Placez ',
        code('styles()'),
        ' dans le head et appelez les composants dans le body. C’est toute la configuration :',
      ),
      codeBlock('src/index.ht.js', s.page, 'javascript'),
      p(
        'Les noms des composants correspondent volontairement à ce qu’ils rendent, ce qui fait que quelques-uns — ',
        code('button'),
        ', ',
        code('input'),
        ', ',
        code('table'),
        ', ',
        code('link'),
        ', ',
        code('code'),
        ', ',
        code('select'),
        ', ',
        code('progress'),
        ' — entrent en collision avec les fonctions d’élément de javascript-to-html. Importez la bibliothèque comme espace de noms quand vous avez besoin des deux :',
      ),
      codeBlock('src/index.ht.js', s.namespace, 'javascript'),

      h2('La convention d’appel'),
      p(
        'Chaque composant prend un objet de props optionnel suivi de ses enfants, exactement comme un élément javascript-to-html. Les props que le composant comprend sont consommées par leur nom ; ',
        strong('tout le reste est transmis à l’élément rendu sous forme d’attribut'),
        ', si bien que ',
        code('id'),
        ', ',
        code('data-*'),
        ', ',
        code('aria-*'),
        ' et les attributs d’événement fonctionnent sans que la bibliothèque ait à les énumérer :',
      ),
      codeBlock('Props transmises', s.passthrough, 'javascript'),
      p(
        'Une prop dont le composant ne reconnaît pas la valeur — ',
        code('variant: \'nonsense\''),
        ' — retombe sur la valeur par défaut au lieu de lever une erreur. Une faute de frappe cosmétique ne devrait pas faire échouer un build.',
      ),

      h2('Styles'),
      p(
        code('styles()'),
        ' renvoie un ',
        code('<link>'),
        ' vers un seul fichier, que le navigateur met en cache d’une page à l’autre ; le plugin de sitelo le sert en dev et l’écrit dans le build, sous un nom porteur d’une empreinte du contenu que vous pouvez servir en ',
        code('immutable'),
        '. Avec ',
        code('{ inline: true }'),
        ', il renvoie plutôt un ',
        code('<style>'),
        ' contenant toute la feuille : environ 11 ko sur le réseau, aucune requête supplémentaire, et rien qui puisse disparaître de ',
        code('dist/'),
        ' :',
      ),
      codeBlock('src/index.ht.js', s.inline, 'javascript'),
      p(
        'La feuille couvre chaque composant de la bibliothèque, et un site en utilise une poignée. ',
        code('pruneCss: true'),
        ' dans ',
        code('sitelo.config.js'),
        ' n’écrit que les règles que le build peut effectivement appliquer : le plugin lit les classes ',
        code('su-'),
        ' des pages qu’il vient d’écrire — et des scripts à côté, pour les classes qu’un toast ou une étape ajoutent plus tard — et supprime toute règle dont le sélecteur nomme une classe que rien ne porte. La décision se prend sur la sortie, pas sur les imports : ',
        code("button({ variant: 'soft' })"),
        ' garde ',
        code('.su-btn--soft'),
        ', un simple ',
        code('button()'),
        ' non. Une feuille liée est réduite à l’échelle du site et reçoit un nouveau hash, chaque ',
        code('<link>'),
        ' étant réécrit en conséquence ; une feuille inline est réduite à sa propre page. Une douzaine de composants tiennent en 2–3 ko gzip. Le serveur de dev sert toujours la feuille entière ; le balisage que le build ne voit jamais — celui d’un îlot serveur, par exemple — doit nommer ses classes dans ',
        code('keep'),
        ' :',
      ),
      codeBlock('sitelo.config.js', s.pruneCss, 'javascript'),

      h3('Thèmes'),
      p(
        'Tout repose sur des propriétés personnalisées CSS déclarées sur ',
        code(':root'),
        ' : cinq palettes, une échelle d’espacement, des rayons, la typographie et les ombres. ',
        code('theme()'),
        ' écrit leurs surcharges et accepte des noms en camelCase (',
        code('radiusMd'),
        ' → ',
        code('--su-radius-md'),
        '), des objets de palette ou des propriétés personnalisées littérales :',
      ),
      codeBlock('src/index.ht.js', s.theming, 'javascript'),
      p(
        'Le mode sombre se résout tout seul à partir de ',
        code('prefers-color-scheme'),
        '. Donner à ',
        code('data-theme'),
        ' ou ',
        code('data-su-theme'),
        ' la valeur ',
        code('light'),
        ' ou ',
        code('dark'),
        ' sur n’importe quel ancêtre l’emporte — c’est exactement ce que fait ',
        code('themeToggle()'),
        ' :',
      ),
      codeBlock('src/index.ht.js', s.themeToggle, 'javascript'),
      p(
        'Ou changez tout le rendu d’un coup : ',
        code("styles({ preset: 'neumorphism' })"),
        ' lie la feuille d’un préréglage juste après la feuille principale, chaque composant la suit, et ',
        code('theme()'),
        ' fonctionne toujours par-dessus. Vous le verrez en direct dans ',
        a({ href: '/fr/ui/theming#prereglages' }, 'Préréglages'),
        ', sur la page Thématisation.',
      ),

      h2('JavaScript, et le peu qu’il en faut'),
      p(
        'La plupart des composants n’en ont pas besoin. La modale et le tiroir sont des éléments ',
        code('popover'),
        ' : le navigateur gère l’ouverture, l’arrière-plan, le clic à l’extérieur et Échap. L’accordéon est un ',
        code('<details name>'),
        '. Les menus sont des ',
        code('<details>'),
        '. Les infobulles sont en CSS. Les onglets dont les panneaux changent sur place sont un groupe de boutons radio : chaque onglet est un ',
        code('<label>'),
        ', et le panneau qui suit le radio coché est celui que le CSS affiche.',
      ),
      p('Trois choses réclament vraiment un script, et chacune va le chercher elle-même :'),
      ul(
        li('le bouton de fermeture d’une alerte que l’on peut masquer'),
        li('la fermeture d’un menu par un clic à l’extérieur ou Échap'),
        li('le bouton de bascule de thème'),
      ),
      p('Rien à ajouter à votre fichier d’entrée : l’import est l’attribut d’événement :'),
      codeBlock('Balisage rendu', s.handler, 'html'),
      p('sitelo sert ces modules depuis /su/ pendant le développement et copie dans la build uniquement ceux que vos pages référencent réellement. Chacun pèse bien moins d’un kilo-octet, aucun n’est chargé avant la première interaction, et jusque-là chaque composant s’affiche correctement : les menus s’ouvrent et se ferment seuls, et le bouton de fermeture ne fait rien.'),
      p(
        'L’exception est ',
        code('toast()'),
        ', car rien sur la page ne le déclenche pour vous :',
      ),
      codeBlock('src/main.js', s.client, 'javascript'),

      h2('Exemples'),
      p('Les formulaires relient eux-mêmes leurs libellés, leurs ids, leurs textes d’aide et leurs messages d’erreur :'),
      codeBlock('Un formulaire', s.form, 'javascript'),
      p('Une modale est un popover, et son déclencheur est n’importe quel bouton qui pointe vers son id :'),
      codeBlock('Une modale', s.modal, 'javascript'),
      p('Les onglets existent sous deux formes : des liens, ou des panneaux.'),
      codeBlock('Onglets', s.tabs, 'javascript'),
      p(
        'Les tableaux prennent ',
        code('columns'),
        ' et ',
        code('rows'),
        ', avec une fonction ',
        code('render'),
        ' partout où une cellule demande plus qu’une valeur :',
      ),
      codeBlock('Un tableau', s.table, 'javascript'),
      p(
        'Le dossier ',
        code('examples/ui'),
        ' du dépôt affiche tous les composants sur une seule page — c’est le moyen le plus rapide de voir l’ensemble.',
      ),

      h2('Référence des composants'),
      p(
        'Tous les exports, par groupe. Les props sont typées : ',
        code('sitelo/ui'),
        ' livre des fichiers ',
        code('.d.ts'),
        ', de sorte qu’un éditeur complète ',
        code('variant'),
        ', ',
        code('color'),
        ' et ',
        code('size'),
        ' aussi bien en JavaScript qu’en TypeScript.',
      ),
      div(
        { class: 'docs-table-scroll' },
        table(
          { class: 'docs-table docs-table--wrap-last' },
          thead(tr(th('Groupe'), th('Composants'))),
          tbody(
            ...GROUPS.map(([group, names]) =>
              tr(td(group), td(names.map((name) => code(name)).join(', '))),
            ),
          ),
        ),
      ),
      p(
        'Deux noms s’écartent de ce à quoi on s’attend : l’interrupteur s’appelle ',
        code('toggle'),
        ', parce que ',
        code('switch'),
        ' est un mot réservé et ne peut pas servir de liaison d’import ; et le lien stylé est exporté à la fois comme ',
        code('link'),
        ' et comme ',
        code('textLink'),
        ', pour cohabiter avec le ',
        code('link'),
        ' de javascript-to-html. ',
        code('table'),
        ', ',
        code('input'),
        ', ',
        code('select'),
        ' et ',
        code('progress'),
        ' disposent de la même échappatoire : ',
        code('dataTable'),
        ', ',
        code('textInput'),
        ', ',
        code('selectField'),
        ', ',
        code('progressBar'),
        '.',
      ),
    ],
  })
