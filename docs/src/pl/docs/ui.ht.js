import {
  a,
  div,
  h2,
  h3,
  li,
  p,
  strong,
  table,
  tbody,
  td,
  th,
  thead,
  tr,
  ul,
} from 'javascript-to-html'
import { code, codeBlock, docsLayout } from '../../lib/pl.js'
import { uiSnippets } from '../../lib/snippets/ui.js'

const s = uiSnippets('pl')

/** Tabela referencyjna komponentów, jeden wiersz na grupę. */
const GROUPS = [
  ['Układ', ['container', 'stack', 'grid', 'divider', 'card', 'cardHeader', 'cardTitle', 'cardSubtitle', 'cardMedia', 'cardBody', 'cardFooter', 'aspectRatio']],
  ['Typografia', ['text', 'heading', 'link', 'code', 'inlineCode', 'kbd', 'visuallyHidden', 'prose']],
  ['Pola formularza', ['button', 'iconButton', 'buttonGroup', 'field', 'input', 'textarea', 'select', 'textField', 'textareaField', 'selectField', 'checkbox', 'radio', 'toggle', 'choiceGroup', 'slider', 'sliderField', 'toggleButton', 'toggleGroup']],
  ['Prezentacja danych', ['avatar', 'avatarGroup', 'badge', 'chip', 'tooltip', 'table', 'list', 'listItem', 'figure', 'carousel']],
  ['Komunikaty', ['alert', 'progress', 'skeleton', 'toasts', 'empty']],
  ['Nawigacja', ['breadcrumbs', 'pagination', 'tabs', 'appBar', 'appBarNav', 'appBarSpacer', 'appBarActions', 'navLink', 'themeToggle']],
  ['Warstwy', ['modal', 'drawer', 'closeButton', 'menu', 'menuItem', 'menuSeparator', 'accordion', 'accordionItem', 'collapsible']],
  ['Sekcje', ['hero', 'footer', 'siteFooter', 'footerColumn', 'footerBottom', 'stat', 'statGroup', 'steps', 'timeline', 'timelineItem', 'mockup']],
  ['Stylowanie', ['styles', 'stylesheet', 'stylesUrl', 'theme', 'themeScript']],
]

export default () =>
  docsLayout({
    title: 'Komponenty',
    description:
      'sitelo-ui: przyciski, karty, formularze, tabele, okna modalne i reszta, jako funkcje zwracające HTML.',
    activeHref: '/pl/docs/ui',
    children: [
      p(
        'sitelo-ui to biblioteka komponentów dla sitelo. Każdy komponent jest funkcją zwracającą ciąg HTML, więc zagnieżdża się wprost w drzewie ',
        a(
          { href: 'https://www.npmjs.com/package/javascript-to-html' },
          'javascript-to-html',
        ),
        ' bez niczego po drodze — bez kompilatora, bez runtime’u, bez hydratacji. To, co budujesz, ląduje w ',
        code('dist/'),
        '.',
      ),
      p(
        'Jest dołączone do sitelo, pod punktem wejścia ',
        code('sitelo/ui'),
        '.',
      ),

      h2('Szybki start'),
      codeBlock('Terminal', s.install, 'bash'),
      p(
        'Wstaw ',
        code('styles()'),
        ' do head i wywołuj komponenty w body. To cała konfiguracja:',
      ),
      codeBlock('src/index.ht.js', s.page, 'javascript'),
      p(
        'Nazwy komponentów celowo odpowiadają temu, co renderują, co oznacza, że kilka z nich — ',
        code('button'),
        ', ',
        code('input'),
        ', ',
        code('table'),
        ', ',
        code('link'),
        ', ',
        code('code'),
        ', ',
        code('select'),
        ', ',
        code('progress'),
        ' — zderza się z funkcjami elementów z javascript-to-html. Gdy potrzebujesz obu, zaimportuj bibliotekę jako przestrzeń nazw:',
      ),
      codeBlock('src/index.ht.js', s.namespace, 'javascript'),

      h2('Konwencja wywołania'),
      p(
        'Każdy komponent przyjmuje opcjonalny obiekt propsów, a po nim dzieci — dokładnie jak element javascript-to-html. Propsy, które komponent rozumie, są konsumowane po nazwie; ',
        strong(
          'cała reszta przechodzi na renderowany element jako atrybut',
        ),
        ', więc ',
        code('id'),
        ', ',
        code('data-*'),
        ', ',
        code('aria-*'),
        ' i atrybuty zdarzeń działają bez tego, żeby biblioteka musiała je wyliczać:',
      ),
      codeBlock('Propsy przechodzące', s.passthrough, 'javascript'),
      p(
        'Props, którego wartości komponent nie rozpoznaje — ',
        code("variant: 'nonsense'"),
        ' — wraca do wartości domyślnej, zamiast rzucać błędem. Kosmetyczna literówka nie powinna przerywać buildu.',
      ),

      h2('Style'),
      p(
        code('styles()'),
        ' zwraca ',
        code('<link>'),
        ' do jednego pliku, który przeglądarka buforuje na wszystkich stronach; wtyczka sitelo serwuje go w dev i zapisuje do buildu, pod nazwą z hashem treści, którą możesz serwować jako ',
        code('immutable'),
        '. ',
        code('{ inline: true }'),
        ' zwraca zamiast tego ',
        code('<style>'),
        ' z całym arkuszem — około 11 kB w sieci, żadnego dodatkowego żądania i nic, co mogłoby zginąć z ',
        code('dist/'),
        ':',
      ),
      codeBlock('src/index.ht.js', s.inline, 'javascript'),
      p(
        'Arkusz obejmuje każdy komponent biblioteki, a witryna używa garstki. ',
        code('pruneCss: true'),
        ' w ',
        code('sitelo.config.js'),
        ' zapisuje tylko te reguły, które build potrafi dopasować: wtyczka czyta klasy ',
        code('su-'),
        ' ze stron, które właśnie zapisała — i ze skryptów obok nich, dla klas dokładanych później przez powiadomienie albo krok — i odrzuca każdą regułę, której selektor wymienia klasę, której nikt nie nosi. Decyduje o tym wynik, a nie importy, więc ',
        code("button({ variant: 'soft' })"),
        ' zachowuje ',
        code('.su-btn--soft'),
        ', a zwykły ',
        code('button()'),
        ' nie. Podlinkowany arkusz jest przycinany względem całej witryny i dostaje nowy hash, a każdy ',
        code('<link>'),
        ' zostaje przepisany; wbudowany przycina się do własnej strony. Kilkanaście komponentów to 2–3 kB po gzipie. Dev serwuje cały arkusz; znaczniki, których build nigdy nie widzi — na przykład z wyspy serwerowej — wymagają wymienienia swoich klas w ',
        code('keep'),
        ':',
      ),
      codeBlock('sitelo.config.js', s.pruneCss, 'javascript'),

      h3('Motywy'),
      p(
        'Wszystkim sterują własne właściwości CSS na ',
        code(':root'),
        ' — pięć palet, skala odstępów, promienie, krój i cienie. ',
        code('theme()'),
        ' zapisuje dla nich nadpisania i przyjmuje nazwy w camelCase (',
        code('radiusMd'),
        ' → ',
        code('--su-radius-md'),
        '), obiekty palet albo dosłowne właściwości własne:',
      ),
      codeBlock('src/index.ht.js', s.theming, 'javascript'),
      p(
        'Tryb ciemny rozstrzyga się sam na podstawie ',
        code('prefers-color-scheme'),
        '. Ustawienie ',
        code('data-theme'),
        ' albo ',
        code('data-su-theme'),
        ' na ',
        code('light'),
        ' lub ',
        code('dark'),
        ' na dowolnym przodku to nadpisuje — i właśnie to robi ',
        code('themeToggle()'),
        ':',
      ),
      codeBlock('src/index.ht.js', s.themeToggle, 'javascript'),

      h2('JavaScript i jak mało go tu jest'),
      p(
        'Większość komponentów nie potrzebuje go wcale. Okno modalne i szuflada to elementy ',
        code('popover'),
        ', więc otwieraniem, tłem, kliknięciem poza i Escape zajmuje się przeglądarka. Akordeon to ',
        code('<details name>'),
        '. Menu to ',
        code('<details>'),
        '. Podpowiedzi to CSS. Zakładki, których panele podmieniają się w miejscu, to grupa radio: każda zakładka to ',
        code('<label>'),
        ', a panel następujący po zaznaczonym radiu jest tym, który CSS pokazuje.',
      ),
      p('Trzy rzeczy chcą skryptu i każda sama po niego sięga:'),
      ul(
        li('przycisk zamykający w alercie, który można zamknąć'),
        li('zamykanie menu kliknięciem poza nim albo Escape'),
        li('przełącznik motywu'),
      ),
      p(
        'Nie ma czego dodawać do pliku wejściowego — importem jest atrybut zdarzenia:',
      ),
      codeBlock('Wyrenderowane znaczniki', s.handler, 'html'),
      p(
        'sitelo serwuje te moduły z /su/ podczas pracy i kopiuje do buildu te, do których Twoje strony faktycznie się odwołują. Każdy waży znacznie poniżej kilobajta, żaden nie jest pobierany przed pierwszą interakcją, a każdy komponent renderuje się poprawnie, dopóki to nie nastąpi: menu otwierają się i zamykają same, a przycisk zamykania nic nie robi.',
      ),
      p(
        'Wyjątkiem jest ',
        code('toast()'),
        ', bo nic na stronie nie uruchamia go za Ciebie:',
      ),
      codeBlock('src/main.js', s.client, 'javascript'),

      h2('Przykłady'),
      p(
        'Formularze same łączą swoje etykiety, identyfikatory, teksty pomocy i komunikaty błędów:',
      ),
      codeBlock('Formularz', s.form, 'javascript'),
      p(
        'Okno modalne to popover, a jego wyzwalaczem jest dowolny przycisk wskazujący na jego id:',
      ),
      codeBlock('Okno modalne', s.modal, 'javascript'),
      p('Zakładki występują w dwóch postaciach — odnośniki albo panele:'),
      codeBlock('Zakładki', s.tabs, 'javascript'),
      p(
        'Tabele przyjmują ',
        code('columns'),
        ' i ',
        code('rows'),
        ', a funkcja ',
        code('render'),
        ' wchodzi wszędzie tam, gdzie komórka potrzebuje czegoś więcej niż wartości:',
      ),
      codeBlock('Tabela', s.table, 'javascript'),
      p(
        'Katalog ',
        code('examples/ui'),
        ' w repozytorium renderuje każdy komponent na jednej stronie — to najszybszy sposób, by zobaczyć cały zestaw.',
      ),

      h2('Referencja komponentów'),
      p(
        'Każdy eksport, grupami. Propsy są otypowane: ',
        code('sitelo/ui'),
        ' dostarcza pliki ',
        code('.d.ts'),
        ', więc edytor podpowiada ',
        code('variant'),
        ', ',
        code('color'),
        ' i ',
        code('size'),
        ' zarówno w JavaScripcie, jak i w TypeScripcie.',
      ),
      div(
        { class: 'docs-table-scroll' },
        table(
          { class: 'docs-table docs-table--wrap-last' },
          thead(tr(th('Grupa'), th('Komponenty'))),
          tbody(
            ...GROUPS.map(([group, names]) =>
              tr(td(group), td(names.map((name) => code(name)).join(', '))),
            ),
          ),
        ),
      ),
      p(
        'Dwie nazwy odbiegają od oczekiwań: przełącznik to ',
        code('toggle'),
        ', bo ',
        code('switch'),
        ' jest słowem zastrzeżonym i nie może być wiązaniem importu; a ostylowana kotwica jest eksportowana i jako ',
        code('link'),
        ', i jako ',
        code('textLink'),
        ', żeby mogła stać obok ',
        code('link'),
        ' z javascript-to-html. ',
        code('table'),
        ', ',
        code('input'),
        ', ',
        code('select'),
        ' i ',
        code('progress'),
        ' mają to samo wyjście awaryjne: ',
        code('dataTable'),
        ', ',
        code('textInput'),
        ', ',
        code('selectField'),
        ', ',
        code('progressBar'),
        '.',
      ),

      h2('Dodatki'),
      p(
        'Drugi punkt wejścia, ',
        code('sitelo/ui-extras'),
        ', mieści komponenty nie dla wszystkich — faktury i efekty, na początek ziarno filmowe. Każdy przynosi własny arkusz stylów, ',
        code('grainStyles()'),
        ' obok ',
        code('styles()'),
        ', więc strona podlinkowuje tylko to, czego używa, a ',
        code('sitelo/ui-extras/client'),
        ' niesie ich wywołania po stronie strony. Są skatalogowane w ',
        a({ href: '/ui-extras' }, 'sitelo UI extras'),
        '.',
      ),
    ],
  })
