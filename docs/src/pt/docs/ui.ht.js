import {
  a,
  div,
  h2,
  h3,
  li,
  p,
  strong,
  table,
  tbody,
  td,
  th,
  thead,
  tr,
  ul,
} from 'javascript-to-html'
import { code, codeBlock, docsLayout } from '../../lib/pt.js'
import { uiSnippets } from '../../lib/snippets/ui.js'

const s = uiSnippets('pt')

/** The component reference table, one row per group. */
const GROUPS = [
  ['Disposição', ['container', 'stack', 'grid', 'divider', 'card', 'cardHeader', 'cardTitle', 'cardSubtitle', 'cardMedia', 'cardBody', 'cardFooter', 'aspectRatio']],
  ['Tipografia', ['text', 'heading', 'link', 'code', 'inlineCode', 'kbd', 'visuallyHidden', 'prose']],
  ['Entradas', ['button', 'iconButton', 'buttonGroup', 'field', 'input', 'textarea', 'select', 'textField', 'textareaField', 'selectField', 'checkbox', 'radio', 'toggle', 'choiceGroup', 'slider', 'sliderField', 'toggleButton', 'toggleGroup']],
  ['Apresentação de dados', ['avatar', 'avatarGroup', 'badge', 'chip', 'tooltip', 'table', 'list', 'listItem', 'figure']],
  ['Feedback', ['alert', 'progress', 'spinner', 'skeleton', 'toasts', 'empty']],
  ['Navegação', ['breadcrumbs', 'pagination', 'tabs', 'appBar', 'appBarNav', 'appBarSpacer', 'appBarActions', 'navLink', 'themeToggle']],
  ['Sobreposições', ['modal', 'drawer', 'closeButton', 'menu', 'menuItem', 'menuSeparator', 'accordion', 'accordionItem', 'collapsible']],
  ['Secções', ['hero', 'footer', 'siteFooter', 'footerColumn', 'footerBottom', 'stat', 'statGroup', 'steps', 'timeline', 'timelineItem', 'mockup']],
  ['Estilos', ['styles', 'stylesheet', 'theme', 'themeScript']],
]

export default () =>
  docsLayout({
    title: 'Componentes',
    description:
      'sitelo-ui: botões, cartões, formulários, tabelas, modais e o resto, como funções que devolvem HTML.',
    activeHref: '/pt/docs/ui',
    children: [
      p(
        'O sitelo-ui é uma biblioteca de componentes para o sitelo. Cada componente é uma função que devolve uma string de HTML, por isso encaixa diretamente numa árvore ',
        a({ href: 'https://www.npmjs.com/package/javascript-to-html' }, 'javascript-to-html'),
        ' sem nada pelo meio — sem compilador, sem runtime, sem hidratação. O que constróis é o que fica em ',
        code('dist/'),
        '.',
      ),
      p(
        'Vem com o sitelo, no ponto de entrada ',
        code('sitelo/ui'),
        '.',
      ),

      h2('Primeiros passos'),
      codeBlock('Terminal', s.install, 'bash'),
      p(
        'Põe ',
        code('styles()'),
        ' no head e chama os componentes no body. A configuração é toda esta:',
      ),
      codeBlock('src/index.ht.js', s.page, 'javascript'),
      p(
        'Os nomes dos componentes correspondem de propósito àquilo que renderizam, o que faz com que alguns deles — ',
        code('button'),
        ', ',
        code('input'),
        ', ',
        code('table'),
        ', ',
        code('link'),
        ', ',
        code('code'),
        ', ',
        code('select'),
        ', ',
        code('progress'),
        ' — colidam com as funções de elemento do javascript-to-html. Importa a biblioteca como espaço de nomes quando precisares das duas:',
      ),
      codeBlock('src/index.ht.js', s.namespace, 'javascript'),

      h2('A convenção de chamada'),
      p(
        'Cada componente recebe um objeto de props opcional seguido dos filhos, tal como um elemento do javascript-to-html. As props que o componente conhece são consumidas pelo nome; ',
        strong('tudo o resto passa para o elemento renderizado como atributo'),
        ', por isso ',
        code('id'),
        ', ',
        code('data-*'),
        ', ',
        code('aria-*'),
        ' e os atributos de evento funcionam sem que a biblioteca tenha de os enumerar:',
      ),
      codeBlock('Props que passam adiante', s.passthrough, 'javascript'),
      p(
        'Uma prop cujo valor o componente não reconhece — ',
        code('variant: \'nonsense\''),
        ' — recai no valor por omissão em vez de lançar um erro. Uma gralha cosmética não deve fazer falhar uma compilação.',
      ),

      h2('Estilos'),
      p(
        code('styles()'),
        ' devolve um elemento ',
        code('<style>'),
        ' com a folha completa, minificada. São cerca de 7 kB na rede e não pode desaparecer de ',
        code('dist/'),
        ' — é por isso que é a opção por omissão. Se preferires ligá-la uma só vez e deixar o browser guardá-la em cache entre páginas, importa o CSS a partir de um ficheiro de entrada empacotado e o Vite emite-o:',
      ),
      codeBlock('src/main.js', s.linked, 'javascript'),
      p('Usa uma ou outra, não as duas.'),

      h3('Temas'),
      p(
        'Tudo é controlado por propriedades personalizadas de CSS em ',
        code(':root'),
        ': cinco paletas, uma escala de espaçamento, raios, tipografia e sombras. ',
        code('theme()'),
        ' escreve as substituições e aceita nomes em camelCase (',
        code('radiusMd'),
        ' → ',
        code('--su-radius-md'),
        '), objetos de paleta ou propriedades personalizadas literais:',
      ),
      codeBlock('src/index.ht.js', s.theming, 'javascript'),
      p(
        'O modo escuro resolve-se sozinho a partir de ',
        code('prefers-color-scheme'),
        '. Definir ',
        code('data-theme'),
        ' ou ',
        code('data-su-theme'),
        ' como ',
        code('light'),
        ' ou ',
        code('dark'),
        ' em qualquer ascendente sobrepõe-se a isso — e é exatamente o que ',
        code('themeToggle()'),
        ' faz:',
      ),
      codeBlock('src/index.ht.js', s.themeToggle, 'javascript'),

      h2('JavaScript, e o pouco que é preciso'),
      p(
        'A maioria dos componentes não precisa de nenhum. O modal e a gaveta são elementos ',
        code('popover'),
        ', por isso é o browser que trata da abertura, do fundo, do clique fora e do Escape. O acordeão é um ',
        code('<details name>'),
        '. Os menus são ',
        code('<details>'),
        '. As dicas são CSS.',
      ),
      p('Quatro coisas querem mesmo um script:'),
      ul(
        li('os separadores cujos painéis trocam no lugar'),
        li('o botão de fechar de um alerta que se pode dispensar'),
        li(
          code('toast()'),
          ' e a região ',
          code('toasts()'),
        ),
        li('o alternador de tema'),
      ),
      p('Importa-o a partir de qualquer ficheiro de entrada empacotado:'),
      codeBlock('src/main.js', s.client, 'javascript'),
      p('Tem cerca de 3 kB, escuta no documento em vez de elemento a elemento, e todos os componentes que toca renderizam corretamente sem ele: os separadores com painel mostram aquele que o servidor marcou como ativo, os menus continuam a abrir e o botão de dispensar simplesmente não faz nada.'),

      h2('Exemplos'),
      p('Os formulários ligam sozinhos as suas etiquetas, ids, textos de ajuda e mensagens de erro:'),
      codeBlock('Um formulário', s.form, 'javascript'),
      p('Um modal é um popover e o seu gatilho é qualquer botão que aponte para o seu id:'),
      codeBlock('Um modal', s.modal, 'javascript'),
      p('Os separadores existem em duas formas — ligações ou painéis:'),
      codeBlock('Separadores', s.tabs, 'javascript'),
      p(
        'As tabelas recebem ',
        code('columns'),
        ' e ',
        code('rows'),
        ', com uma função ',
        code('render'),
        ' onde uma célula precisar de mais do que um valor:',
      ),
      codeBlock('Uma tabela', s.table, 'javascript'),
      p(
        'A pasta ',
        code('examples/ui'),
        ' do repositório renderiza todos os componentes numa única página — é a forma mais rápida de ver o conjunto completo.',
      ),

      h2('Referência de componentes'),
      p(
        'Todas as exportações, por grupo. As props têm tipos: o ',
        code('sitelo/ui'),
        ' traz ficheiros ',
        code('.d.ts'),
        ', por isso o editor completa ',
        code('variant'),
        ', ',
        code('color'),
        ' e ',
        code('size'),
        ' tanto em JavaScript como em TypeScript.',
      ),
      div(
        { class: 'docs-table-scroll' },
        table(
          { class: 'docs-table docs-table--wrap-last' },
          thead(tr(th('Grupo'), th('Componentes'))),
          tbody(
            ...GROUPS.map(([group, names]) =>
              tr(td(group), td(names.map((name) => code(name)).join(', '))),
            ),
          ),
        ),
      ),
      p(
        'Dois nomes fogem ao esperado: o interruptor é ',
        code('toggle'),
        ', porque ',
        code('switch'),
        ' é uma palavra reservada e não pode ser uma ligação de importação; e a ligação com estilo é exportada como ',
        code('link'),
        ' e como ',
        code('textLink'),
        ', para poder conviver com o ',
        code('link'),
        ' do javascript-to-html. ',
        code('table'),
        ', ',
        code('input'),
        ', ',
        code('select'),
        ' e ',
        code('progress'),
        ' têm a mesma saída de emergência: ',
        code('dataTable'),
        ', ',
        code('textInput'),
        ', ',
        code('selectField'),
        ', ',
        code('progressBar'),
        '.',
      ),
    ],
  })
