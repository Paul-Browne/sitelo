import { a, h2, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/pt.js'
import { islandsSnippets } from '../../lib/snippets/islands.js'

const s = islandsSnippets('pt')

export default () =>
  docsLayout({
    title: 'Ilhas de servidor',
    description:
      'Mantém as páginas estáticas e renderiza regiões marcadas no servidor, no momento do pedido.',
    activeHref: '/pt/docs/islands',
    children: [
      p(
        'Às vezes uma região de uma página, de resto estática, precisa de dados frescos a cada pedido — os comentários por baixo de um artigo em cache, uma etiqueta de stock numa página de produto. As ilhas de servidor mantêm a página estática e renderizam só essa região num servidor, quando a página é vista.',
      ),
      h2('1. Escreve a ilha'),
      p(
        'Uma ilha é um módulo de fragmento dentro de ',
        code('src/islands/'),
        ' — um ficheiro ',
        code('.js'),
        ' ou ',
        code('.ts'),
        ' normal (não ',
        code('.ht.js'),
        ', porque as ilhas são fragmentos, não páginas). A mesma ideia de todo o sitelo: uma função que devolve HTML.',
      ),
      codeBlock('src/islands/comments.js', s.islandModule, 'javascript'),
      p(
        'Recebe ',
        code('{ name, props, request }'),
        ' e tem de devolver uma string HTML. Os módulos de ilha são exclusivos do servidor — o código sem referências dentro de ',
        code('src/'),
        ' nunca chega ao navegador.',
      ),
      h2('2. Coloca-a numa página'),
      p(
        'Importa ',
        code('island()'),
        ' de ',
        code('sitelo/islands'),
        '. A compilação estática publica o HTML de reserva; as props são embutidas no marcador, por isso mantém-nas pequenas e sem segredos.',
      ),
      pageCodeTabs({
        file: 'src/blog/[slug].ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Acrescenta o carregador de cliente'),
      p(
        'Um script minúsculo vai buscar cada fragmento renderizado e troca-o. Passa pelo pipeline de recursos habitual, por isso basta um ponto de entrada ',
        code('src/islands.js'),
        ' normal:',
      ),
      codeBlock('src/islands.js', s.loader, 'javascript'),
      p(
        'Em ',
        code('sitelo'),
        ' (dev) e em ',
        code('sitelo preview'),
        ' isto já funciona — ambos servem as ilhas em ',
        code('/_sitelo/islands/<name>'),
        ' a partir de ',
        code('src/islands/'),
        '. O preview carrega módulos ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' nativos (tal como um host Node); as ilhas em TypeScript funcionam em desenvolvimento graças ao Vite.',
      ),
      h2('Produção'),
      p(
        'O teu alojamento estático continua a servir as páginas. Monta um pequeno handler onde correres código de servidor — Node, serverless ou uma função edge — e ele renderiza os mesmos módulos de ilha. Para um percurso completo com um host Node executável e esboços para Netlify e Vercel, vê o ',
        a({ href: '/pt/examples/islands' }, 'exemplo de ilhas de servidor'),
        '.',
      ),
      codeBlock('islands-function.js', s.server, 'javascript'),
      p(
        'Com http do Node simples ou com express, usa antes ',
        code('createIslandsNodeHandler(options)'),
        ' — as mesmas opções, assinatura ',
        code('(req, res, next)'),
        '. Podes ligar automaticamente todos os módulos ',
        code('.js'),
        ' / ',
        code('.mjs'),
        ' de ',
        code('src/islands/'),
        ' com ',
        code('createIslandsFromDirectory'),
        '. Se o carregador for buscar a outra origem ou caminho, passa ',
        code("mountIslands({ endpoint: 'https://api.example.com/islands' })"),
        ' e faz corresponder à opção ',
        code('endpoint'),
        ' do handler.',
      ),
      h2('Lista de estabilidade'),
      ul(
        { class: 'docs-list' },
        li(
          code('island()'),
          ' / ',
          code('mountIslands()'),
          ' / ',
          code('createIslandsHandler'),
          ' / ',
          code('createIslandsNodeHandler'),
          ' — API pública, considerada estável',
        ),
        li(
          code('sitelo'),
          ' e ',
          code('sitelo preview'),
          ' servem ambos ',
          code('/_sitelo/islands'),
          ' a partir de ',
          code('src/islands/'),
        ),
        li(
          code('createIslandsFromDirectory'),
          ' — o mesmo mapa para hosts Node e para o preview (nativo ',
          code('.js'),
          ' / ',
          code('.mjs'),
          ' / ',
          code('.cjs'),
          ')',
        ),
        li(
          'Esboços de host em ',
          a({ href: '/pt/examples/islands' }, 'examples/islands'),
          ': ',
          code('server.js'),
          ' para Node, função Netlify + rewrite, serverless Vercel + rewrite',
        ),
        li(
          'As props ficam pequenas e sem segredos (query string de GET) — é intencional; vai buscar os segredos dentro da ilha, no servidor',
        ),
        li(
          'As props vêm do cliente — valida-as, ou assina-as com ',
          code('SITELO_ISLANDS_SECRET'),
        ),
      ),
      h2('Estratégias de carregamento'),
      p(
        'Por omissão cada ilha vai buscar os dados assim que a página carrega, por isso uma página com oito ilhas faz oito pedidos em simultâneo durante a primeira pintura. Passa ',
        code('when'),
        ' para adiar as que não estão logo à vista.',
      ),
      codeBlock('islands-when', s.strategy, 'js'),
      ul(
        { class: 'docs-list' },
        li(code("'load'"), ' (por omissão) — de imediato, junto com todas as outras ilhas'),
        li(
          code("'idle'"),
          ' — num ',
          code('requestIdleCallback'),
          ' (com um temporizador como alternativa)',
        ),
        li(code("'visible'"), ' — quando entra no ecrã, via IntersectionObserver'),
      ),
      p(
        code('rootMargin'),
        ' só se aplica a ',
        code("'visible'"),
        ' e vale ',
        code("'200px'"),
        ' por omissão. As ilhas também expiram em vez de ficarem a rodar para sempre:',
      ),
      codeBlock('islands-mount-options', s.mountOptions, 'js'),
      p(
        code('mountIslands()'),
        ' resolve assim que as ilhas imediatas assentam — as adiadas carregam mais tarde por si e, deliberadamente, não são esperadas. Uma ilha que falhe ou expire mantém o seu HTML de reserva.',
      ),
      h2('As props são entrada não fiável'),
      p(
        'As props de uma ilha vêm do cliente. São embutidas na página, devolvidas no pedido, e qualquer pessoa as pode editar pelo caminho:',
      ),
      codeBlock('islands-forged', s.forged, 'http'),
      p(
        'Trata as props que a tua ilha recebe exatamente como um parâmetro de query — valida-as, e nunca as uses para ir buscar dados a que quem visita não tenha já direito.',
      ),
      p(
        'Quando as props selecionam dados privilegiados, assina-as. Define um segredo e o sitelo assina cada marcador na compilação, rejeitando tudo o resto com um ',
        code('403'),
        ':',
      ),
      codeBlock('islands-secret', s.secret, 'bash'),
      p(
        'A mesma variável é lida por ',
        code('sitelo'),
        ', ',
        code('sitelo preview'),
        ' e ',
        code('createIslandsHandler'),
        ', por isso desenvolvimento, preview e produção concordam. Dá o mesmo segredo ao teu host de produção. Preferes defini-lo em código?',
      ),
      codeBlock('islands-configure', s.configure, 'js'),
      p(
        'As assinaturas são HMAC-SHA256 sobre o nome da ilha e as suas props, por isso uma assinatura emitida para uma ilha não pode ser reutilizada noutra. Assinar prova que as props vieram da tua compilação — não as esconde, por isso continuam a não poder ser segredos. Sem segredo, as props são aceites tal como estão e validá-las cabe inteiramente ao teu módulo de ilha.',
      ),
      h2('Coisas a saber'),
      ul(
        { class: 'docs-list' },
        li(
          'Não implementaste nenhum endpoint de ilhas? Fica simplesmente o HTML de reserva — as páginas degradam-se com elegância.',
        ),
        li(
          'Os pedidos são ',
          code('GET'),
          ' com as props na query string, por isso as respostas são cacheáveis — define a opção ',
          code('cacheControl'),
          ' do handler se quiseres que uma CDN segure os fragmentos por um bocado.',
        ),
        li(
          'Durante o carregamento, o marcador leva ',
          code('data-sitelo-island-state="loading"'),
          ' (e depois ',
          code('loaded'),
          ' ou ',
          code('error'),
          ') — prático para o CSS.',
        ),
      ),
    ],
  })
