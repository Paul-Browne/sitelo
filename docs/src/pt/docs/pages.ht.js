import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, docsLayout, pageCodeTabs } from '../../lib/pt.js'
import { pagesSnippets } from '../../lib/snippets/pages.js'

const s = pagesSnippets('pt')

export default () =>
  docsLayout({
    title: 'Escrever páginas',
    description:
      'Como escrever páginas do sitelo — templates de string, ht.js (recomendado) ou JSX.',
    activeHref: '/pt/docs/pages',
    children: [
      p(
        'Qualquer ficheiro terminado numa extensão de página é uma página. Extensões por omissão: ',
        code('.ht.js'),
        ', ',
        code('.html.js'),
        ', e as variantes TypeScript / JSX (',
        code('.ht.ts'),
        ', ',
        code('.ht.tsx'),
        ', …).',
      ),
      p(
        'Se a saída começar por ',
        code('<html>'),
        ', o sitelo acrescenta ',
        code('<!DOCTYPE html>'),
        ' automaticamente. Para a marcação recomendamos o ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' (',
        code('javascript-to-html'),
        '): funções de etiqueta que devolvem strings, sem motor de templates nem runtime do React.',
      ),
      h2('1. Uma função que devolve HTML'),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.fnTemplate,
        ht: s.fnHt,
        jsx: s.fnJsx,
      }),
      p(
        'Os ficheiros JSX usam ',
        code('*.ht.jsx'),
        ' / ',
        code('*.ht.tsx'),
        ' e precisam de ',
        code('react'),
        ' e ',
        code('react-dom'),
        ' instalados. Vê as ',
        a({ href: '#limitacoes-de-jsx' }, 'limitações de JSX'),
        ' mais abaixo — o sitelo transforma JSX em HTML estático na compilação; não é uma aplicação React no navegador.',
      ),
      h2('2. Uma simples string'),
      codeBlock('page.ht.js', s.string, 'javascript'),
      h2('3. Um módulo estruturado'),
      p(
        'Mantém ',
        code('render'),
        ', ',
        code('data'),
        ' e ',
        code('generateStaticParams'),
        ' juntos:',
      ),
      pageCodeTabs({
        file: 'page.ht.js',
        template: s.structuredTemplate,
        ht: s.structuredHt,
        jsx: s.structuredJsx,
      }),
      h2('Limitações de JSX'),
      p(
        'O sitelo consegue renderizar páginas ',
        code('.ht.jsx'),
        ' / ',
        code('.ht.tsx'),
        ' com ',
        code('react-dom/server'),
        ', para poderes escrever com a sintaxe JSX habitual. Isso é uma comodidade de escrita — não uma aplicação React.',
      ),
      p(
        'O React foi feito para o navegador: uma árvore de componentes que monta, hidrata e responde a eventos. O sitelo nunca envia esse runtime com as tuas páginas. Na compilação renderiza o teu JSX para uma string HTML, escreve o ficheiro e fica por aí. Não há bundle do React no cliente, nem hidratação, nem DOM virtual à espera no navegador.',
      ),
      p('Por isso:'),
      ul(
        { class: 'docs-list' },
        li(
          'Os handlers de eventos (',
          code('onClick'),
          ', ',
          code('onChange'),
          ', …) não fazem nada no site final — não há ninguém à escuta. O servidor de desenvolvimento avisa se os encontrar.',
        ),
        li(
          'Os hooks (',
          code('useState'),
          ', ',
          code('useEffect'),
          ', …) não fazem parte da página publicada. Podem correr uma vez durante a renderização da compilação; não guardam estado no navegador.',
        ),
        li(
          'As APIs exclusivas do navegador (',
          code('window'),
          ', ',
          code('document'),
          ') não estão disponíveis enquanto a página é renderizada na compilação.',
        ),
        li(
          code('dangerouslySetInnerHTML'),
          ' serve para injetar strings de HTML; a interatividade continua a precisar de um ',
          code('<script>'),
          ' de cliente à parte.',
        ),
      ),
      p(
        'Para cliques, formulários e outro comportamento de cliente, liga um script normal através do ',
        a({ href: '/pt/docs/assets' }, 'pipeline de recursos'),
        ', ou usa ',
        a({ href: '/pt/docs/islands' }, 'ilhas de servidor'),
        ' para HTML gerado no momento do pedido. Quando não precisares da sintaxe JSX, prefere o ',
        a({ href: 'https://ht.js.org', rel: 'noopener' }, 'ht.js'),
        ' ou templates de string — sem instalar o React, e com um modelo que encaixa em «HTML na compilação».',
      ),
      h2('Contexto de renderização'),
      p('Cada função de página recebe um único argumento:'),
      h3('params'),
      p(
        'Os parâmetros de rota desta página (',
        code('Record<string, string | string[]>'),
        ').',
      ),
      h3('data'),
      p('O que a tua função ', code('data()'), ' devolveu.'),
      h3('page'),
      p(
        'Metadados da rota (',
        code('routePath'),
        ', ',
        code('relativePath'),
        ', …).',
      ),
      h3('dev'),
      p(
        code('true'),
        ' no servidor de desenvolvimento, ',
        code('false'),
        ' na compilação.',
      ),
    ],
  })
