import { a, h2, h3, li, p, ul } from 'javascript-to-html'
import { code, codeBlock, examplesLayout, pageCodeTabs } from '../../lib/pt.js'
import { islandsExampleSnippets } from '../../lib/snippets/examples-islands.js'

const s = islandsExampleSnippets('pt')

export default () =>
  examplesLayout({
    title: 'Ilhas de servidor',
    description:
      'Publica um site sitelo estático e monta um host Node que renderiza ilhas no momento do pedido.',
    activeHref: '/pt/examples/islands',
    children: [
      p(
        'O sitelo gera HTML estático. As ilhas de servidor preenchem as partes que têm de estar frescas — um relógio, comentários, stock, tudo o que depende do pedido. Esta receita constrói uma página com uma ilha de hora e depois corre um pequeno servidor Node que serve ',
        code('dist/'),
        ' e ',
        code('/_sitelo/islands'),
        '.',
      ),
      p(
        'Há uma cópia deste projeto no repositório do sitelo, em ',
        code('examples/islands/'),
        '.',
      ),
      h2('O que obténs'),
      ul(
        { class: 'docs-list' },
        li('Uma página inicial estática com um marcador de ilha e HTML de reserva'),
        li(
          'Um módulo de ilha exclusivo do servidor dentro de ',
          code('src/islands/'),
          ' que nunca chega ao navegador',
        ),
        li(
          'Um carregador de cliente que substitui o fragmento vindo de ',
          code('/_sitelo/islands/<name>'),
        ),
        li(
          'Um ',
          code('server.js'),
          ' com forma de produção — ficheiros estáticos + ',
          code('createIslandsNodeHandler'),
        ),
      ),
      h2('Estrutura do projeto'),
      codeBlock('project', s.structure, 'bash'),
      codeBlock('sitelo.config.js', s.config, 'javascript'),
      h2('1. Módulo de ilha'),
      p(
        'Um ficheiro ',
        code('.js'),
        ' normal (não ',
        code('.ht.js'),
        '). Recebe ',
        code('{ name, props, request }'),
        ' e devolve uma string HTML. Este usa a hora do pedido e o user-agent para veres que é renderizado a cada pedido.',
      ),
      codeBlock('src/islands/time.js', s.island, 'javascript'),
      h2('2. Coloca a ilha numa página'),
      p(
        code('island()'),
        ' embute as props no marcador. A compilação publica a reserva; o carregador substitui-a quando o endpoint responde.',
      ),
      pageCodeTabs({
        file: 'src/index.ht.js',
        template: s.pageTemplate,
        ht: s.pageHt,
        jsx: s.pageJsx,
      }),
      h2('3. Carregador de cliente'),
      codeBlock('src/js/islands.js', s.loader, 'javascript'),
      codeBlock('src/css/styles.css', s.styles, 'css'),
      h2('4. Host Node'),
      p(
        'Depois de ',
        code('sitelo build'),
        ', este processo serve ',
        code('dist/'),
        ' e renderiza as ilhas com ',
        code('createIslandsNodeHandler'),
        ' de ',
        code('sitelo/islands/server'),
        '. Os módulos de ilha ficam fora de ',
        code('dist/'),
        ' — o host importa-os de ',
        code('src/'),
        '.',
      ),
      codeBlock('server.js', s.server, 'javascript'),
      h2('5. Compilar e executar'),
      codeBlock('shell', s.buildRun, 'bash'),
      p(
        'Abre ',
        code('http://localhost:3000'),
        '. Deves ver a reserva por um instante e depois a hora do servidor. Atualiza — a marca temporal muda. Em ',
        code('sitelo'),
        ' (dev) e ',
        code('sitelo preview'),
        ' não precisas do ',
        code('server.js'),
        ': a CLI já serve ',
        code('/_sitelo/islands'),
        '.',
      ),
      h2('Implementação'),
      p('Este exemplo traz esboços de host ao lado do servidor Node:'),
      ul(
        { class: 'docs-list' },
        li(
          code('Node'),
          ' — ',
          code('npm run build && npm start'),
          ' (define ',
          code('PORT'),
          ' na plataforma). Usa ',
          code('createIslandsFromDirectory'),
          '.',
        ),
        li(
          code('Netlify'),
          ' — ',
          code('netlify.toml'),
          ' reescreve ',
          code('/_sitelo/islands/*'),
          ' para ',
          code('netlify/functions/islands.mjs'),
          '.',
        ),
        li(
          code('Vercel'),
          ' — ',
          code('vercel.json'),
          ' reescreve para ',
          code('api/islands/[...path].js'),
          '.',
        ),
        li(
          'Alojamentos só estáticos (GitHub Pages, S3 simples) — os marcadores mantêm a reserva até acrescentares uma função.',
        ),
      ),
      p(
        'Para serverless ou edge noutro sítio, usa ',
        code('createIslandsHandler'),
        ' (',
        code('Request'),
        ' web → ',
        code('Response'),
        ') — vê a ',
        a({ href: '/pt/docs/islands' }, 'documentação de ilhas de servidor'),
        '. Aponta ',
        code('mountIslands({ endpoint })'),
        ' para o URL dessa função se não for da mesma origem.',
      ),
      h2('Notas'),
      h3('Alojamentos só estáticos'),
      p(
        'O GitHub Pages, o S3 simples e alojamentos parecidos não têm processo de servidor. Sem um endpoint de ilhas, o HTML de reserva fica simplesmente lá — as páginas continuam a funcionar, só sem o fragmento ao vivo.',
      ),
      h3('Mantém as props pequenas'),
      p(
        'As props viajam no atributo HTML e na query string do pedido. Não ponhas aí segredos nem cargas grandes — vai buscá-los dentro do módulo de ilha, no servidor.',
      ),
      p(
        a({ href: '/pt/docs/islands' }, 'Documentação de ilhas de servidor'),
        ' · ',
        a({ href: '/pt/examples/basic' }, 'Site básico / implementação'),
        ' · ',
        a({ href: '/pt/examples' }, 'Todos os exemplos'),
      ),
    ],
  })
